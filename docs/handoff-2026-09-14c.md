# Resume handoff — 2026-09-14 (afternoon, after the autonomous session)

Written for the next orchestrator session. Read this file, `AGENTS.md`, `CLAUDE.md`,
`docs/status.md`, `docs/goals.md`, then `git log --oneline -60`, then only the
owning notes named per task. The evening handoff `archive/handoff-2026-09-14b.md`
and the morning one are historical; every item they listed is merged or superseded.

## Installed build and bottle

- Installed DLL: checkpoint `76d7750`, SHA-256
  `608b35d8f7aded08d30220ec279a14c3ac92b55b845de63e56ce32fb1e92c5c9`
  (13,939,273 bytes), record `verification/results/screen-emission-install.json`.
  Rollback: the `f56a393` DLL `2bbe7904…` and its records under
  `/var/folders/l6/…/T/x3-screen-emission-candidate-ZpJagMpd/rollback-*`;
  the `3f06979` DLL `4022a3a4…` under `…/x3-station-voice-candidate-3Rszg5Zr/`.
- EXE `fdbf3418…` and `cxbottle.conf` `cc5d6c00…` unchanged; bottle X3 byte-identical
  to its pre-experiment state (the WMP11 attempt is fully reverted; never repeat it).
- Everything merged today is default-off; the game behaves as before without the
  new options. Candidate procedure: Opus `implement` builds/audits/records, the main
  session installs (user preference, memory `candidate-build-routing`).

## Main branch state (all merged, reviewed, committed; 53 commits since 09:30)

| Area | Commits | Evidence |
| --- | --- | --- |
| Run 11 recorded (fade witness clean, cutout armed, docking port unchanged) | `a56eedb` | `docs/archive/user-runs-completed.md` §11 |
| Voice load hang: root cause + fix (game Inits a DMO wrapper with the unregistered Speech decoder, adds the pin-less wrapper, Pause fails, Wine's release loop spins; hook at the `Init` return retries with the WMA decoder, only under `--voice-decoder`) | `64b3a21`, `d1de1b9`, `b86b8bc` | `docs/reverse-engineering/voice-startup-sequence.md` §11–13, `docs/architecture/voice-decoder-adapter.md` "DMO fallback hook", replica `game-dmo-fallback` |
| Station docking-port source-over draws admitted to the linear fade route (seventh pair, read-only refused-rect diagnostic) | `f56a393` | `docs/architecture/linear-station-source-over.md`; `docs/reverse-engineering/station-material-distance.md` (fixed material property, run-11 census) |
| Shimmer trace diagnostic (`--shimmer-trace`) | `09e62bb` | region note subsection |
| Capture-only draw-state fields on `motion_route` | `f4cd0a0` | `docs/verification/motion-output.md` |
| Host suite repairs (harness drift) | `93f359c`, `188ffb4`, `6ac3dcd` | full discovery 1710 tests OK |
| Screen emission for the nine SM1 bullet pairs: design, step A (packed policy 8 in the bracket), step B (Unlock-time bullet bound), step C (`--screen-emission`) | `d559891`, `2c12f33`, `2c15609`, `76d7750` | `docs/architecture/screen-emission-region.md`; results `screen-emission-*.json` |
| Ambient occlusion: design ratified (v1) + step 1 detached pass | `1b4f755`, `c4f6940`, `4dc9d44` | `docs/architecture/ambient-occlusion.md`, `docs/verification/ambient-occlusion.md`, RE `camera-state-and-frame-routine.md` "Ambient occlusion inputs" |

## Pending at handoff

1. **User runs 13, 14, 15** (`docs/verification/user-runs.md`, all Ready on `76d7750`,
   in that order): 13 speech with the plugin + DMO fallback (short; force-quit and
   report if it hangs); 14 station route + fade witness + shimmer trace; 15 = 14 plus
   `--screen-emission`, fire at a target. Analysis per run: 13 → `voice_dmo_fallback`
   activation lines, `game_phase_audio` counters, selection timing; 14 → `fade_witness`
   zero outside pixels, `fade_refused_rect` vs the HDR captures (docking-port pixel
   proof), shimmer trace around zoom frames, `motion_route` draw-state census of the
   remaining gate-4 refusals; 15 → witness zero outside, bounded rate near 100 %,
   frame-time median vs run 11 (4.097 ms), bracket count per frame (no cap exists;
   30 bullets ≈ 5 ms — decide a cap/refusal policy from the data, never by hiding
   cost).
2. **Ambient occlusion step 1b** (cost reduction: fold linearize into GTAO, one 2D
   blur; target ≤ 0.5 ms, cap 0.8 ms at 1280×768) was in flight in worktree
   `agent-a687429be804c2dd5` at handoff time; if the worktree still exists, read its
   last commit and report, review (Opus), merge or discard. Step 2 (scene-end hook
   before the TAA resolve, launcher option, live fixture) waits for the run-14 capture
   query on Z-test-off draws before the scene end (`ambient-occlusion.md` §3, §8).
3. **Decisions for the user**: fade route (`--linear-distance-fade`) default-on after
   run 14; AO cost acceptance before step 2; screen-emission bracket cap after run 15.
4. **Voice fallback**: if run 13 still fails or speech is wrong, the user's decision
   (memory `voice-audio-decision`) is to fail early on the missing audio to remove the
   selection stutter; design that as a bounded hook only then.

## Standing decisions

- No native-codec or bottle-clone route for audio; plugin path first, fail-early
  fallback second. Never launch the game; bottle X3 untouched; one Wine command at a
  time via `wine_lock.py` with `X3M_FIXTURE_BOTTLE=X3`.
- Fade/composition: never hide cost by capping draws mid-frame or mixing policies;
  unbounded screen-emission draws refuse to native, never full viewport.
- Cutout: the census shows the alpha-tested Argon pairs now routed in gameplay; the
  docking-port darkening is the coexisting-lighting split (station route addresses it).
- Full objective (`docs/user-objective.md`, `docs/goals.md`) still binding and
  incomplete: HDR display, whole-scene linear lighting, AO (in progress), shadows,
  reflections, particles, volumetrics, lens effects, clustered lighting, window/cursor.

## Worktrees, branches, local artefacts

- All agent worktrees merged today are pruned; `/tmp/x3-diag-witness` kept; the
  older `/tmp/x3-*` worktrees predate this session. The AO step-1b worktree may exist.
- Local evidence: `/tmp/x3-bottleX3-run36/` (user run 11), `/tmp/x3-bottleX3-run37/`
  + `/tmp/x3-witness-quartz.log.z` (5 GB, run 12), `/tmp/x3-wine-src/wine`
  (wine-11.15 sparse checkout), `/tmp/x3-wma-plugin-v3` (backup under
  `/Users/asvetl/x3-mod-resume-2026-09-14/artifacts/wma-plugin-v3`), raw RE output
  under `/tmp/x3-voice-dmo`, `/tmp/x3-bullet-vb`, `/tmp/x3-station-blend`, `/tmp/x3-ao-re`.
- Process notes: routing as in `CLAUDE.md` worked; consequential hooks/GPU routes got
  an Opus review plus a Fable second review; every agent report held to 25 lines.
