# Cull census: verification ledger

Feature: `--cull-census` / `X3M_CULL_CENSUS=1`, `src/proxy/cull_census.cpp`,
sites `0x0047d258` (measure) and `0x0047d528` (exit) of the cull/LOD pass
`0x0047cfe0` ([lod-selection.md](../reverse-engineering/lod-selection.md),
"Cull census sites"; [engine-frame-time.md](../architecture/engine-frame-time.md)
2.3). Default off; nothing is written unless the variable is exactly `1`, the
executable hash matches and both window bytes verify. Read-only: the stubs
record and never change a verdict.

| Date | Check | Command | Result |
| --- | --- | --- | --- |
| 2026-09-18 | Site qualification on the installed EXE (`fdbf3418…`): 31-byte and 27-byte windows, one whole 6-byte `mov` at the measure site followed by the flag writer `test eax,eax`, two whole instructions at the exit site followed by the flag consumer `je 0x0047d548`, no direct branch into either displaced span, incoming sources exactly `0x0047d231`/`0x0047d24e` and the eight documented exit sources, `ret 8` at `0x0047d54f`, source constants and windows, stub encoders | `python3 verification/probe/verify_cull_census_sites.py` | PASS, 16/16 checks, 373 instructions decoded in `0x0047cfe0..0x0047d551` |
| 2026-09-18 | Host tests: verifier on a synthetic image (changed window/site/next-instruction bytes, an interior branch, an extra exit source and a changed `ret` refused), constants, encoders, install/frame/row parsers, `classify`/`size_limit` and both encoders compiled with the host compiler, `tools/analysis/cull_census.py` bucket table and draw join on a synthetic log (view selection, pixel buckets at `m00 = 0.8`, savings, JSON), launcher gate | `python3 -m unittest discover -s verification/analysis -p 'test_cull_census.py'` | 12 tests OK |
| 2026-09-18 | X3 CPU fixture: synthetic pass with both windows byte-exact, the production module patching it; native-vs-patched identity of every node (renderable bit, LOD, flags) and of EAX/ECX/EDX/EFLAGS at the return over a 12-node tree in the main view, the env-map view (`< 20` zeroing, +1 LOD) and view-distance 4 (LOD forced to 0); callee-saved registers, ESP and empty x87 preserved; rows in traversal order with the engine's `s`/measure/`D`/radius/thresholds/limit/LOD and verdicts `kept`/`culled_size`/`culled_min`/`culled_other` (parent threshold as the limit, own threshold above the parent's, `D < 640` saturation); two early exits `unmeasured`; disarmed frame records nothing; 8,201 nodes keep 8,192 rows with `overflow=9` and the pass unaffected; LastError preserved; exact rollback and native pass back; option off/`0`/engine sites absent untouched; changed measure/exit window bytes and a null site refused; closed window `late_claim` | `python3 verification/probe/build_cull_census.py` (module audit: 12 functions, no x87/MMX/XMM, no EH symbols, both handlers present) then `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_cull_census.py` | 71 checks, 0 failures; `verification/results/cull-census-cpu.json`; bench per 12-node pass (10 measured): native 0.234 µs, patched disarmed 0.244 µs, patched armed 0.311 µs (Wine/FEX, harness included, not game FPS) |
| 2026-09-18 | Clean DLL build and the no-x87 walk including both handlers | `cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build build -j4` (fresh directory); `python3 verification/probe/check_no_x87.py build/d3d9.dll` | 0 warnings; PASS, 539 reachable functions, `_x3m_cull_census_measure` and `_x3m_cull_census_exit` walked, no violations; `build/d3d9.dll` sha256 `6b86763e15d0e9a62e31c60fcfa7f41fd79f462550e1c46c9c40b7e2cbd2a16b` (worktree build, not a candidate) |
| 2026-09-18 | Launcher dry run | `python3 tools/manage.py launch --cull-census --dry-run` | env carries `X3M_CULL_CENSUS=1`; no launch |
| 2026-09-18 | Unaffected fixtures rerun (engine_patch and capture-path neighbours) | `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_object_lifetime.py`; `… run_ownership.py` | object lifetime 674 checks, 0 failures, 13 cases PASS; ownership baseline and wrapped fixtures exit 0 (`verification/results/bottle-X3/`) |
| 2026-09-18 | Verdict `culled_small` for the small-parts stub (`classify(e, small_threshold)`, named after the engine's own `culled_size`/`culled_min`; `note_small_threshold` stores the frame's threshold, 0 without the option): host tests and the CPU fixture rerun with the changed module | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_cull_census`; `python3 verification/probe/build_cull_census.py` then `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_cull_census.py` | 12 tests OK; 71 checks, 0 failures (13 functions in the module audit, no x87/MMX/XMM); both stubs armed beside the small-parts stub: [cull-small-parts.md](cull-small-parts.md) |
| 2026-09-22 | LOD-ladder fields (`lods=`, `thr=`; [lod-selection.md](../reverse-engineering/lod-selection.md), "LOD ladder fields"): the ladder byte pattern (D in EBX/`[esp+0x10]`, the `0x004863c0` model in EBX/`[esp+0x14]`, count word `+0x10`, records `+0x0c`, threshold `+0x34`) as whole instructions; EBX writers between the sites exactly `0x0047d2f6`/`0x0047d303`/`0x0047d436`/`0x0047d45f`/`0x0047d46e`, ESP writers exactly `0x0047d2fd`/`0x0047d305`/`0x0047d439`, `[esp+0x10]` writers `0x0047d1c8`/`0x0047d1eb`, `[esp+0x14]` writer `0x0047d30a`; the 39-byte exit stub; host tests of the guard, the suffix formatter, the row parser and `draw_accounting.py --ladder`; scratch DLL build and x87 walk | `/usr/bin/python3 verification/probe/verify_cull_census_sites.py`; `PYTHONPATH=verification/probe /usr/bin/python3 -m unittest verification.analysis.test_cull_census verification.analysis.test_draw_accounting`; `cmake -S . -B build-ladder … && cmake --build build-ladder -j6`; `/usr/bin/python3 verification/probe/check_no_x87.py build-ladder/d3d9.dll`; `python3 verification/probe/build_cull_census.py` | PASS 21/21 checks (373 instructions); 23 tests OK; 0 compiler warnings; x87 PASS (638 reachable functions, 0 violations); fixture built (module audit: 13 functions, no x87/MMX/XMM, no EH symbols). Wine CPU fixture run 2026-09-23 on main 7f974d8b: `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_cull_census.py` checks=83 failures=0 (bench native 0.228 / disarmed 0.249 / armed 0.320 us, within jitter of the 71-check row); `run_cull_small_parts.py` (now linking engine_memory.cpp) checks=113 failures=0 with the 2026-09-19 flip/draw counts unchanged (`verification/results/cull-census-cpu.json`, `cull-small-parts-cpu.json`). Review fixes 2026-09-23: captured Present advances the `engine_memory` epoch before the ladder reads (fixture check added); `--ladder` joins draws through the chosen view's census nodes and refuses an absent `--view`. Mutation check (`verification/results/cull-census-ladder/mutate.py`, table in `mutate_out.txt`): six one-byte EXE mutations (model-slot and D-slot writer retargeted, extra EBX writer inside and after the loop, restore from the D slot, extra ESP writer) each fail their named check (`slot_writers`, `ebx_writers_between_sites`, `esp_writers_between_sites` or `ladder_pattern_bytes`); the unmodified EXE passes |
| 2026-09-23 | Body-name field (`body=`; [lod-selection.md](../reverse-engineering/lod-selection.md), "Body name field"): the model id's name from the engine's body table (`*(0x00608518)`, `+0xb4` 11000, `+0xb8`, `+0xbc` slots of `0x1c`, name `char*` at `+0x0c`) read at Present through `engine_memory::read`; EXE pin of 24 byte patterns in `0x0046d910`/`0x0046df60`/`0x0046e400`, including the four signed id-bound branches (one-byte change of each caught on a synthetic image); the global seam `set_body_table_global()` only in the fixture build (`-DX3M_CULL_CENSUS_FIXTURE`), absent from the production DLL's symbol table; host tests of the id→slot map, the census's `v\%05d` label for a null name, the 63-character cap and sanitising, the row parser and `draw_accounting.py --ladder`; CPU fixture extended with a synthetic body table (ids 42, 10500 (9000..19999) and 20001 named, 20005 slot out of range, 20002 name on a no-access page, 43 null name, 5000 invalid range, a page-edge name, a name across two readable pages, 256 bytes without a NUL, a 100-character name, one read set per id, a moved slot array, no manager, wrong fixed count) | `/usr/bin/python3 verification/probe/verify_cull_census_sites.py`; `PYTHONPATH=verification/probe /usr/bin/python3 -m unittest verification.analysis.test_cull_census verification.analysis.test_draw_accounting verification.analysis.test_body_table_exe`; `cmake -S . -B build-bodyname … && cmake --build build-bodyname -j6`; `/usr/bin/python3 verification/probe/check_no_x87.py build-bodyname/d3d9.dll`; `python3 verification/probe/build_cull_census.py` | PASS 21/21 (373 instructions); 26 tests OK; 0 compiler warnings; x87 PASS (638 reachable functions, 0 violations); fixture built (module audit: 14 functions, no x87/MMX/XMM, no EH symbols). Wine CPU fixture not yet run: Wine CPU fixture run 2026-09-23 on main cf492661: `checks=101 failures=0` (83 after the ladder + 18 body-name checks; bench native 0.221 / disarmed 0.243 / armed 0.326 us, `verification/results/cull-census-cpu.json`) |
| 2026-09-24 | `flag31=` field (bit 31 of the root's `+0x12c`, [lod-selection.md](../reverse-engineering/lod-selection.md), "Terran stations and bit 31" §5) from a 16-deep ancestor stack pushed at the exit site, no extra engine read; `Entry` 68 -> 72 bytes; shipped with the Terran-station LOD reader patch (`src/proxy/terran_station_lod.cpp`, `0x0047d01c` `74 05` -> `eb 05`) | `/usr/bin/python3 verification/probe/verify_terran_lod_site.py`; `/usr/bin/python3 verification/results/executable-identity/run_verifiers.py --output …`; `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_terran_lod_site verification.analysis.test_cull_census`; scratch `cmake … && cmake --build`; `check_no_x87.py`; `python3 verification/probe/build_cull_census.py`; `run_host_suite.py` | site PASS 16/16 checks (373 instructions, 0 raw branch encodings and 0 dwords into the window, sources of `0x0047d023` = `0x0047d00b/0x0047d010/0x0047d01c`); identity variants 24/24 PASS x4, different build 24/24 FAIL, site-corrupt `0x0047d01c` FAIL with identity true, anchors 41; host 10 + census tests OK; 0 compiler warnings; x87 PASS (673 reachable functions, 0 violations); census fixture built (14 functions, no x87/MMX/XMM); host suite 250 modules, 2,566 tests, 0 failing. Wine CPU fixture (worktree build, three flag31 checks added: every row `flag31=0` through the grandchildren, root bit 31 set gives all ten rows `flag31=1` with registers/x87 preserved, the bit comes from the root): `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_cull_census.py` checks=104 failures=0 (bench native 0.226 / disarmed 0.243 / armed 0.338 us, `verification/results/cull-census-cpu.json`). The Terran patch's write/read-back/rollback/restore sequence is `install()`/`restore()` in `terran_lod_sites.h`, host-tested against a buffer (mismatched window: no protect, no write; `eb 05` read back; restore gives `74 05`; dropped, stuck and corrupted stores). Review fixes: `rollback_failed` logs `status=patched_unverified`; `restore()` writes `74 05` over whatever a registered site holds and reports `found=`; one `terran_station_lod_restore` row via the log handle; `restore_failed` stays registered. Rerun: host 48 tests OK (`test_terran_lod_site`, `test_cull_census`, `test_exe_identity`, whose `SITE_CHECK` now accepts `sites::install(`); identity verifiers PASS 24/24 x4, different build 24/24 FAIL, three site-corrupt FAIL with identity true; 0 warnings; x87 PASS 673/0; host suite 250 modules, 2,567 tests, 0 failing. Flight not run |
| 2026-09-24 | Terran-station LOD patch, real write path under Wine: `verification/probe/terran_lod_patch_fixture.cpp` links the unchanged `src/proxy/terran_station_lod.cpp` and `engine_patch.cpp` (a force-included pass-through seam, `terran_lod_patch_fixture_shim.h`, only counts and injects faults on `VirtualProtect`/`FlushInstructionCache`/`read_code`/`write_code`); the stub carries the header's 17-byte window in its own image section linked at `0x0047d000`, so `initialize()` patches the production constant `0x0047d012` on a MEM_IMAGE `PAGE_EXECUTE_READ` page, plus a VirtualAlloc page via `install_at()` and a read-only executable view; the patched branch is executed (bit-31 node returns 1 on `je`, 0 on `jmp`) | `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_terran_lod_patch.py` (builds with `build_terran_lod_patch.py`); `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_terran_lod_patch_result verification.analysis.test_terran_lod_site` | 92/92 checks PASS, 5.4 s, bottle X3 arm64 (`verification/results/bottle-X3/terran-lod-patch.json`). `write=atomic` (one `lock cmpxchg8b` in `engine_patch.o`), `eb 05` read back, the other 4,094 bytes of the page unchanged, protection back to `PAGE_EXECUTE_READ`; executed branch before 1, patched 0, restored 1, after every rollback 1. Refusals write nothing: `distance`, `invalid_setting`, `too_long`, `executable_mismatch`, changed window byte and foreign `eb 05` (`bytes_mismatch`, 0 protect calls), injected `protect_failed`, `late_claim` from `initialize()` and `install_at()`. Rollbacks on the real page: failed read-back and dropped store give `patch_rolled_back`, failed re-protects give `rollback_unprotected` (page left writable), a corrupted store (`eb 06`) that cannot be undone gives `patched_unverified`/`rollback_failed`, then `restore_failed found=eb06 registered=1` twice (dropped store, failed protect) and `restore_not_owned found=eb06 registered=0`; `restored found=7405` without a write, `restore_not_owned found=--` on an unreadable first read. Real protect failure: `VirtualProtect` on the read-only view refused with error 87, `install_at()` gives `protect_failed`, nothing written. LastError preserved across all 26 checked `initialize()`/`shutdown()` calls. Image page: `VirtualQuery` after the `PAGE_EXECUTE_READWRITE` request shows `PAGE_EXECUTE_WRITECOPY` (0x80), still 0x80 after the store (Windows expected to show `PAGE_EXECUTE_READWRITE` after the copy-on-write: inferred, not verified); private page 0x40. `FlushInstructionCache` not needed here: without it (and on a hot RWX page even without `VirtualProtect`) the next call ran the new branch in both directions. Install 212 us, restore 216 us, fixture-inclusive, one-off. Native Windows not run |
| 2026-09-24 | LOD-switch rows (`--lod-switch-log [N]`, `X3M_LOD_SWITCH_LOG=N`, 1..4096, default 16; implies `--cull-census`; for the Run 79 A ODS flicker question): while on, the stubs are armed every frame and Present looks up each kept, exited entry's (node, view) in a 16,384-slot open-addressed table (`core::track_observe`, probe 16, committed once, never freed; a key not seen on the previous frame, a changed model id or a Reset (`cull_census::reset()`, now the capture.cpp Reset call) re-seeds without a row; shutdown clears it). Rows: `lod_switch frame= node= body= from= to= s= D= T_pad= flag31= view=` (T_pad = last ladder threshold or `-`) up to N per frame, `lod_switch_overflow frame= dropped= cap=`, `lod_switch_frame frame= switches= nodes=` only on frames with a switch; one `cull_census_lod_switch state= cap= table=` row at install. Handlers unchanged | `python3 verification/probe/build_cull_census.py`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_cull_census.py`; `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_cull_census` plus the 45 other modules that load `tools/manage.py`; worktree `cmake … && cmake --build build -j8`; `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `python3 verification/results/run299-303-run79a/ods-flicker/lod_switch_rows.py --self-test` | Wine CPU fixture checks=119 failures=0 (104 + 15: seed/unchanged frames silent, one D change gives exactly one row `from=3 to=2 s=38 D=50000 T_pad=25` plus `switches=1 nodes=6`, cap 1 gives one row + `dropped=1` + `switches=2`, Reset re-seeds, captured frame keeps its 10 census rows, static 4,097-node scene silent); Present-time table scan 4.02 ns per tracked node (measured: 4,097 nodes, pass+present 96.3 us off vs 112.8 us on, fixture-inclusive, not game FPS); with the option on both stubs are also armed every frame, about 8 ns per node more (inferred from the armed-minus-disarmed bench), so ~12 ns per node in total and ~8 us per frame at the run299 figure of 620 rows (inferred); existing bench unchanged (native 0.225 / disarmed 0.245 / armed 0.341 us). Host: 14 census tests OK (core table: seed/same/switch, per-view keys, gap and model re-seed, 16 colliding keys fill the probe window, stale-slot reuse; cap parser; launcher env and range refusal), 651 tests in the manage.py-loading modules OK; 0 compiler warnings; x87 PASS (683 reachable, 0 violations); parser self-test PASS. Flight not run; native Windows not run |

## Open

- Not yet run in the game: the first capture with `--cull-census` at the run124
  station view gives the bucket table (`tools/analysis/cull_census.py
  <session.log> --frames <F8 window>`); expect one `cull_census_frame` row per
  captured frame per Present and up to 8,192 rows each, so keep the F8 window
  to a few frames.
- The rows are emitted with one `log()` call each at Present: a full ring is
  8,192 lines (~1.5 MB) per captured frame, which lengthens that frame only.
- The ring is read at Present on the assumption that the cull pass and Present
  run on the same thread (the frame routine `0x00471f50` issues both); a pass
  on another thread would at worst tear one frame's rows, never the engine's
  verdicts.

**Run 42 C (run131, 2026-09-18).** First in-game census on the run42 candidate, fighter save at the run117
station, F8 on the ≈ 900-draw view (frames 4991/4992, dt 31–32 ms before the capture stall): 1,296 / 1,324
census entries per frame, overflow 0, unmeasured 3,881 / 3,853 (nodes the pass rejects before the measure).
`tools/analysis/cull_census.py --frames 4991`: kept nodes under 2 px = 403 draws of 901 (44.7 %, 9.55 ms at
23.7 µs/draw), under 4 px 458 (50.8 %, 10.85 ms), under 8 px 479 (53.2 %, 11.35 ms). The engine's own cull
already removes 1,050 of 1,147 sub-2 px nodes (91.5 %); every surviving tiny node has `+0x1d8` = `+0x1dc` =
limit = 0 (no per-node threshold), top model `35ba45c3` 150 draws at LOD 0. The run124 offline world-scale
proxy (50–65 % under 2 px) overstated the under-2 px share by 6–20 points and matched the under-4 px share.
Only one vantage was captured (both frames the same static view). Lever ratified: `--cull-small-parts <px>`
(engine patch) for run 43 at 2 and 4 px.

**Scope and the model join (2026-09-19).** `Entry` records the node's parent link (`+0x18`, 60 bytes per entry) and
`classify` takes cull_small_parts' scope: under `bodies` a parented node below the threshold is never named
`culled_small`; `culled_small` rows end with ` scope=all|bodies` (appended after `verdict=`, the existing row
parsers are unaffected). `tools/analysis/cull_census.py` lists the models of kept / `culled_small` nodes under
`--bodies-px` (default 4) with body flags (`flags_in & 0x09000000`), radius class, D range, nodes and draws.
Gap: the repository has no model-id -> object-type table (the game's type files are not extracted), so bodies are
named by model id and flag class only, and the rows print no parent link, so the flag class stands in for the
stub's `[node+0x18] == 0` test. Notes for reading a census beside `--cull-small-parts`: a static caster culled
by the stub keeps casting under `--shadow-caster-retention`; the script occluder list `0x00488aef`/`0x004886a0`
loses culled nodes; the `cull_small_parts_value` line is capped at 16 and `m00` lags a zoom by one frame;
`camera_state::reset()` is unreachable with only that option on. The small-parts site verifier is 19/19
(18/18 before the scope check; "16/16" in earlier small-parts records was stale). Host:
`verification.analysis.test_cull_census` + `test_cull_small_parts` 26 tests OK.

**2026-09-24 Run 79 A (run299/300/302/303): Terran station LOD patch flown.** Every session logs
`terran_station_lod site=0047d01c status=patched reason=ok mode=size setting=size write=atomic` (measured). In run299's
three F8 bursts (frames 5885, 7308, 8417) all 16 slot-06 bodies carry `flag31=1`; every census row with s/T_pad in
0.1..0.99 is lod 1 and every row at >= 1.0 is lod 0 (`usc_dock_e_tower` lod 1 at 0.99, lod 0 at 1.05/1.12; the SPP
panel/center/bottom at 0.15..0.34 draw lod 1). The user confirms the coarse record on the Orbital Defence Station
(screenshots/terran-lod1.png vs -lod2.png) but the coarse record loses the station's red colouring and one part
flickers under motion (triage under `verification/results/run299-303-run79a/terran-colour/` and `ods-flicker/`).
`--terran-station-lod distance` was not flown. Script: `run299-303-run79a/overlay_bursts.py` (`_run299_out.txt`).

**2026-09-24 Run 80 A (run305, `--lod-switch-log`): the ODS flicker is a LOD pop without hysteresis.** 48 `lod_switch`
rows over 11 bodies, every `lod_switch_frame` row `switches=1`, no `lod_switch_overflow` (measured;
`run304-305-run80a/lod_switch_counts_out.txt`, `terran-colour/switch_intervals_run305.txt`). `usc_dock_e_tower` switched
25 times between frames 5666 and 14229: 1 -> 0 at s = 197 and 0 -> 1 at s = 196 with T_pad = 197, gaps 27 / 187 / 1767
frames (min / median / max), D between 15.42 M and 15.53 M; `usc_dock_e_upper_core` 12 times (s 212 <-> 213/214, T_pad
213, gaps down to 3 frames). One pop per crossing, made visible by the record swap changing the material constants and
the sun-shadow casters together (lod-overlay.md, sun-shadow ledger). Fix direction: make the records match visually
(baker shading classes, alpha-tested casters) before any hysteresis patch of the size loop.
