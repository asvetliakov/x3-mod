# Effects stage ledger

Feature: `docs/architecture/effects-modernisation-opus.md` (phase 1: the stage, projectiles, shield hits; section 9 is the brief, "Implementation (phase 1)" the record). One row per verification outcome; append. Marks: measured unless the row says inferred.

| Date | Change | Commands | Result |
| --- | --- | --- | --- |
| 2026-09-25 | Phase 1 implemented (`--effects-stage`, default off): `effects_stage_core.h` (records, sparse texture key, key table parser, arming, bolt instances + nearest-centroid velocity match, shell association + four slots, node-age map, object→world from clip rows), `renderer/effects_stage_pass.{h,cpp}` (three vs/ps pairs, dynamic VB, icosphere, quad IB, caps, Reset), `TemporalPass::FrameInputs::stage_callback` (the stage as the first act of the resolve's bracket, normalize re-run after it), ownership upload-time texture keys (`Texture::LockRect/UnlockRect`, level-0 `Surface` links from `GetSurfaceLevel`, the read-only fallback, `get_texture_key_view`, `borrowed_native_texture`), `motion_output_effects_inc.h` (recogniser on the DEFAULT/INSTANCE effect pair, bolt records from the additive bullet draws (suppression with `S_OK` in this row's build; removed by review B1 in the review-fix row: the native draw always goes through), owner boxes from the candidate route, the stage run, census and frame rows), `capture.cpp` env gates and the key-table read, loader scan/keys arming, Ctrl+Shift+F3 (moved to Ctrl+Alt+F5 by review fix 10), `tools/effects/effect_keys.py` + `effect_keys.json` (3 keyed textures), launcher options. Work order 1 (pass-opening price) measured first; verdict: **own-pass cost 0.008 ms ≤ 0.1 ms, the stage is built as designed (no in-place bolt fallback)**. | `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_effects_stage.py` (build + two Wine runs; record `verification/results/bottle-X3/effects-stage/summary.json`, logs `gpu.log`, `keys.log` beside it); host `PYTHONPATH=verification/probe /usr/bin/python3 -m unittest verification.analysis.test_effects_stage_core verification.analysis.test_effect_keys verification.analysis.test_bolt_footprint verification.analysis.test_screen_emission_live`; fog fixture reuse `python3 verification/results/effects-stage/reuse_deps.py main` (`reuse_deps_out.txt`) | GPU fixture PASS 121/121 checks at 1920x1080 and 5120x1440 (bottle X3, builtin d3d9, `effects_stage_fixture.exe`): `pass_open` at 5120x1440 (fenced frame tail, no TIMESTAMP answer on this backend, the fog fixture's law; 15 pairs): stage riding an RT0-only pass 0.012 ms, opening its own after the RT1/RT2 unbind 0.021 ms, own-pass price 0.008 ms (runs 2–4: 0.014 / 0.027 / 0.021 own, −0.002 / 0.017 / 0.008 price; noise ±0.015 ms), depth surface attached before the unbind (`depth_bound_before_unbind=1`; inside the bracket it is unbound by `normalize`, so the stage occludes by the lane alone, unknown 11 settled). Bolt capsule: core 6.0 = I_core, width 3 px at both sizes from a 0.8 / 2.2 px native width, length 133 / 351 px (native 130.6 / 348.2), a 0.5-unit bolt lengthened to 15 / 19 px (L_min 12 / 16), occluded half 0.000 with the open half 6.000 (4-channel and R32F lanes), soft end 3.00 / 2.99 of 6 with the hull 2.5 units behind the bolt. Shell: rim (70° on eight meridians) 0.50 / 0.42 against 0.24 / 0.20 at 45°, four rings 8.0/1.5, 7.8/2.1, 4.6/0.02, 5.9/0.04 on/off (1080p), the hull cut 0.000 left / 8.23 right, the soft cut 2.80 of 8.60 open, the decal 3.33 on the hull and 0.000 over sky. Resolve (the production TemporalPass, 8 frames, Halton jitter, weight 0.9): bolt core ratio 1.000 at 0, 4 and 8 px/frame with trail 0 px; shell ratio 1.000; a callback that draws nothing gives the run without the field byte for byte; no record: `S_FALSE`, 0 calls, the frame byte-identical. Arming rule (core `Arming` driven by the fixture, not the proxy draw path; the end-to-end path is the motion_output case of the review-fix row): 10 armed frames admit, a missing resource refuses, a failed run (`E_FAIL` injected on the bolt draw, step 0 reported) refuses the next 64 frames and none is both, the 65th frame runs again `S_OK`. Reset: 13 references → 9 before Reset → 13 after (`ensure_resources`), the next run draws. FP16 blending refused by fault: `attach` `D3DERR_NOTAVAILABLE`, reason `fp16_blending`, 0 references. Timing (fenced frame tail, marginal, submit subtracted): 60 bolts + 2 shells 0.124 ms at 1080p / 0.162 ms at 5120x1440 (**above the 0.05 ms target of section 9**), a shell projecting past every screen edge 0.54 / 0.77 ms (**above the 0.1 ms target**; 0.79 / 0.78 before the shell program's back-face early-out); slots bolt 95/62, shell 44/216, decal 27/84 (vs/ps). Keys fixture PASS 8/8: the three D3DX-loaded catalogue textures (`exp_PL_imp_diff` 128² DXT1, `fx_bullets2_diff` 1024² DXT5, `fx_sphereshockwave_diff` 512² DXT5) keyed at upload through the level-0 surface path (`source=2`) equal to the table's keys; the texture-lock path, the surface path and the read-only fallback each match the in-process key of known bytes; a DEFAULT-pool texture stays unknown (fail closed). Host: `test_effects_stage_core` 8 tests OK (key mirror, table, matching within 1 %, ambiguity refusal, cut reset, association among 3 boxes, sphere / decal beyond 1.5 radii, four slots, arming, ages, clip rows, derivation, launcher gates), `test_effect_keys` 3 OK (determinism, `.pck` over `.dds`, loose over catalogue, mod over catalogue with the last mod winning, a symlinked package refused), `test_bolt_footprint` 22 OK (two source asserts updated to the shared gate lines), `test_screen_emission_live` 19 OK. Fog fixtures: 0 of the fog pass fixture's 73 and the route bridge's 58 dependency paths changed against main (reused from run85). Dry run: `python3 tools/manage.py launch --dry-run --motion-output --hdr --taa --object-trace --object-lifetime --ownership --effects-stage --effects-shields` forwards `X3M_EFFECTS_STAGE=1`, `X3M_EFFECTS_SHIELDS=1`, `X3M_EFFECTS_CENSUS=0`, `X3M_EFFECTS_BOLT_VIEWS=chase` (`_DEFAULT=1`), `X3M_SCREEN_EMISSION_ADDITIVE=1.0`, `X3M_EFFECTS_KEYS=Z:\…\tools\effects\effect_keys.json`; `--vanilla` forwards none. No launch; the combat flight (Run 85 A launch 2) is open. |
| 2026-09-25 | Regression of the resolve seam (`TemporalPass::FrameInputs::stage_callback`, `stage_context`; `Diagnostics::stage_ran / stage_result / stage_depth_bound / ticks_stage`; the callback path re-runs `normalize`): the temporal pass fixture with the field null. | `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_temporal_pass.py` | `RESULT PASS numerical=744 state_restorations=278 generations=2`, the same line as run85's; `verification/results/bottle-X3/temporal-pass.txt` byte-identical to the committed report (`git diff --stat main -- verification/results/bottle-X3/temporal-pass.txt` empty). Fog pass fixture and fog route bridge: reused from run85 (0 of 73 / 58 dependency paths changed, `verification/results/effects-stage/reuse_deps_out.txt`). |
| 2026-09-25 | Acceptance script (host modules, key table `--check`, program slots, scratch mingw build of `d3d9`, x87 audit). | `sh verification/results/effects-stage/acceptance.sh` (tee'd to `acceptance_out.txt`) | Acceptance modules 52 tests OK (`test_effects_stage_core` 8, `test_effect_keys` 3, `test_bolt_footprint` 22, `test_screen_emission_live` 19); `test_comparison_hotkeys` + `test_taa_image_defaults` 31 tests with 6 errors, all `test_launcher_auto_default_fixed_and_manual_override` `NameError: sys` at `manage_under_test.py:554` (the `fog-families` wrapper line of commit 89f4b014, untouched here: pre-existing). Key table `SAME` (3 entries). Slots vs/ps: bolt 95/62, shell 44/216, decal 27/84. Scratch build `build_exit=0 warnings=0`; x87 audit PASS, 119 roots, 705 reachable (run85: 690), 0 violations; DLL sha256 f438a27e… (provenance only); scratch removed. |
| 2026-09-25 | Review fixes (Opus + Fable). **B1 (option b):** no draw is suppressed in phase 1: `record_effect_draw` / `record_bolt_draw` record and leave `route.submit`; the bullet draw keeps the additive route (only the footprint rewrite is skipped for a recorded draw, `screen_emission_additive_refused_window effects_recorded=`), the sprites draw natively, the stage adds on top; option a refused (a suppressed sprite has no native forward path without retained game resources). Help text corrected (unflown, missed timing targets, no suppression). Arming moved from Present to the first draw after the latching clear (`effects_armed_now`: the HDR redirect, depth surface and jitter are latched inside the frame), post-Reset buffer recreation under the reference accounting. **B2:** `run_motion_output.py` case `seam-ownership-effects-stage` (mode `effectsstage`, `motion_output_screen_emission_inc.h`) drives the production path: 13 bullet draws (24 vertices each) through the proxy's draw path with the seam DLL, `X3M_EFFECTS_STAGE=1`, telemetry rows per frame, the HDR Resolve fault at frame 6, a Reset after frame 9. Low: (1) a malformed / truncated / trailing-bytes table keeps nothing (`count=0`, pinned); (2) the pass skips its second `normalize` on `S_FALSE`; (3) DEFAULT-pool textures are never keyed at upload; (4) the additive window counts recorded draws; (5) no box work when nothing draws; (6) the callback only with the FP16 scene, comment fixed; (7) the atlas is the wrapper pointer (`borrowed_native_texture` removed); (8) decals draw with `BaseVertexIndex` (no stream offset cap); (9) the shell keeps its last hit's tint (`ShipHits::tint`); (10) hotkey Ctrl+Alt+F5 (Shift up), one F5 read; (11) `summary.json` reports the timing targets as `advisory`; (12) `install` copies `tools/effects/effect_keys.json` to `<game>/x3m/effect_keys.json` (the DLL default), `uninstall` removes it, the launcher forwards `X3M_EFFECTS_KEYS` to the repository copy only when that file is absent. | `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_motion_output.py seam-ownership-effects-stage` (record `verification/results/effects-stage/motion-output-effects-stage-case.json`); `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_effects_stage.py`; `sh verification/results/effects-stage/acceptance.sh`; host `PYTHONPATH=verification/probe /usr/bin/python3 -m unittest verification.analysis.test_effects_stage_core verification.analysis.test_effect_keys verification.analysis.test_bolt_footprint verification.analysis.test_motion_output_runner verification.analysis.test_comparison_hotkeys verification.analysis.test_screen_emission_live`; dry run `python3 tools/manage.py launch --dry-run --motion-output --hdr --taa --object-trace --object-lifetime --ownership --effects-stage --effects-shields` | End to end (bottle X3, seam DLL from the worktree's `build/`): `EFFECTS_STAGE frames=14 submissions=13 native_draws=13 admitted=13`: every bullet draw admitted by the additive route and forwarded natively once; `effects_stage_frame` rows: frames 2–5, 7–9 and 11–13 `recorded=1 bolt_draws=1 bolts=1 ran=1 result=00000000 calls=43` (the stage drew inside the resolve's bracket; 10 drawn frames), frame 1 and frame 10 (the first draw of the writer's buffer, new again after the Reset) `recorded=0` by the Unlock scan's mark rule, frame 6 (Resolve fault) `recorded=1 ran=0 bolts=0` with the native draw standing; one `effects_stage_device attached=1` (the pass survives the Reset, its buffers come back); `stage_us` 116–173 per drawn frame (CPU, the resolve's callback bracket at 1280x768; measured); `depth_bound=0` in every row; device final Release reaches zero; `RESULT PASS`. GPU fixture PASS 121/121 + keys 8/8 (129 checks): `pass_open` own-stage 0.013 ms / riding 0.016 ms / price −0.003 ms (noise; runs 2–5: −0.002, 0.017, 0.008, −0.003), 60 bolts + 2 shells 0.030 / 0.158 ms, full-screen shell 0.53 / 0.78 ms at 1080p / 5120x1440 (advisory targets: 1 of 4 met), resolve core 1.000 / trail 0 px, decals with `BaseVertexIndex` 3.33 on hull / 0.000 on sky, the dynamic DEFAULT-pool upload not keyed. Acceptance: 52 + 31 host tests OK (the merged main fixes the `sys` NameError), key table `SAME`, slots unchanged, build 0 warnings, x87 PASS 119 roots / 713 reachable / 0 violations. Host: 80 tests OK across the six modules. Dry run forwards `X3M_EFFECTS_STAGE=1 _SHIELDS=1 _CENSUS=0 _BOLT_VIEWS=chase (_DEFAULT=1) X3M_SCREEN_EMISSION_ADDITIVE=1.0 X3M_EFFECTS_KEYS=Z:\…\tools\effects\effect_keys.json` (no installed table on the bottle). Not exercised: the `install` copy on the bottle (no install this checkpoint; the launcher's installed-table branch is host-tested), the shield recogniser end to end (no DEFAULT-pair draw in the fixture; covered by the core and keys fixtures), Windows. |
