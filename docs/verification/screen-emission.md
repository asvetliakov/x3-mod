# Packed screen emission (bullets) — verification ledger

Owning design note: [screen-emission-region.md](../architecture/screen-emission-region.md)
(steps A–C, the step-B bound and its near-plane clipping). Compact records under
`verification/results/bottle-X3/`: `screen-emission-gpu-step-a.json`,
`screen-emission-bound-gpu1.json`, `screen-emission-bound-live1.json`,
`screen-emission-live1.json`. Earlier step A/B/C evidence is summarised in the note's
implementation sections; this ledger starts with the run-15 diagnosis.

| Date | Change / evidence | Commands | Result |
| --- | --- | --- | --- |
| 2026-09-14 | Step B near-plane clipping (run 15: 400/800 bullet draws refused `reason_w`, admitted rectangles 58–90 %; boxes straddle or lie behind the camera plane). `project_box` cuts the locked-prefix box at clip z = 0, `BehindNear` (7) reason, `clipped` counters, capture-only `packed_sample` line. | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_fade_region verification.analysis.test_screen_emission_live`; `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_locked_prefix_live.py --fixture verification/probe/build/locked_prefix_live_fixture.exe --dll build/d3d9.dll`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_distance_fade_live.py --screen-emission --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll` | Host: 8 + 7 tests OK (`--near` 600 random cases, 0 failures, 0 outside points; hand cases straddle/exact/behind/beam equal to the Python restatement). x87: PASS, 224 reachable functions. Step B live: PASS, 15 frames (9 bound, 6 refused), near_straddle clipped=4 rect (46,10,96,51) 222 ‰, near_exact clipped=4 (46,10,96,63) 287 ‰, near_behind reason 7 refused, near_beam (46,0,96,63) 341 ‰, 11 scans at 24.0 µs. Step C live: PASS, 12 processes; functional 21 frames / 26 sources, 19 eligible / 13 admitted / 12 linear / 1 incomplete / 3 unbounded; brackets s 441, n 986, x 1258, b 1462 px (all < 50 % of 64×64, covering their footprints); witness outside=0 on frames 2,4,5,7,8,10,12,17,18,20; straddle violations {2,7,10,12,18,20: 128, 17: 84}; caps 16 refused; 7 `packed_sample` lines (6 changed). |
| 2026-09-14 | Review fixes: `packed_sample` capped at 4 admitted draws per capture frame (`packed_sample_skipped=` on the frame line), one retained readback surface (counted, released at Reset/teardown), pending pre cleared in `begin_frame`, unclipped path keeps NonPositiveW-before-NonFinite precedence. | same host tests and x87 audit; step-C live rerun (same command) | Host: 21 tests OK. x87: PASS, 224 reachable functions. Step C live: PASS, 12 processes; functional totals unchanged (19/13/12/1/3, brackets s 441, n 986, x 1258, b 1462), witness outside=0 on the 10 sampled frames, 7 `packed_sample` lines (6 changed), `packed_sample_skipped=0` on all 21 frames, held references 59 (unchanged), released=1. |
| 2026-09-14 | Run 17 defect diagnosis (bolts nearly invisible under `--screen-emission`, fixtures green): the packed law decodes each sprite fragment before the ONE/INVSRCALPHA accumulation, so a bolt's chain of overlapping dim sprites (99 quads per run-17 draw) loses its soft tail while the core survives; not a constant, bound, ordering or publication defect (witness 5,000–9,400 covered px / 0 outside per firing frame; a live frame with the run-17 c0–c35 block set after the PS bind composed the law and restored the block). Measurement script + host test added; no production change (design note, step C, "Run 17 defect"). | `PYTHONPATH=verification/probe:verification/analysis python3 -m unittest verification.analysis.test_screen_emission_display_ratio verification.analysis.test_screen_emission_live`; `python3 verification/analysis/screen_emission_display_ratio.py [--overlap 1|8|16] [--background 0.2 0.22 0.16]`; `python3 verification/probe/check_no_x87.py build/d3d9.dll`; one hand-run `screenemission` functional process under `wine_lock.py` with the constant-block frame (kind c, reverted) | Host: 10 tests OK. Display luminance native/packed (overlap 8, σ 3, spacing 2, black) at 0/3/5/7 px: 0.779/0.662, 0.749/0.567, 0.586/0.317, 0.202/0.051; tail-to-core 0.26 vs 0.08 (gain 4: 0.17); overlap 1 identical; option (a) = native exactly. x87: PASS, 224 reachable. Constant-block frame: after = (0.859, 0.902, 0.946, 0.695) from white, law within tolerance, block restored. |
| 2026-09-14 | `packed_sample` extended with the whole-rectangle scan (`scan_result scan_px changed_px max_pre_y max_post_y sum_pre_y sum_post_y argmax argmax_pre argmax_post`; second retained pre readback surface, counted and released with the post one) so the dimmer verdict rests on bullet pixels rather than the bracket centre, plus the opt-in per-Present `screen_emission_frame frame= packed_admitted= brackets_px= cpu_us=` line behind `--screen-emission-timing` (`X3M_SCREEN_EMISSION_TIMING=1`, requires the enabled option; one QPC and one log call per Present). | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_screen_emission_live verification.analysis.test_linear_material_live verification.analysis.test_fade_region`; `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_distance_fade_live.py --screen-emission --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll` | Host: 32 tests OK (9 + 15 + 8); review fixes: one FP16 decoder for the centre and the scan, `memcmp` on the A8R8G8B8 compare, and the pending pre armed after the readback so a mid-sample size/format change cannot drop an admitted draw's line. x87: PASS, 224 reachable functions. Step C live: PASS, 12 processes; functional totals unchanged (21 frames / 26 sources, 19/13/12/1/3, brackets s 441, n 986, x 1258, b 1462, held references 59, released=1); 7 `packed_sample` lines, 6 with a changed centre and all 6 with `changed_px>0`: 1536 changed of 3087 scanned pixels, every `argmax` inside its rectangle (functional frame 2: rect 30,22,51,43, scan_px=441, changed_px=256, max_pre_y=1, max_post_y=1, sum 441 → 414.375, argmax=30,22, pre 1,1,1 post 1,1,1 — the beam's own pixel, where the centre sample reads post 0.859375,0.901855,0.945801). The straddle twin scans 896 px with 768 changed. New `screen-timing-line` case (13 processes): 21 `screen_emission_frame` lines on one device, 20 measured deltas (the first Present has none), mean 28090 µs, packed_admitted 13 / brackets_px 8116 equal to the frame lines, and its functional totals and packed samples identical to `screen-functional`; no other case logs the line. |
| 2026-09-14 | w-scaled conservative pad in the shared bound derivation (`fade_region_math.h`): `pad_px = max(1, ceil(k / w_min))`, `k = 2 * half * 2^-22 * S` (S = largest Σ|terms| over the x, y, w rows and the eight expanded corners; `w_min` = smallest clip w of the projected polytope vertices), capped at 8 px, never below the previous 1 px. Both the locked-prefix near-cut route and the part-bound fade route use it; `Region::pad` added to the `locked_prefix` capture line. Host `--near` oracle extended: every third case at world coordinates 1e3–1e5 with the cancelling row translation, and every interior point perturbed by the whole fp32 `dp4` error bound in all 8 sign directions (screen-emission-bullet-bound.md section 4 pad item: done). | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_fade_region`; `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_locked_prefix_live.py --fixture verification/probe/build/locked_prefix_live_fixture.exe --dll build/d3d9.dll`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_distance_fade_live.py --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll` | Host: 9 tests OK; `--near` 600 cases, 0 failures, 0 outside, 0 fp32-perturbed outside where the pad is not capped, pad histogram 1–8 px (218 at 1, 33 between 2 and 7, 58 capped at 8); forcing the cap back to 1 px puts 4 perturbed points outside — the oracle bites. New pad cases at w = 6 / 30 / 100 / 1000 with S = 1.2e5 and half = 640 px: pad 7 / 2 / 1 / 1 px, padded rect ⊇ the old 1-px rect in each. x87: PASS, 224 reachable functions. Locked-prefix live: PASS, 15 frames (9 bound, 6 refused), 11 scans at 26.8 µs; near_* rects (46,10,96,51) 222 ‰, (46,10,96,63) 287 ‰, reason 7, (46,0,96,63) 341 ‰ — **byte-identical to the previous run, rect delta 0 px** (fixture coordinates are small, so pad = 1; the log shows pad=1 on all bound frames, pad=0 on the refused ones). Fade live (no `--screen-emission`): PASS, 22 cases; witness-full and witness-rect 24,064 covered px, union 90,112 / 33,792, **outside=0**, 30 region lines, and a baseline run of the same fixture with the pre-change sources gives the identical covered/union/histogram/per-frame numbers — **rect delta 0 px**; witness-control still fails on frames 16, 19, 21, 23, 27, 31 with 512 px each. |
| 2026-09-14 | Step E implemented: the packed producer writes `P_c = (q_c, q_c, q_c, q_c)` without a per-fragment decode, the bracket masks the planes to red|blue (green keeps `decode(A)`), the composite decodes the accumulated native lane once with the gain as a patched `def c1` literal (`C = encode(g decode(B) + (1 - g) decode(A))`), `--screen-emission-gain` / `X3M_SCREEN_EMISSION_GAIN` (0.5..8, default 1). Packed corpus gains an 8-layer one-DIP chain schedule; the live fixture an 8-quad soft-sprite chain (kind `c`, frame 21) compared with its native twin and a gain-2 run (design note, "Step E — implemented"). | `PYTHONPATH=verification/probe python3 -m unittest discover -s verification/analysis -p 'test_*.py'`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_emission_sm1_packed.py --fixture verification/probe/build/linear_emission_sm1_packed_fixture.exe`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_distance_fade_live.py --screen-emission --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll` (once before the change for the cost baseline, once after); `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `./x3run <run-17 command> --screen-emission --screen-emission-gain 2 --dry-run` | Packed: 864 rows, 720 in-domain 0 failures, 54 boundary (q-lane overflow/signed on the chain), gain-1 C = native within 1 FP16 code (15,900 of 2,603,097 channels one code low, the rest exact), chain `max_layers` 8, assemble_c 44 ALU. Live: PASS, 13 processes; chain rect (8,24,38,40) 480 px, 456 changed, 8 layers, max 1 code vs the native twin, alpha exact, gain 2 within fraction 0.15 and brighter on 1,300/1,300 bolt channels, withdrawn law off by 0.38; 53 samples within 0.124 (gain 2: 0.243); witness 0 outside. Bracket cost at 16 DIPs, paired source-window median delta per bracket: retained step C record (`883797d`) 0.165/0.160 ms (1280×768), 0.164/0.172 ms (1920×1080); after 0.171/0.164 and 0.167/0.164 ms. `packed_samples.luminance_changed` 6 → 0 (native pixels stay native; alpha still moves). Display ratio: step E tail/core 0.259 = native (gain 2: 0.343, gain 4: 0.441). x87 PASS, 224 reachable. Dry run: `X3M_SCREEN_EMISSION_GAIN=2.0`. |
| 2026-09-14 | Step D implemented (`screen-emission-bullet-bound.md`, last section): Lock-time sentinel, exact Unlock scan with copied positions (`locked_prefix_core.h`), per-triangle near-cut vertex hull at the draw (`project_prefix`/`derive_prefix`, cut at z = −ε for the GPU's fp32 z row), revision recheck after the projection; checkpoints removed. Log grammar: per-draw `scanned= hull_px= aabb_px= ticks=`, frame `rechecks= hull_px= aabb_px= vertices= derive_us= sentinel_bytes= window_end_scans=`; `X3M_LOCKED_PREFIX_LOG=1`. Fixture gains the section-1 geometries (fan 176, straddling 72, 605 with the origin tail, 27) and a GPU-readback footprint per frame. | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_fade_region verification.analysis.test_linear_distance_fade_report`; `sh verification/probe/build_locked_prefix_live.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_locked_prefix_live.py --fixture verification/probe/build/locked_prefix_live_fixture.exe --dll build/d3d9.dll`; `sh verification/probe/build_motion_output.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_distance_fade_live.py --screen-emission --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll`; `python3 verification/probe/check_no_x87.py build/d3d9.dll` | Host: 11 + 8 tests OK (`--hull` 600 lists: 345 bound / 255 BehindNear, 1,853,907 covered px, 0 outside, hull ⊆ AABB rect, 191/345 strictly tighter; footprints over 40,000 px are sampled — 4,000 random pixel centres plus the 5×5 neighbourhood of each polygon vertex — not rasterised exhaustively; `--prefix` 43 scenarios, 300 random: 150 sentinel-exact + 150 garbage-window, 0 failures, host scan 391 ns @1056 vs 2414 ns window). Live prefix: PASS 20 frames (14 bound / 6 refused), hull vs AABB of the viewport: fan_176 1.2 % vs 89.6 %, straddle_72 17.5 % vs 100 % (clipped 18, pad 3), big_605_origin_tail 10.6 % vs 96.5 % (scanned 6144, drawn 3630), small_27 0.1 % vs 70 %; GPU footprint inside the rect on all 14 bound frames; derive 4.6 / 8.4 / 18.6 / 56.3 µs per draw at 27 / 72 / 176 / 605 bolts; 16 scans 12.4 µs mean (13 window-end, 3 exact), 18 locks, sentinel 2,247,552 B, 0 rechecks. Fade live `--screen-emission`: PASS 14 cases, witness outside 0 (functional 4,948 covered px, union 14,678), chain max 1 code. Detached fade `run_linear_distance_fade.py` (prefix group rewritten: tails never enter the bound, `seventeen_quads_prefix_nan` refused): PASS 78 cases, prefix 14 (13 bound / 1 refused), 0 violations. x87: PASS, 224 reachable. |
| 2026-09-14 | Step D review fixes (Opus review of `051b765`): `run_ownership.py` wrapped count set to the observed 563 (2 device iterations × 5 sentinel checks over 553); sentinel memset timed (`sentinel_us=` beside `sentinel_bytes=`); the AABB comparison runs on capture frames / `--screen-emission-timing` only and the fixture repeats the four batches outside the capture window (`_plain`) for the production cost; `project_prefix` takes the pad/cut |term| sums from the prefix extent's 8 corners instead of per vertex; `--hull` reports cap-truncated pads (`capped=`); `rechecks` in the JSON record; step-D native gap in `platform-portability.md`. | `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_ownership.py`; the step-D host, live-prefix and x87 commands above | Ownership: baseline 370 / wrapped 563 checks, 0 failures. Host: 19 tests OK (`--hull` capped pads reported: 72 of 345 bound lists at the 8 px cap, all at bullet-scale cancellation). Live prefix: PASS 24 frames (18 bound / 6 refused), same rectangles on the plain copies; derive_us plain **1.7 / 4.2 / 9.0 / 27.2 µs at 27 / 72 / 176 / 605 bolts** (with AABB comparison 3.4 / 6.6 / 29.9; fan first-draw outlier 369 µs on the capture copy, 9.0 plain), 20 scans 10.3 µs mean, 22 locks, sentinel 2,434,608 B in 371 µs (16.9 µs per lock, whole-window fills dominate), 0 rechecks. x87: PASS, 224 reachable. |
| 2026-09-14 | User run 19 (snapshot `/tmp/x3-bottleX3-run47/`, log `session-20260914-225307-216.log`, installed DLL `ab6e17ba…` from `5d06316`; pre-step-D build, step D rides run 20). Firing captures are frames 7877 and 9326, the only frames carrying `packed_sample` lines; `packed_admitted=4` on both. The run-15 near-fullscreen box recurs in this build, as expected without step D. User report: the bolts look right, so gain 1 (native parity) is accepted and no gain-2 run was made. | Log queries only (no Wine run, no fixture): `packed_sample`, `screen_emission_frame`, `linear_composition_frame` and `locked_prefix_frame` greps over the run-47 log, never read whole | Firing brackets: `brackets_px` 2,659,442 (7877) and 2,802,580 (9326); rects `166,0,1280,642` (72.8 % of 1280×768) and `303,0,1280,629` (62.5 %) at 7877, `119,0,1189,681` (74.1 %) and `210,0,1205,676` (68.4 %) at 9326. Elsewhere frame 2484 (2 brackets at 100 %) and frames 2485, 2490, 2499, 2520, 2625, 5523, 9329 (4 brackets at 100 %, `brackets_px` 3,932,160). `packed_sample`: argmax pre 0.002–0.33 → post 0.95–0.97, `changed_px` 0.13–0.64 %, `max_post_y` 0.952–1.041 — the composite is not a no-op on bullet pixels. `screen_emission_frame` 24,728 lines, 1,652 admitted (447 with 2 brackets, 1,205 with 4). `cpu_us` 6,975–256,317 (mean 12,077 µs) is inflated by the locked-prefix scan (`scan_us` 454,793 over 67,940,352 vertices at 7877) and is not a production cost figure. |
| 2026-09-15 | Run-20 install candidate `39b090d0` built from `77a649b` (step D plus its review fixes, the `loading_phase` markers and the z_only prepass jitter): step-D live evidence re-established on the candidate sources. | `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_locked_prefix_live.py --fixture verification/probe/build/locked_prefix_live_fixture.exe --dll build/d3d9.dll`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_distance_fade_live.py --screen-emission --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll`; `PYTHONPATH=verification/probe:verification/analysis python3 -m unittest verification.analysis.test_fade_region verification.analysis.test_linear_distance_fade_report verification.analysis.test_screen_emission_display_ratio verification.analysis.test_game_phases verification.analysis.test_loading_phase_markers verification.analysis.test_loading_phases verification.analysis.test_depth_prepass_profiles verification.analysis.test_motion_output_runner`; `python3 verification/probe/check_no_x87.py <candidate>` | Live prefix: PASS 24 frames (18 bound / 6 refused), hull vs AABB of the viewport fan_176 1.2 % vs 89.6 %, straddle_72 17.5 % vs 100 % (clipped 18, pad 3), big_605_origin_tail 10.6 % vs 96.5 % (scanned 6144, drawn 3630), small_27 0.1 % vs 70 %; GPU footprint inside the rect on all bound frames; derive plain 1.9 / 4.7 / 8.7 / 27.5 µs at 27 / 72 / 176 / 605 bolts; 20 scans 10.5 µs mean (14 window-end), 22 locks, sentinel 2,434,608 B in 419 µs, 0 rechecks; all 24 frame lines carry `hull_px=`, `aabb_px=` and `sentinel_us=`. Fade live `--screen-emission`: PASS 14 cases (functional, off, timing-line, gain 2, straddle, caps and the eight paired timing runs); chain frame 21 rect (8,24,38,40) 480 px, 456 changed, 8 layers, max 1 code, alpha exact, gain 2 within fraction 0.151 on 1,300 bolt channels (withdrawn law off by 0.381); functional 22 frames / 28 sources, 361 px per bracket (straddle 128), 59 held references, 53 samples within 0.124 (gain 2: 0.243); paired per-bracket source delta 0.221 / 0.312 ms at 1280x768. Host: 48 tests OK. x87: PASS, 224 reachable, 0 violations. Record: `verification/results/run20-candidate-install.json`. |
| 2026-09-15 | User run 20 (snapshot `/tmp/x3-bottleX3-run48/`, log `session-20260915-002408-212.log`, 335 MB, installed DLL `39b090d0…` from `77a649b`, record `verification/results/run20-candidate-install.json`): first gameplay run of step D. The near-fullscreen bracket of runs 15/19 is gone — no frame brackets at fullscreen, and the firing window sits at 3.8–12.4 % of the frame. User report: the bolts look the same, possibly very slightly dimmer, unsure; `gain=1` in both runs, so the step-D bound is accepted on shape and the brightness question stays open. | Log queries only (no Wine run, no fixture): `locked_prefix_frame`, `screen_emission_frame`, `packed_sample` and `fade_witness` greps over the run-48 log, never read whole | Locked prefix: 916 lines, **zero refusals**; hull/aabb ratio mean 0.971 overall and 0.32–0.35 on the firing frames 23902–23909 (`hull_px` ≈1.05–1.11 M against `aabb_px` ≈2.8–3.45 M); `window_end_scans`/`scans` mean 5.5 %; `sentinel_us` accumulates to 1.04 ms by frame 50520 (63,309 scans). `screen_emission_frame` 50,654 lines, 3,641 admitted; `brackets_px` min 176 / max 284,276 / mean 21,874; firing window 37,500–122,028 px. `cpu_us` 132,054–178,493 during the capture burst (the locked-prefix scan again) against ≈8–10 k elsewhere; not a production cost figure. `packed_sample` 32 lines (frames 23902–23909, 4 per frame): the sampled rect and `scan_px` equal `hull_px` in 32/32, so the sample covers the hull, not a centre box; `max_post_y` 0.776–0.991 (mean 0.924), firing-frame peaks 0.959/0.962 against run 19's 0.954/0.967; `changed_px` 5–32 % of the small hull rect against 0.13–0.64 % of run 19's near-fullscreen rect. `fade_witness` 1,689 lines (k=30), 0 with nonzero outside anywhere — but none of them fall on the firing frames, so the witness does not cover the bolts in this run (run 21 uses `--fade-witness 1`). |
| 2026-09-15 | User run 21 session A (snapshot `/tmp/x3-bottleX3-run49/`, log `session-20260915-010311-212.log`, 495 MB, installed DLL `39b090d0…` from `77a649b`): the same step-D build with `--fade-witness 1`, so the witness samples every frame and finally covers the firing frames that run 20 missed. **The "dimming outside the hull" hypothesis is refuted**: not one sampled pixel outside the published region is ever touched, on the firing frames or anywhere else. User report: the bolts are unchanged, so step D is accepted in game. | Log queries only (no Wine run, no fixture): `fade_witness`, `screen_emission_frame`, `packed_sample` and `locked_prefix_frame` greps over the run-49 log, never read whole | `fade_witness` 13,385 lines, **0 with nonzero outside, max outside 0**. Firing group frames 9889–9896 (the only burst with `packed_prepared>0` and `rects=4`): `outside=0` on all 8, covered 8,173–18,695 px, union 33,605–58,313 px. Frame 9895 `packed_sample` rects `517,382,651,624` and `637,379,731,570`, `clipped=0`, composed rect equal to the sampled rect, inside `hull_px` 1,083,804 (`aabb_px` 3,269,680). `max_post_y` peak 0.990 against run 20's 0.959/0.962 and run 19's 0.954/0.967 — no brightness loss on bullet pixels. `brackets_px` max 127,282 (12.9 % of the viewport), 1,180 of 13,385 frames nonzero, **none fullscreen**. `sentinel_us`/`sentinel_bytes` are running totals in this log (303,560 µs / 1.168 GB by frame 13,260, ≈23 µs per frame), not per-frame costs — the reporting convention differs from the per-frame `cpu_us`/`derive_us` fields and should be stated in the grammar (open doc item). `cpu_us` on admitted frames outside the capture bursts: max 58,331 µs, again the locked-prefix scan and not a production cost figure. |
| 2026-09-15 | `--screen-emission` decoupled from `--linear-materials` ([linear-material-decoupling.md](../architecture/linear-material-decoupling.md)): launcher gate now `--taa --motion-output --ownership --hdr --hdr-tonemap` + gamma2.2 decode; `capture.cpp` / `configure_screen_emission` drop the material prerequisite; the sampler-state hook is installed and `resync_samplers` queries the stage-0 sRGB flag when the option is on (the readiness gate's only material by-product). `--fade-witness` accepted with `--screen-emission` alone. Runner gained the `screen-nomaterials-functional/-off/-gain2` trio (`X3M_LINEAR_MATERIALS=0`, distance fade 0, mip bias 0), parametrised required-producer/policy bits and the emission-named frame lines of the both-off run. Worktree build, not the install candidate. | `cmake --build build -j4` (0 warnings); `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `sh verification/probe/build_motion_output.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_distance_fade_live.py --screen-emission --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll`; `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_screen_emission_live verification.analysis.test_fade_region verification.analysis.test_linear_distance_fade_report`; `./x3run … --hdr --hdr-tonemap --fade-witness 1 --screen-emission --dry-run` without `--linear-materials`/`--sun-shadow-lane` | x87 check PASS (225 reachable, 0 violations); seam DLL `fcafd552ab0d…`, production `06d8e7a15b1c…`. Live: **passed, 17 cases, 34.0 s**. `screen-nomaterials-functional` totals equal `screen-functional` exactly (eligible 20, admitted 14, linear 13, incomplete 1, unbounded 3, caps 0, region px 5183); witness 11 sampled frames, **outside 0**; `chain_nomaterials` frame 21: 480 px, native_changed 468, max 1 FP16 code, alpha exact, gain-2 tolerance fraction 0.152 ≤ 1 (materials-on twin: 456 changed, 1 code, 0.151); `screen-nomaterials-off` packed_eligible 0, no witness lines (unconfigured without either route). Host: 33 tests OK (launcher: refused without `--ownership`/`--hdr-tonemap`/`--taa`/gamma2.2, accepted without `--linear-materials` with `X3M_LINEAR_MATERIALS=0`, `X3M_LINEAR_DISTANCE_FADE=0`, `X3M_SCREEN_EMISSION=1`). Dry-run rc 0, env `X3M_SCREEN_EMISSION=1 … X3M_LINEAR_MATERIALS=0 X3M_SUN_SHADOW_LANE=0 X3M_FADE_WITNESS=1`. |
| 2026-09-15 | Review follow-ups to the decoupling: the DLL gate (`capture.cpp`) now also requires `X3M_OWNERSHIP=1` (the bound's Unlock scan) and logs `ownership=` in `screen_emission_mode`; host test `DllGate` pins the gate's prerequisites; the runner gained one paired timing run in the user's configuration (`screen-timing-1280x768-pair0-screen{0,1}-nomaterials`: materials 0, mip bias 0, so the sampler-state hook is installed by the option alone) reported as `paired_cost[].nomaterials`; the oracle assumption for fade sources with the route off is documented in the decoupling note. Rebuilt: production `32f8156349d6…`, seam `404964aca07c…`. | same commands as the row above (host suite now 34 tests) | Build 0 warnings; x87 PASS (225 reachable, 0 violations). Live **passed, 19 cases, 36.9 s**; no-materials trio and chain unchanged (totals equal, outside 0, 1 code, alpha exact, gain fraction 0.152). No-materials timing pair at 1280×768 (paired-window medians, 4 samples per count): per-bracket source delta **0.363 / 0.167 / 0.169 ms** at 1/4/16 ordered DIPs (materials-on pair0 twin 0.167 / 0.166 / 0.168; the count-1 value is a single-bracket window at the noise floor); per-frame total delta 0.30 / 0.74 / 2.56 ms against off totals 2.40 / 2.28 / 2.36 ms; terminal delta 0.000 / 0.073 / −0.097 ms. The newly installed sampler hook adds no measurable per-bracket cost beyond the bracket itself. |
| 2026-09-15 | `--emission-source-gain G` (`X3M_EMISSION_SOURCE_GAIN`, finite 1..8, default 1 = off; requires `--hdr` only): source-only encoded gain of the twenty additive PS2 pairs, `linear_emission_source_gain_variant` inserts `def c31 = (G,0,0,0)` and `mul r0.xyz, r0, c31.x` before the untouched native `mov oC0`; `MotionOutput::prepare_source_gain` binds it natively for one draw in the exact ADD/ONE/ONE state with the FP16 redirect active and restores after the draw; other blends refuse with `emission_source_gain_refused reason=blend`. No bracket, copy or composition (`docs/architecture/linear-emission-cost.md`, "Implemented"). | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_linear_emission_source_gain verification.analysis.test_linear_emission_transformer verification.analysis.test_linear_emission_original_report verification.analysis.test_linear_emission_report verification.analysis.test_screen_emission_live verification.analysis.test_linear_material_fill`; `sh verification/probe/build_linear_emission.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_emission.py --mode source-gain --exe verification/probe/build/linear-emission/linear_emission_fixture.exe`; `cmake --build build -j4`; `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `./x3run <run-28 command> --emission-source-gain 2 --dry-run` | Host: 63 tests OK; driver 10 programs, 20 pairs, 40 variants, 10 byte-identical at gain 1, max 8 arithmetic slots, 602 checks. GPU (`linear-emission-source-gain-gpu.json`): 80 cases (20 pairs × flat/2×2 source × zero/non-zero background), 400 draws; gains 2, 3.5, 8: 61,440 colour channels each, all exact (max 0 FP16 codes; zero background = G × native, non-zero background within the propagated half-ULP interval), 48,000 covered channels brighter per gain, alpha 61,440 channels bit-exact per gain, gain-1 image bit-exact (81,920 channels), max value 4.75 (unclipped). DLL built from worktree `7268cf5` + this diff, 0 warnings; x87 PASS, 225 reachable, 0 violations. Dry run: `X3M_EMISSION_SOURCE_GAIN=2.0`, `X3M_LINEAR_EMISSIONS=0`. |
| 2026-09-16 | Run 26 (run65, `/tmp/x3-bottleX3-run65/session-*.log`): `--emission-source-gain 2` admitted zero engine draws; all 16 logged refusals were `reason=blend` with ONE/ONE/ADD colour and `sepalpha=1` (11) or ONE/INVSRCCOLOR screen (5), the 16-line cap hid the totals. Fix: the colour law moved to the shared `renderer::linear_emission_source_gain_blend` (ONE/ONE/ADD admits regardless of SEPARATEALPHABLENDENABLE and the alpha triple, ONE/INVSRCCOLOR refuses as `screen_blend`, the rest as `blend`); the alpha triple joins the composition blend shadow (`composition_blend[4..6]`, setter hook + resync, no per-draw getter) and is logged only; `emission_source_gain_frame ... admitted= refused_blend= refused_screen= refused_other=` every frame with a candidate draw, capture or not; refusal samples capped 16 per reason per device epoch. Fixture: 40 separate-alpha cases (ONE/ONE colour, SRCALPHA/INVSRCALPHA alpha) and 40 screen cases added to `--mode source-gain`. Worktree build, not the install candidate. | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_linear_emission_source_gain verification.analysis.test_linear_emission_transformer verification.analysis.test_linear_emission_original_report verification.analysis.test_linear_emission_report verification.analysis.test_screen_emission_live verification.analysis.test_linear_material_fill`; `cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build build -j4` (clean); `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `sh verification/probe/build_linear_emission.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_emission.py --mode source-gain --exe verification/probe/build/linear-emission/linear_emission_fixture.exe` | Host: 73 tests OK (blend-law truth table 12 cases compiled from the renderer source, refused/frame line grammar, per-reason cap, fixture/runner gate coverage). GPU (`linear-emission-source-gain-gpu.json`): passed, 160 cases, 800 draws; gains 2/3.5/8: 92,160 colour channels each, all exact (max 0 FP16 codes), 78,720 brighter per gain, alpha 92,160 bit-exact per gain, gain-1 identity 163,840 channels, max 4.75; separate alpha: 40 cases admitted, 92,160 colour channels exact, 30,720 alpha channels bit-exact native, 7,680 covered pixels with alpha actually blended; screen: 40 cases refused `screen_blend` (fixture reads back src=2 dst=4), 122,880 channels native, 0 darkened pixels. DLL: clean worktree build, 0 warnings; x87 PASS, 225 reachable, 0 violations. Not installed, no game run. |
| 2026-09-16 | Run 27 (run68, `/tmp/x3-bottleX3-run68/session-20260916-011944-212.log`, 140 MB, never read whole; `--emission-source-gain 2 --screen-emission-additive 2`, the first run admitting the separate-alpha draws): user sees a broad soft orange halo around the weapon impact point and brighter bolts. Log totals over 20,643 `emission_source_gain_frame` lines: admitted 21,437, refused_blend 0, refused_screen 37,313, refused_other 66,024 = refused_state 66,024 (unknown 0, bind 0); the 16 `emission_source_gain_refused` samples are all `screen_blend` ONE/INVSRCCOLOR of `d5e1c753`/`8360f422`; only PS `8360f422` was ever created (10 variant lines), so the admitted draws are the two base pairs (DEFAULT `d5e1c753`, INSTANCE `89193868`) and the log cannot tell them apart; `admitted=2` in 6,442 frames (engine baseline), `admitted=8` bursts (frames 8260–8299 …). Change: gain split by pair family (`linear-emission-cost.md`, "Family split"): `--emission-source-gain` = engine family (5 pairs), new `--effect-source-gain` (default 1 = original bytes) = effect family (15 pairs); per-pair first-admission line, per-family frame counts and a state-refusal sample line. The bolt brightness (`--screen-emission-additive 2`) is unchanged; the halo attribution to the effect family is a hypothesis until the pair log of the next run confirms it. Worktree build, not the install candidate. | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_linear_emission_source_gain` (+ `test_linear_emission_transformer test_linear_emission_original_report test_linear_emission_report test_screen_emission_live test_linear_material_fill test_motion_output_runner`); `cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build build -j4` (clean); `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `sh verification/probe/build_linear_emission.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_emission.py --mode source-gain --exe verification/probe/build/linear-emission/linear_emission_fixture.exe`; `python3 tools/manage.py launch --bottle X3 <run-27 command> --effect-source-gain 1 --dry-run` | Host: 16 tests OK (family table 20/20 rows one family each, 5 engine, compiled lookup 80-cell matrix; both launcher gates; frame 13-field / pair 6-field / state 16-field grammars); siblings 74 tests OK. Build 0 warnings, DLL `98e67ebc1871…`; x87 PASS, 230 reachable, 0 violations. GPU (`verification/results/bottle-X3/linear-emission-source-gain-gpu.json`): passed, 200 cases, 1,000 draws; gains 2 / 3.5 / 8: 122,880 / 92,160 / 122,880 colour channels all exact (max 0 FP16 codes), 109,440 / 78,720 / 109,440 brighter, max 4.75; family split 40 cases (10 engine, 30 effect): 80 native slots 81,920 channels bit-exact, 80 gained slots 61,440 channels exact; separate alpha 40 admitted, screen 40 refused native, gain-1 identity 163,840 channels. Dry run rc 0: `X3M_EMISSION_SOURCE_GAIN=2.0 X3M_EFFECT_SOURCE_GAIN=1.0 X3M_LINEAR_EMISSIONS=0`. |
| 2026-09-15 | Additive option implemented (`--screen-emission-additive G`, `X3M_SCREEN_EMISSION_ADDITIVE`, architecture note "Additive option"): the same nine pairs in the same exact native screen state draw in place with `DESTBLEND ONE` and the `AdditiveGain` PS2 variant (`mul r0.rgb, r0, c31.x`, 73 words for `ec1f5c4a2f4e1445`, alpha unchanged); needs `--motion-output --hdr` only, exclusive with `--screen-emission`. Live case `screen-additive-gain2` (G = 2, packed/emission/fade/material routes off, TAA on for the fixture's verifier): bolt over the dark quad (background max 0.04999) and over the white scene (background 1.0), every quad pixel `G q + D` within one FP16 code (max 1 code apart on the dark bolt, 0 on the bright), 512 of 512 bolt pixels above 1.0 (max 2.0), `additive_admitted 2 / refused 2` (opaque-state and PROJECTED kinds native, the latter matching the native saturating law at both samples), the fixture's exact blend/mask/shader restoration after every draw; the 19 pre-existing cases' functional fields unchanged (`passed=true`, 20 cases; the paired timing numbers vary run to run). Admission guards mirror the packed route's (state, dither, draw shape, scene/queries/recording/MSAA, FP16 target, variant, sRGB sampler 0, PROJECTED), each refusal reason logged once per device as `screen_emission_additive_refused reason=`. Host: 18 tests in `test_screen_emission_live.py` (launcher gate, exclusivity, range, DLL gate, synthetic additive parser); x87 audit PASS (225 reachable). Native Windows behaviour unverified. | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_screen_emission_live`; `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_distance_fade_live.py --screen-emission --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll` → `verification/results/bottle-X3/screen-emission-live1.json` |
| 2026-09-15 | Host transform coverage for the `AdditiveGain` outputs, closing the gap noted in [host-suite-2026-09-15.md](host-suite-2026-09-15.md) (`test_screen_emission_live.py` only asserted the wiring string). New driver `verification/probe/screen_emission_additive_structure.cpp` (separate from `linear_emission_sm1_structure.cpp`, whose `variants`/`max_slots` pins are untouched) writes the nine pairs' AdditiveGain programs at G = 1, 2, 3.5, 8 plus a `Native` reference and reports `pairs 9 / programs 6 / gains 4 / variants 36 / identical 9 / checks 974 / max_words 83 / max_arithmetic 5` (PS2 budget 64). Oracle `verification/analysis/test_screen_emission_additive_transform.py` (6 tests) asserts per pair and gain: version word `ffff0200`, exactly one added `mul r0.xyz, r0, c31.x` (unpredicated, not coissued) and one `def c31 = (G, 1, 0, 0)` against the native reference, +10 words, the native `mov oC0, r0` retained verbatim as the last instruction with the mul penultimate, no instruction between them, no added writer of `r0.w` (native r0.w writers unchanged), texture/arithmetic/constant counts inside ps_2_0 and c31 < 32, and at G = 1 byte-identity with the original program (the runtime creates no variant there: `screen_additive_gain_ != 1.f`). Driver also pins invalid-gain (0, 0.5, 0.999, 8.001, 16, -1, inf, NaN), partial-precision, null, truncation and re-promotion refusals with caller output and aliased input preserved. Corpus `/tmp/x3-shader-sweep/programs` (751 programs); absent corpus skips with a printed reason unless `X3M_REQUIRE_SHADER_CORPUS=1`. No production source change; no Wine, no game. | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_screen_emission_additive_transform` → 6 tests OK; `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_linear_emission_sm1_transformer verification.analysis.test_linear_emission_sm1_report verification.analysis.test_screen_emission_live` → 40 tests OK |
| 2026-09-16 | Runtime emitter A/B hotkeys (`docs/architecture/comparison-hotkeys.md`): Ctrl+Shift+**F5** additive bullets, **F6** `--emission-source-gain` (engine family), **F4** `--effect-source-gain` (effect family; F7 is the telemetry marker, F8 the capture key). Three enable flags default on; the per-draw gate skips `prepare_screen_additive` / `prepare_source_gain` when a family is off, so the draw is native with no substitution and nothing is created, released or reconfigured at toggle time. A family that was not requested or whose gain is 1 (no variant) is a logged no-op (UNAVAILABLE). Toggle logs `screen_emission_additive_toggle device= frame= accepted= enabled= requested= gain=` and `emission_source_gain_toggle device= frame= family= accepted= enabled= requested= gain=`, plus `renderer_comparison key=ctrl_shift_f5|f6|f4`. New per-Present telemetry line (only with `--telemetry`): `screen_emission_additive_frame device= frame= admitted= refused= pairs=<hex mask over the nine `screen_emission::pairs` indices admitted this frame> toggled=<option on>`, counters reset every frame. Gain caps unchanged at finite 1..8 in the launcher, the DLL parser, `linear_emission_source_gain_valid` and the SM1 additive validator, so 5 is accepted on all three options. No Wine, no GPU fixture, not installed. | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_comparison_hotkeys verification.analysis.test_linear_emission_source_gain verification.analysis.test_screen_emission_additive_transform verification.analysis.test_linear_material_live verification.analysis.test_motion_wrap_states verification.analysis.test_capture_bloom_lifetime verification.analysis.test_motion_hdr_scene verification.analysis.test_linear_cutout_contract`; `cmake -S . -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo -B <worktree build> && cmake --build <worktree build> -j4` | Host: 71 tests OK; `comparison_controls` fixture 11,116 checks / 0 failures (release and ASan+UBSan), `linear_material_live` 20,495 checks / 0 failures, `motion_wrap_states` 34,773 / 0, `linear_cutout_contract` 41 scenarios 284 checks / 0. DLL build: 0 warnings, 0 errors. |
| 2026-09-16 | Per-source bloom attenuation of the additive bullets: `--screen-emission-additive-alpha K` (`X3M_SCREEN_EMISSION_ADDITIVE_ALPHA`, finite 0..1, requires `--screen-emission-additive`; absent keeps `a + D.a`), the blend-state form of option 1 in `docs/architecture/bloom-per-source-attenuation.md`. The admitted draw writes `K*a + D.a` to the scene alpha the bloom extract weighs by while the colour law stays `G q + D`: separate-alpha blending applied after the program bind (BLENDFACTOR only for 0 < K < 1, SRCBLENDALPHA ZERO/ONE/BLENDFACTOR, DESTBLENDALPHA ONE, BLENDOPALPHA ADD, SEPARATEALPHABLENDENABLE last) and restored in reverse from the setter-hook shadow, which now also holds BLENDFACTOR (`composition_blend[3..7]`, count 7 -> 8). Two new fail-closed refusal reasons before the first setter, draw unchanged and native: `alpha_state` (an alpha state not shadowed) and `alpha_caps` (no D3DPMISCCAPS_SEPARATEALPHABLEND, or no D3DPBLENDCAPS_BLENDFACTOR for 0 < K < 1); a failed setter mid-sequence unwinds the applied states, the program and DESTBLEND. Logged in `screen_emission_additive_mode` as `alpha=K|native alpha_requested= alpha_valid=`; Ctrl+Shift+F5 turns it off with the option, and (review fix) F5 is accepted whenever the additive option is requested, since G=1 still changes the draw; only the two source-gain keys keep the gain-1 UNAVAILABLE case. The launcher omits X3M_SCREEN_EMISSION_ADDITIVE_ALPHA when the option is absent rather than exporting an empty value. | `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_distance_fade_live.py --screen-emission --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll`; `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_comparison_hotkeys verification.analysis.test_linear_emission_source_gain verification.analysis.test_screen_emission_additive_transform verification.analysis.test_linear_material_live verification.analysis.test_motion_wrap_states verification.analysis.test_capture_bloom_lifetime verification.analysis.test_motion_hdr_scene verification.analysis.test_linear_cutout_contract` (+ `test_screen_emission_live`); `cmake -S . -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo -B build && cmake --build build -j4`; `sh verification/probe/build_motion_output.sh` | GPU (`verification/results/bottle-X3/screen-emission-live1.json`): **passed, 22 cases** (20 before), the 20 pre-existing unchanged. New `screen-additive-alpha0` and `screen-additive-alpha-half`: each admitted 2 / refused 2 / 0 failures, 512 quad pixels checked per case, colour max **1 FP16 code** apart from `G q + D` (identical to the unattenuated `screen-additive-gain2` baseline), alpha max **0 codes** apart from `K*a + D.a` on 512/512 pixels, and at K=0 the written alpha equals D.a **bit-exactly**; K=0.5 was applied as the quantised 0.50196 (BLENDFACTOR supported, no skip), max_after 2.0, 256 pixels above 1.0 per bolt. Host: 53 tests OK (+34 in `test_screen_emission_live`/`test_linear_emission_source_gain`). Builds: DLL and seam 0 warnings. Not installed, no game run. |
| 2026-09-16 | Run 28 (run74–run80, DLL `2b0969e5…` from `a26eb9b`; `--emission-source-gain 2 --screen-emission-additive 2`, effect gain 1). Halo: the user reports it persists with `--hdr-ev-max 1.0` (run74/75) and with `--taa-sharpen 0 --taa-mip-bias 0`, and disappears only when bloom is toggled off (Ctrl+Shift+F10) — it is bloom of the >1.0 bolts, more visible over black than over nebula, not exposure, sharpen or mip bias. The 21 vs 13 MB `bloom_prepare` working set is the RGBA16F sharpen-stage buffer (1280×768×8 = 7,864,320 bytes, allocated only when sharpen > 0), exactly the 20,958,720 − 13,094,400 difference; `create_device`/`hdr_target` width=1280 height=768 identical in every run, so resolution is not involved. Emitter admission, `emission_source_gain_frame` summed per run: engine family admitted 162 (run76, 2,014 frames) / 5,939 (run77, 5,837) / 13,287 (run78, 7,702) / 0 (run79, 667) / 1,559 (run80, 3,531); effect family admitted **0** in all five runs across ~19,751 effect-family evaluations, as expected at effect gain 1 (no transformed variant). The additive option had no per-draw telemetry in this build (only the static `screen_emission_additive_mode` and one `screen_emission_additive_variant`, `ec1f5c4a2f4e1445`), so "additive affects engines too" is neither confirmed nor refuted. Follow-ups merged: runtime A/B hotkeys Ctrl+Shift+F5/F6/F4 and `--screen-emission-additive-alpha K` with the per-frame `screen_emission_additive_frame` counter (`6c8a375`); design [bloom-per-source-attenuation.md](../architecture/bloom-per-source-attenuation.md) ratified. | Session A logs `/tmp/x3-bottleX3-run74 … run80/session-*.log` (never read whole; `grep bloom_prepare`, `grep emission_source_gain_frame`, `grep create_device`) | Halo attributed to bloom of the additive bolts; working-set difference fully explained by the sharpen stage; engine-family gain demonstrably applies to real draws, effect-family gain is a no-op at 1. No new host or GPU evidence in this row. |
| 2026-09-16 | Screen substitution and the family merge (`docs/architecture/linear-emission-cost.md`, "Screen substitution"): one `--emission-source-gain G` / `X3M_EMISSION_SOURCE_GAIN` / Ctrl+Shift+F6 for all twenty pairs, `--effect-source-gain` / `X3M_EFFECT_SOURCE_GAIN` / F4 removed (launcher error naming the replacement; `LinearEmissionFamily` gone, `linear_emission_pair_index`); an admitted ADD/ONE/INVSRCCOLOR draw gets DESTBLEND ONE substituted for the draw (`prepare_source_gain`, restored by `finish_source_gain` through the setter shadow; failed setter = `refused_screen`, failed bind unwinds the DESTBLEND), law `G*S + D` (brighter by `D*S` over a lit background, alpha `a + D.a` with separate alpha off: the accepted trade); ONE/ONE unchanged; frame line `gain= admitted= admitted_screen= refused_blend= refused_screen= ...`, pair line `gain= screen=`. Worktree build, not the install candidate. | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_linear_emission_source_gain verification.analysis.test_comparison_hotkeys verification.analysis.test_screen_emission_live verification.analysis.test_linear_material_live verification.analysis.test_motion_wrap_states verification.analysis.test_capture_bloom_lifetime verification.analysis.test_motion_hdr_scene verification.analysis.test_linear_cutout_contract`; `cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build build -j4` (clean); `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `sh verification/probe/build_linear_emission.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_emission.py --mode source-gain --exe verification/probe/build/linear-emission/linear_emission_fixture.exe`; `python3 tools/manage.py launch --dry-run --direct --motion-output --hdr --emission-source-gain 5` | Host: 67 tests OK. Build 0 warnings, DLL `7a41c699…`; x87 PASS, 230 reachable, 0 violations. GPU (`verification/results/bottle-X3/linear-emission-source-gain-gpu.json`, bottle X3 arm64): passed, 160 cases, 800 draws, 120 substituted; the 120 ONE/ONE case records (80 additive + 40 separate alpha) byte-identical to the previous raw baseline (`pixels.bin` sha `(hash not retained)…`, gains 2/3.5/8: 92,160 channels each, all exact, max 4.75); screen 40 cases (black and grey 128/255): gains 2 / 5 / 8: 30,720 colour channels each, all exact (max 0 codes) against `G*s + D`, 30,720 brighter than the native screen draw, alpha 10,240 per gain exact against `a + D.a`, max 4.5; gain-1 slot 40,960 channels bit-identical to the native screen draw, which matches `s + D(1 - s)` within one code (30,720 channels, 23,168 exact). Dry run: `X3M_EMISSION_SOURCE_GAIN=5.0`, no `X3M_EFFECT_SOURCE_GAIN`; `--effect-source-gain 2` exits 2 naming `--emission-source-gain`. Not installed, no game run. | Note: with separate alpha off the substituted draw writes `a + D.a`, so engine glow over alpha-authored backgrounds raises scene alpha and the authored-glow bloom term; `--bloom-source-clamp` bounds the resulting halo, and no per-draw alpha attenuation exists for these draws yet.
| 2026-09-17 | Run 30 session A (run87, DLL `bbadc568…`): single `--emission-source-gain 2` admitted 70,996 draws of which 54,589 screen-substituted (engines now respond, user confirmed); explosion drawn by the same effects pair, no new program in 82 sessions; `--bloom-source-clamp 1.0` active on every frame, bolt halo accepted by the user with `--screen-emission-additive-alpha 0` (kept). |
| 2026-09-17 | Emitter plan phase 3, transform and evidence only (no live route): the hull-program emitters the effects gain cannot reach (position lights, deco flares, warning signs, warp tunnels; the ADD ONE/ONE material census in [effect-shader-users.md](../reverse-engineering/effect-shader-users.md)). `linear_emission_hull_source_gain_variant` covers the **twelve** exact ps_3_0 material originals (six `XT_standard_lighting.fx`, six `standard_lighting.fx`), hash-keyed with pinned DWORD sites and a narrow SM3 walk that refuses relative addressing, a read of the gain constant or an executable body over the documented 512-slot ps_3_0 budget. One MUL-class insertion per program **at the emission term**: `def c223 = (G,0,0,0)` at the first declaration and `mul r0.xyz, r0, c223.x` immediately before the final colour instruction, whose added operand is that r0 - the unlit emission sample (`add oC0.xyz, r1, r0`, or `mad oC0.xyz, r1, r2.z, r0` on XT). The lit hull shading, the native alpha `mul oC0.w, r2.w, v0.w` and every other word are verbatim, so a pixel whose emission sample is zero is bit-identical; gain 1 returns the original bytes. Admission law `linear_emission_hull_source_gain_blend`: only ADD ONE/ONE with blending on and sRGB write off; screen is **not** substituted in this population (the plan keys phase 3 on the ONE/ONE materials). Scope of the gain: it scales exactly the sampler value the final colour instruction adds from r0 (`texld r0, v1, s2` or `s3`, the illumination/glow map), and no diffuse, specular, reflection or vertex-colour term; the inserted `mul` carries no `_pp` although its neighbours do (full precision on one multiply, no original word changed). **Open**: nothing yet establishes that the emitter art of these materials (`poslight.tga`, `decoflare.tga`, `warning.tga`, `construction_signs.tga`, `fx_warp_tunnel.tga`) is bound to that sampler rather than to the diffuse one - the census names textures, not slots - so if the art is diffuse-authored the option will brighten little; the first user bracket at G = 2 decides. Launcher: `--hull-emitters` applies the existing `--emission-source-gain G` to this population (`X3M_HULL_EMISSION_GAIN`, default 1.0, requires the gain and `--hdr`); **no proxy variant creation or per-draw admission is wired yet**, so the key is inert in the DLL. | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_linear_emission_hull_gain` (+ siblings `test_linear_emission_source_gain test_linear_emission_pass_programs test_comparison_hotkeys test_linear_material_report`); `sh verification/probe/build_linear_material.sh`; `cmake -S . -B <worktree build> -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build <worktree build> -j8` (clean); `python3 verification/probe/check_no_x87.py <worktree build>/d3d9.dll`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_material.py --exe verification/probe/build/linear_material_fixture.exe --hull-emission-gain`; `python3 tools/manage.py launch --dry-run --vanilla --motion-output --hdr --emission-source-gain 2 --hull-emitters` | Host: 8 tests OK in the new file (driver: 12 programs, 6 modulated tails, 48 variants, 12 gain-1 identities, 1,036 checks, blend truth table 1 admit / 127 refuse; oracle: sequence verbatim per program and gain, DEF/MUL forms and sites, +1 arithmetic slot and +2 instructions, no original read of c223, no relative addressing, oC0 and r1 writers unchanged), siblings 70 tests OK. Fixture build 0 warnings; DLL clean build 0 warnings; x87 PASS, 504 reachable functions, 0 violations. GPU (`verification/results/bottle-X3/hull-emission-gain-gpu.json`, bottle X3): **passed**, gains 2.0 and 4.0, 20 cases each (10 emitter + 10 control faces) over 10 of the 12 programs, 180 samples per gain, **max 0 FP16 codes** against `G x base` on the emitter faces, **2,560 bit-identical pixels** on the control faces (10 x 256, whole image), alpha exact, 10 gain variants created per run. Pairs 148/149 (`fffdabd910793aba`, `e6794b6ec37ff71a`) are host-only: no fixture mode submits their incomplete original XT DEFAULT linkage. Not installed, no game run. |
| 2026-09-17 | Emitter plan phase 3, law change and proxy wiring. **Finding**: the gained `texld r0` sampler (s2 DEFAULT/2s, s3 BUMPMAP) is `t_LightMapTexture`, and every emitter-art material (`poslight`, `decoflare`, `warning`, `construction_signs`, `fx_warp_tunnel`) binds it to `fx\NONE_BLACK.DDS` or NULL with the art in `t_DiffuseTexture`, so the previous r0-only MUL multiplied black. **New law** (`linear_emission_hull_source_gain_variant`): `def c223` at the first declaration, the final colour instruction (`add oC0.xyz, r1, r0` / XT `mad oC0.xyz, r1, r2.z, r0`) redirected to `r0.xyz` with opcode, `_pp`, mask and operands kept (r0 is dead after it), then `mul oC0.xyz, r0, c223.x` with the original destination token: the whole colour output of the ONE/ONE draw is gained; alpha MUL, every earlier word, gain-1 identity, the c223 walk and the 512-slot budget kept; still +10 words / +2 instructions / +1 slot. **Proxy**: one variant per covered original at registration (`hull_emission_variant`), per-draw ONE/ONE admission from the draw-time blend shadow (`prepare_hull_gain`/`finish_hull_gain`; opaque/screen draws `refused_blend`, a routed draw `refused_routed`, no variant `refused_variant`, bind rollback, restore after the draw), `X3M_HULL_EMISSION_GAIN` parsed in capture.cpp (refused without the effects gain), Ctrl+Shift+F6 shared (`hull=` on the toggle line), per-Present `hull_emission_frame ... admitted= refused_blend= refused_variant= programs=` line; default path one bool test per draw. New live fixture mode `hullemission` (`hull_emission_live_inc.h`) and `run_sun_share_live.py --case hull_emission`: the covered pair `494fe349b8bc12ec`/`7c83ed50c9894e44` on object B over an opaque black underlay, diffuse art / lightmap black, three frames (opaque, ADD ONE/ONE, screen) at gain 2 and with the option unset. Snippet mocks extended (hull members plus the pre-existing sun-share/apply/candidate drift the mock lacked). | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_comparison_hotkeys verification.analysis.test_linear_emission_hull_gain verification.analysis.test_linear_material_live verification.analysis.test_linear_emission_source_gain` (+ `test_sun_share_lane`); `sh verification/probe/build_linear_material.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_material.py --exe verification/probe/build/linear_material_fixture.exe --hull-emission-gain`; `cmake -S . -B build -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo && cmake --build build -j8` (clean); `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `sh verification/probe/build_motion_output.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_sun_share_live.py --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll --case hull_emission --result verification/results/bottle-X3/hull-emission-live.json`; `python3 tools/manage.py launch --dry-run --direct --ownership --object-trace --object-lifetime --motion-output --taa --hdr --hdr-tonemap --telemetry --emission-source-gain 2 --hull-emitters` | Host: 47 tests OK (hull oracle 8: 12 programs, 48 variants, 12 identities, DEF + redirected colour + whole-output MUL, alpha writer verbatim, blend table 1/127) + 11 sun-share runner tests OK. GPU slice (`hull-emission-gain-gpu.json`, bottle X3): **passed**, gains 2.0 and 4.0, 20 cases each over 10 programs, diffuse-authored emitter faces **max 0 FP16 codes** against `G x base`, black faces **2,560 bit-identical**, alpha exact, 10 variants per run. DLL clean build 0 warnings; x87 PASS 0 violations. Live (`hull-emission-live.json`, seam DLL `319f028e…`): **passed**; gain 2: 1 variant (`program=7 transform=0 create=00000000 words=1308`), frame 0 opaque refused_routed=1 (the motion route took the draw), frame 1 ONE/ONE admitted=1 programs=080 at **2 x base, max 0 codes on 171 B pixels, alpha native**, frame 2 screen refused_blend=1 (`src=2 dst=4`) at base; gain unset: 0 variants, no hull line, all three frames at base (max 0 codes). Launcher dry-run: `X3M_HULL_EMISSION_GAIN=2.0`, `X3M_EMISSION_SOURCE_GAIN=2.0`. Not installed, no game run; the first user bracket at G = 2 decides. |
| 2026-09-17 | Run 38 session C (run114, `/tmp/x3-bottleX3-run114/session-20260917-080441-216.log`, 29.6 MB, never read whole; `--hull-emitters --emission-source-gain 2`, install `5b4be52e` from `8000230`). User saw no visible difference toggling `--hull-emitters` (Ctrl+Shift+F6). **Finding: the option is not "never admitting" - it fires - but only 2 of the 12 covered programs ever produced an admitted draw, F6 is shared with the much larger `--emission-source-gain` population so the user's toggle mainly changes engine-glow draws, and this build logs no per-draw object identity, so the specific objects cannot be read from this log.** | Log queries only (no Wine run): Python regex counts over `hull_emission_frame`/`hull_emission_program`/`hull_emission_variant`/`hull_emission_refused_state`/`emission_source_gain_toggle`; `grep -c capture_frame\|packed_sample`; `grep -o "^[a-z_]* " \| sort \| uniq -c` for the log's line grammar | `proxy_options`: `X3M_HULL_EMISSION_GAIN=2.0 X3M_EMISSION_SOURCE_GAIN=2.0 X3M_BLOOM_SOURCE_CLAMP=1.0 X3M_HDR_TONEMAP=agx`, `hull_emission_gain_mode requested=1 enabled=1 source_gain=2 gain=2 gain_valid=1`. 24,219 `hull_emission_frame` lines (through frame 26,686), sums: `admitted=123,464`, `refused_blend=361,059`, `refused_routed=1,467,914` (**71 % of all refusals**, `route.routed \|\| route.composition` at `src/proxy/motion_output.cpp:4517`), `refused_state=239,891` (pre-HDR/scene-closed samples, e.g. `hdr=0 scene=0` at frame 4), `refused_variant=0`, `refused_unknown=0`, `bind_failures=0`; 43.9 % of the 24,219 lines have `admitted>0` (median 0 over all candidate-active lines, max 50/frame); gain constant `2` on every line - no downstream clamp evidence in the log (the GPU oracle for this variant already showed the render-target write itself is exactly `G x base`, not clamped there; `X3M_BLOOM_SOURCE_CLAMP=1.0` only bounds what feeds bloom, not the scene colour). `hull_emission_variant`: all 12 covered originals registered (15 lines, 3 duplicate device-epoch re-registrations), `create=00000000` (success) on every one, so every program had a bound variant available. `hull_emission_program` (first admission per program): only **program 7 (`7c83ed50c9894e44`) at frame 3489** and **program 11 (`e70adc744a38ca59`) at frame 1146** ever appear; `programs=` bitmask union over the whole session is `0x880` (bits 7 and 11 only) - the other 10 programs, including **all six `XT_standard_lighting` originals** (`5f82ecacd39529cd`, `6733b119142c8d42`, `fffdabd910793aba`, `496049cec2066ed3`, `e6794b6ec37ff71a`, `f1b0e820c7b488c3`) and four of six `standard_lighting` originals, never once reached the ONE/ONE admission in this flight, though they were bound (e.g. the `hull_emission_refused_state` sample at frame 4 is `ps=5f82ecacd39529cd`). Toggle: `emission_source_gain_toggle` 76 lines, all `hull=1` (hull gain shares the one enable flag with the effects-pair gain, no independent hull toggle exists); the log confirms the toggle actually gates hull admission - during the disabled window frame 4816-4854 there are **zero** `hull_emission_frame` lines at all (not `admitted=0`, no candidate activity logged), resuming at frame 4855 with `admitted=12` immediately, so the mechanism works as designed. `grep -c capture_frame\|packed_sample` = **0** and `proxy_options` carries `X3M_CAPTURE_START=999999` (sentinel: F8 was never pressed) - **this run has no pixel readback, so screen bounds/size of admitted draws cannot be measured from it.** | Census (`docs/reverse-engineering/effect-shader-users.md:237-238`, inference not log-measured): the two programs that did admit belong to the `standard_lighting.fx` row ("cutscene warp tunnels, ANIMPROPS and ship position lights, 6 materials inside `objects/effects/engines/`" — 6 ONE/ONE of that shader's 9+6+... uses per line 163), consistent with small ship-hull position-light dots rather than the `highway_construction_v1/v2.pbb` construction-sign/warning/decoflare/poslight prop cluster that drives the six never-admitted `XT_standard_lighting` originals (632 of 639 uses, line 237) - that prop was evidently not present/visible in this flight. **This object attribution is inferred from the census, not read from the log**: `object_trace`/`object_lifetime` in this build log only one mode-confirmation line each (`object_trace active=1 status=active`) despite `--object-trace --object-lifetime` being requested; no per-draw object/model identifier line exists in this log grammar (checked: full `^[a-z_]* ` prefix census of the log, no `object_*` line beyond the two mode lines), so admitted hull draws cannot be tied to a specific station/ship/object id from this evidence. |
| 2026-09-17 | Hull emitters made visible and testable (worktree on `c27e974`, not installed). **Finding on `refused_routed`**: a routed draw of a covered program is never ONE/ONE. Gate 4 of the motion route admits `ALPHABLENDENABLE=0` only (`draw_state_ok`), the fade arm `fade_route::state` requires SRCALPHA/INVSRCALPHA with z-write off, and the composition route takes the fade band, the SM1 screen pairs and the effects pairs (`prepare_composition`); `route.routed` is set at one site only (`evaluate_draw`), `route.composition` at one. Run 114's 1,467,914 `refused_routed` were therefore the opaque station-hull draws of the same twelve programs, which the ONE/ONE law refuses regardless (the fixture's frame-0 opaque draw was already the witness: routed, then refused). A gained routed variant could never be selected under the ONE/ONE admission, so none is created; instead the admission takes the blend verdict first for every draw and `refused_routed` becomes the fail-closed witness of a ONE/ONE draw a route took (expected 0). `refused_blend` split into `opaque=` (blend off: non-routed opaque draws, e.g. alpha-tested mask-7 XT hulls without the lane) and `alpha=` (SRCALPHA/INVSRCALPHA); both rightly refused, and the sampled `hull_emission_refused` lines now cover blended refusals only. `refused_state` = 239,891 in run 114 sits in exactly two frame ranges, 4-596 and 26,529-26,686 (751 frames, every one with **no** admitted/blend/routed count at all: `hdr=0 scene=0`, `hdr_frame redirected=0`): the main-menu background scene before the FP16 redirect and the exit to the menu, rightly refused (not the FP16 scene target). Independent key **Ctrl+Shift+F4** (`hull_emission_gain_toggle`, notice `HULL ON/OFF/UNAVAILABLE`, `ComparisonKeys::hull_gain`; F6 no longer touches the hull flag), own launcher gain `--hull-emission-gain G` (`X3M_HULL_EMISSION_GAIN` now needs `--hdr` only, `refused=hdr`; `--hull-emitters` alone takes the effects gain's value as before), per-frame `opaque= alpha= toggled=`, and on F8 capture frames one `hull_emission_draw` line per admitted draw (program, node/handle/serial via `sample_scope`, model, LOD, epochs, primitives, projected object origin in pixels) joined per model by `tools/analysis/summarize_hull_emitters.py`. Per-draw cost (from the code, not measured: no hull bind/restore timer exists): an uncovered program still pays one bool; a blend-off draw of a covered program now ends at the `ALPHABLENDENABLE` shadow read (a cache hit even with the hooks off, since gate 4 read it in the same draw) instead of the routed bool, i.e. one load and two compares more for the ~60 routed hull draws per frame of run 114; the admitted path (two native `SetPixelShader`) is unchanged, plus one `sample_scope` and one log line per admitted draw on capture frames only. | `cmake --build build -j8` (worktree, clean configure); `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `sh verification/probe/build_motion_output.sh`; `sh verification/probe/build_linear_material.sh`; `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_comparison_hotkeys verification.analysis.test_linear_emission_hull_gain verification.analysis.test_summarize_hull_emitters` and the six mock-drift modules; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_material.py --exe verification/probe/build/linear_material_fixture.exe --hull-emission-gain`; `... run_sun_share_live.py --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll --case hull_emission --result verification/results/bottle-X3/hull-emission-live.json`; `... run_sun_share_live.py --fixture ... --dll ...` (all 21 cases); `... run_motion_output.py --dll build/d3d9.dll --seam verification/probe/build/motion-output-seam/d3d9.dll --fixture verification/probe/build/motion_output_fixture.exe production-off production-on seam-on production-jitter-on seam-taa-on` | Build 0 warnings, DLL sha256 `ba45cf370e3b2f57…` (17,594,505 B); x87 audit PASS, 521 reachable functions, 0 violations. Host: 15 + 15 + 2 tests OK (controls fixture 11,220 checks, linear_material_live 20,495, motion_wrap_states 34,773, linear_cutout 284, 0 failures). GPU slice: PASS, gains 2 and 4, 20 cases each (10 emitter + 10 control), max FP16 code error 0, control pixels bit-identical. Live `hull_emission` (now 5 frames, no effects gain, capture window frames 1-8): gain 2 → frames opaque/additive/screen/additive_off/additive_on at max_codes 0 on 171 pixels each, `admitted=2 refused_blend=2 (opaque=1 at frame 0, screen at frame 2) refused_routed=0`, toggle `state=0` then `1`, two `hull_emission_draw` lines (frames 1 and 4: `known=1 node=00001100 node_handle=8 node_serial=12 model=00000012 lod=00000002 origin_px=32.0,32.0`), `motion_route` of the pair on frames 1-4 all `routed=0 blend=1`; gain 1 → no variant, no hull line but the two refused toggles (`accepted=0`), every frame at the base; colour hashes identical across the two gains on frames 0, 2, 3 (`59ff6b7d21e23cae`) and differing only on the admitted frames 1 and 4. Sun-lane live set: PASS, 21/21 cases. Motion-output selected cases: production-off/on, seam-on, production-jitter-on, seam-taa-on all exit 0 (checks 43/43/103/43/164, motion_pixels 0/0/44,284/0/44,279, each equal to the same case in the committed `motion-output-summary.json`; `PARTIAL` status is the selected-case mode's own, no cross-case comparison, and the unrelated committed `motion-output-partial.json` was restored rather than overwritten). Run 114's counters are not comparable with the new bucketing (its `refused_routed` population now lands in `refused_blend`/`opaque=`), and `refused_routed=0` in the game is a code-derived expectation until run 39's `hull_emission_frame` lines confirm it. Not installed; the first isolated user bracket is `--hull-emitters --hull-emission-gain 4` with F4 and one F8 capture on a station. |
| 2026-09-18 | Hull light-map gain `--hull-lightmap-gain G` (`X3M_HULL_LIGHTMAP_GAIN`, finite 1..8, default 1 = off, `--hdr` only, excludes `--linear-materials`, composes with `--original-fill`; [linear-emission-cost.md](../architecture/linear-emission-cost.md) "Hull light-map gain", [hull-self-illumination.md](../reverse-engineering/hull-self-illumination.md)): `linear_material_hull_lightmap_gain_pixel_variant` hosts one `def c223 = (G,0,0,0)` and one `mul rL.xyz, rL, c223.x` after the pinned light-map fetch inside `original_fill_transform`; `lightmap_term` proves the term on the original and on the emitted program. Proxy: `hull_lightmap_variant` per reviewed program, selected in `bind_variant_pair` over the plain/fill motion variant under the shared F4 flag, `hull_lightmap_frame` line, `hull_emission_gain_toggle` logs both gains. Worktree build, not the install candidate. | `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_hull_lightmap_gain verification.analysis.test_original_fill verification.analysis.test_comparison_hotkeys verification.analysis.test_linear_emission_hull_gain verification.analysis.test_linear_emission_source_gain`; `cmake -S . -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo -B build/hull-lightmap && cmake --build build/hull-lightmap -j4`; `python3 verification/probe/check_no_x87.py build/hull-lightmap/d3d9.dll`; `sh verification/probe/build_linear_material.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_material.py --exe verification/probe/build/linear_material_fixture.exe --hull-lightmap-gain --raw-dir <scratch>`; `python3 tools/manage.py launch --bottle X3 --dry-run --direct --motion-output --hdr --hull-lightmap-gain 4` | Host: 43 tests OK (new file 6: driver 751 programs, 108 supported, **100 gained / 8 untouched** = the four glass and four asteroid originals, 15,090 checks; oracle: exactly one DEF + one MUL per variant with the authored tokens, MUL directly after the last `texld` (rL r0 in 94 / r1 in 6, stage s2 in 44 / s3 in 56), one consumer `add`/`mad oC0.xyz`, +1 slot and +1 instruction, max base 184 / gained 185 slots, no c223 collision, G=1 and the 8 untouched byte-identical to the fill variant, invalid gains InvalidConfig with rollback; launcher gate: default `1.0`, refused without `--hdr`, with `--linear-materials`, at 0.5/8.5/nan/inf, boundaries 1 and 8 accepted, composes with `--original-fill 0.05`; F4 sources). DLL clean build 0 warnings; x87 PASS, 537 reachable functions, 0 violations. Fixture build 0 warnings. GPU `verification/results/bottle-X3/hull-lightmap-gain-gpu.json`: 108 cases x fill {0, 0.05} at gain 4 (216 draws, 1x1 light-map texel (.5,.25,.125)): every pixel of the FP16 output within **1 binary16 code** of `base32 + (G-1)*L` (max error 1, 972 samples re-derived per run), alpha exact, motion and depth identical, the 8 untouched programs **2,048 pixels bit-identical** per run, 108 mode-13 PS created per run. Dry-run forwards `X3M_HULL_LIGHTMAP_GAIN=4.0` with `X3M_HULL_EMISSION_GAIN=1.0`, `X3M_ORIGINAL_FILL=0.0`. Not covered: live route (no `run_sun_share_live.py` case), the sun-share lane variant, native Windows, texel content of the game light maps. |
| 2026-09-18 | Hull light-map gain composed with the sun-share lane (the user's `--sun-shadow-lane --sun-shadow-apply` configuration): `linear_material_original_sun_share_pixel_variant(..., lightmap_gain, &gain_applied)` carries the same `def c223` + `mul rL.xyz` fragment (share reservations c212/c221, r11-r23: no collision, RE note unchanged); proxy `sun_original_lightmap_variant` beside every share variant, `bind_variant_pair` lane selection `gained_original` under the shared F4 flag, `sun_shadow_original_lightmap_variant` line, fixture counter 79; GPU mode 14 and live case `original_lane_lightmap`. Worktree build on main `022b8339`, not the install candidate. | `verification/probe/hull_lightmap_gain_structure.cpp` (host); `PYTHONPATH=verification/probe python3 -m unittest verification.analysis.test_hull_lightmap_gain verification.analysis.test_original_fill verification.analysis.test_comparison_hotkeys verification.analysis.test_linear_emission_hull_gain verification.analysis.test_linear_emission_source_gain`; `cmake -S . -DCMAKE_TOOLCHAIN_FILE=cmake/mingw-i686.cmake -DCMAKE_BUILD_TYPE=RelWithDebInfo -B build && cmake --build build -j4`; `python3 verification/probe/check_no_x87.py build/d3d9.dll`; `sh verification/probe/build_linear_material.sh`; `sh verification/probe/build_motion_output.sh`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_linear_material.py --exe verification/probe/build/linear_material_fixture.exe --hull-lightmap-gain --raw-dir <scratch>`; `X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_sun_share_live.py --fixture verification/probe/build/motion_output_fixture.exe --dll verification/probe/build/motion-output-seam/d3d9.dll --case original_lane_lightmap --result verification/results/bottle-X3/hull-lightmap-live.json` | Structure driver: 108 supported, 100 gained / 8 untouched on the plain **and** the share variant (same verdict per program), share variant +10 words / +1 slot, max gained share variant 264 of 512 (plain 185, base 184), 19,298 checks. Host: 44 tests OK (new file 7). DLL clean build 0 warnings; x87 PASS 537 reachable, 0 violations; fixture and seam builds 0 warnings. GPU `verification/results/bottle-X3/hull-lightmap-gain-gpu.json`: 108 cases x fill {0, 0.05} at gain 4, each over the plain/fill variant (mode 13) and the share variant (mode 14): every FP16 pixel within **1 binary16 code** of `base32 + (G-1)*L` (max 1; 1,944 samples/run re-derived), alpha exact, motion and depth identical, share law `s'*Y(C')=s*Y(C)` max error **2.6e-5 FP16 codes** of Y(C) (27,648 positive share pixels/run), the 8 untouched programs 4,096 pixels bit-identical per run, 108 + 108 variants per run. Live `verification/results/bottle-X3/hull-lightmap-live.json` (seam DLL, `original_lane` script under `X3M_HULL_LIGHTMAP_GAIN=4`): 2 gained share variants beside the 2 share variants (`share_applied=1 gain_applied=1`), gained share bound on frames 0, 1, 2 (2 draws: receiver + cutout pair), 5 and not on the toggled-off frames 3-4 (F4 pressed at the frame boundary before 3 and 5, `hull_emission_gain_toggle ... lightmap_requested=1 lightmap_gain=4`), **6/6 TAA frames exact**, 4 histories, 1 Reset, 5.6 s. Not covered: native Windows, the game's light-map texel content. |

**Run 41 C (run128, 2026-09-18).** `--hull-lightmap-gain 4` with `--hull-emitters --hull-emission-gain 4` at
the run122 station on the run41 candidate. User verdict: hull lights work, "very very good", 4 becomes the
launcher default. Log: light-map gained draws per frame p50 171 / p95 952 / max 1,035 over 8,177 frames,
981–983 per frame in the gain-on burst (frames 3330–3337); 26 hull programs attempted the gain, 23 bound
the gained plain variant and the same 23 the gained share variant, 3 refused (untouched set); guide lights
1 admitted draw per frame (one program). 44 F4 toggles, gain 4 throughout; 0 DEVICELOST. The gain-on /
gain-off readback pair is confounded by camera motion between and within the bursts (camera_state
differs), so no pixel-level isolation; acceptance is the user's verdict. Follow-up accepted by the user:
guide lights move to the effects key and take `--emission-source-gain` (2), hull light maps stay at 4.

## Run 270 (Run 72 A, 2026-09-23): bolt visibility in third person, open

The user reports the M6's projectiles ("small white balls") hard to see in third person. Evidence
(`verification/results/run270-defaults/`, measured unless marked): the bolt draws are the bullet pair VS 5e484a06 /
PS ec1f5c4a, two draws per frame; the screen-emission-additive route admits them in third person (2 admitted, 0
refused in the two bolt frames; across the flight 3,272 admitted / 1,680 refused, the sampled reason `state`; in first
person one of the two draws is refused every frame). In the third-person burst the pair was drawn in 2 of 8 frames
with 48 primitives, against 8 of 8 frames with 144–168 primitives in first person. Exposure sits at +1.30 EV in
third person (−0.29 in first), so the tonemapper is not the limit; bloom_source_clamp 1.0 caps their bloom input
to a hull highlight's. The readbacks could not isolate the bolt pixels (a bolt-free neighbour frame had as many
candidate pixels). Most likely limit (inferred): fewer/shorter bolt primitives on screen at the chase distance,
not the emission route. Next: Run 72 B holds fire in third person for a burst, once with `--cull-small-parts 0`
to rule out the small-part cull, and a per-draw bolt rectangle/luminance diagnostic if that does not settle it.

### Run 271 (Run 72 B): with the cull off, bolts are 1–2 px dots

With `--cull-small-parts 0` and fire held: the bullet pair (ec1f5c4a, ps_1_1 / vs_1_1) is drawn and admitted in 8 of 8
frames, 792–840 primitives per draw (about 33–35 bolts on screen, 792–840 primitives at 24 each), exposure 1.10 EV; the bolts reach the screen as
components of median area 1–2 px and at most 4 px extent (measured, `run271-music-keep/bolt_center_out.txt`).
With the cull off the emission route does not limit them; the hull widening rewrites SM3 light-map fetches and cannot
add coverage. **Correction (Run 75 B, run279, 2026-09-23):** this run did not rule the cull out for a launch
that has it on. With `--cull-small-parts 4` the stub culled 30–33 of the 51–54 bullet nodes per chase-view frame
(s = 1–3 against threshold 4) one frame after they left the muzzle, and 0–3 were drawn; Run 271's 33–35 drawn bolts
were the cull-off count (`docs/verification/bolt-footprint.md`, Run 75 B). Projectile nodes are now exempt by
default (`--cull-small-parts-projectiles`, `docs/reverse-engineering/lod-selection.md` "Projectile nodes"). Plausible fix (inferred): a minimum on-screen footprint for the bullet draws in the additive route
(design note pending); a per-effect gain would only brighten a 1 px dot under the bloom clamp.
