# Bolt footprint ledger

Feature: `docs/architecture/bolt-footprint.md` (option A′, Implementation section). One row per verification outcome; append.

| Date | Change | Commands | Result |
| --- | --- | --- | --- |
| 2026-09-23 | Option A′ implemented: `bolt_footprint_core.h` (period rule, camera-plane expansion), the additive-route hook with the proxy-owned dynamic buffer, `X3M_BOLT_FOOTPRINT` gate, loader scan enable, widened Unlock scan (UV/colour words kept), `--bolt-footprint` (default `3,8` on modded launches). | `sh verification/results/bolt-footprint/acceptance.sh` (host modules `test_bolt_footprint` + `test_screen_emission_live`, scratch build of `d3d9`, `check_no_x87.py`); also run: `test_screen_emission_additive_transform`, `test_fade_region`, `test_motion_wrap_states`, `test_linear_emission_source_gain`, `test_comparison_hotkeys`, `test_music_keep` | Host: 36 tests OK (17 bolt-footprint: period rule 15 cases incl. remapped/short/odd/past-cap refusals, regimes below/between/above G and behind the camera, gate continuity, six ≥ 2G bolts untouched byte for byte, 120 random instances over 3 scenes against the double-precision oracle with clip z/w within 2e-4, rows refusals, widened scan 40/40; launcher default/opt-out/custom/vanilla/prerequisite/malformed cases) + 19 screen-emission-live; the other six modules 39 + 22 tests OK. Scratch build `build-bf-90262` d3d9: 0 warnings, DLL sha256 f8970d18…; x87 audit PASS, 668 reachable, 0 violations (`verification/results/bolt-footprint/acceptance_out.txt`). No Wine, no launch; in-game `bolt_footprint` lines, `bolt_center.py` on a third-person burst and the first-person control (note §4 items 3–4) are open. |
| 2026-09-23 | Review round 1 + 2 fixes: bullet-producer gate (`admitted_vertex_shader`) before any counter; substitute buffer created once at 147 456 B; QPC only with telemetry or on the window frame (`timed_draws=`); a 31+-char `X3M_BOLT_FOOTPRINT` logs the refused mode line; loader `source=` field; stock-body UV sub-period check (3 of 20 bodies: PlasmaBeam/Repair thirds share the centroid, Repeat 234 = 3 × 78 collinear) and the promotion of a non-stock period to the stock body it divides (78 → 234); tests: the 8e5-coordinate scene with a derived fp32 tolerance (dp4 term + world quantum; observed residue held to 0.05 + 0.5 px × 200 m / w) and an absolute depth bound (0.05 m or two world quanta); note's cost statements corrected; acceptance script tees its real output including the six extra modules. | `sh verification/results/bolt-footprint/acceptance.sh` (tee'd to `acceptance_out.txt`); `python3 verification/results/bolt-footprint/bullet_uv_periods.py` (`bullet_uv_periods_out.txt`) | `acceptance_out.txt` (tee'd): acceptance modules 37 tests OK (`test_bolt_footprint` 18 + `test_screen_emission_live` 19); the six extra modules 61 tests OK; scratch build `d3d9` exit 0, 0 warnings; x87 PASS, 668 reachable, 0 violations; DLL sha256 3567ca12…; scratch dir removed by the script. Bodies: 20 read, 3 with a sub-period, max segment centroid offset 0.642 (Repeat), PlasmaBeam ≤ 0.062, Repair ≤ 0.002 of the body half-extent. |
