# Bolt footprint ledger

Feature: `docs/architecture/bolt-footprint.md` (option A′, Implementation section). One row per verification outcome; append.

| Date | Change | Commands | Result |
| --- | --- | --- | --- |
| 2026-09-23 | Option A′ implemented: `bolt_footprint_core.h` (period rule, camera-plane expansion), the additive-route hook with the proxy-owned dynamic buffer, `X3M_BOLT_FOOTPRINT` gate, loader scan enable, widened Unlock scan (UV/colour words kept), `--bolt-footprint` (default `3,8` on modded launches). | `sh verification/results/bolt-footprint/acceptance.sh` (host modules `test_bolt_footprint` + `test_screen_emission_live`, scratch build of `d3d9`, `check_no_x87.py`); also run: `test_screen_emission_additive_transform`, `test_fade_region`, `test_motion_wrap_states`, `test_linear_emission_source_gain`, `test_comparison_hotkeys`, `test_music_keep` | Host: 36 tests OK (17 bolt-footprint: period rule 15 cases incl. remapped/short/odd/past-cap refusals, regimes below/between/above G and behind the camera, gate continuity, six ≥ 2G bolts untouched byte for byte, 120 random instances over 3 scenes against the double-precision oracle with clip z/w within 2e-4, rows refusals, widened scan 40/40; launcher default/opt-out/custom/vanilla/prerequisite/malformed cases) + 19 screen-emission-live; the other six modules 39 + 22 tests OK. Scratch build `build-bf-90262` d3d9: 0 warnings, DLL sha256 f8970d18…; x87 audit PASS, 668 reachable, 0 violations (`verification/results/bolt-footprint/acceptance_out.txt`). No Wine, no launch; in-game `bolt_footprint` lines, `bolt_center.py` on a third-person burst and the first-person control (note §4 items 3–4) are open. |
| 2026-09-23 | Review round 1 + 2 fixes: bullet-producer gate (`admitted_vertex_shader`) before any counter; substitute buffer created once at 147 456 B; QPC only with telemetry or on the window frame (`timed_draws=`); a 31+-char `X3M_BOLT_FOOTPRINT` logs the refused mode line; loader `source=` field; stock-body UV sub-period check (3 of 20 bodies: PlasmaBeam/Repair thirds share the centroid, Repeat 234 = 3 × 78 collinear) and the promotion of a non-stock period to the stock body it divides (78 → 234); tests: the 8e5-coordinate scene with a derived fp32 tolerance (dp4 term + world quantum; observed residue held to 0.05 + 0.5 px × 200 m / w) and an absolute depth bound (0.05 m or two world quanta); note's cost statements corrected; acceptance script tees its real output including the six extra modules. | `sh verification/results/bolt-footprint/acceptance.sh` (tee'd to `acceptance_out.txt`); `python3 verification/results/bolt-footprint/bullet_uv_periods.py` (`bullet_uv_periods_out.txt`) | `acceptance_out.txt` (tee'd): acceptance modules 37 tests OK (`test_bolt_footprint` 18 + `test_screen_emission_live` 19); the six extra modules 61 tests OK; scratch build `d3d9` exit 0, 0 warnings; x87 PASS, 668 reachable, 0 violations; DLL sha256 3567ca12…; scratch dir removed by the script. Bodies: 20 read, 3 with a sub-period, max segment centroid offset 0.642 (Repeat), PlasmaBeam ≤ 0.062, Repair ≤ 0.002 of the body half-extent. |
| 2026-09-23 | Run 73 B: visibility rule (full width below W = 3 px widened, full length below L = 12 px lengthened along the projected world axis from the triangles' area-weighted second moment; without a world axis the area-weighted 2D shape axis when elongated ≥ 1.5, else a stable W × W disc on the screen axes; a scale moving no vertex by 0.25 px is dropped), frame-stamped chase-view gate (`chase_camera::pose_applied_since(mark)`, mark = pose write count at the last Present), `bolt_footprint_hist` per view (chase: plan axes; gated: bounding box only, no plan), `screen_emission_additive_refused_window` per 300 frames, `--bolt-footprint W[,L]` default `3,12`; run273 triage of the burst, the draw 9/90 pair and the post-new-game drop (architecture note, section Run 73 B); review F1–F7 folded in. | `sh verification/results/bolt-footprint/acceptance.sh` (tee'd to `acceptance_out.txt`; no set -e, the build exit and the scratch removal are always printed; the DLL hash is build-directory dependent, provenance only); run273 scripts in `verification/results/run273-fog-bolts/`: `bolt_components.py`, `bolt_views.py`, `bullet_pair_state_diff.py`, `fire_segments.py` (outputs beside them); modules reading the changed sources: `test_chase_camera`, `test_game_phase_sites`, `test_linear_cutout_contract`, `test_motion_cut_defaults`, `test_taa_image_defaults`, `test_object_bounds_log`, `test_hull_emissive_widening`, `test_hull_lightmap_gain`, `test_volumetric_fog`, `test_original_fill` | Acceptance: 41 tests OK (`test_bolt_footprint` 22 + `test_screen_emission_live` 19); the script's six further modules 61 OK; the ten modules listed 151 OK; scratch d3d9 build and full-target build 0 warnings; x87 PASS, 673 reachable, 0 violations; `build_exit=0`, `scratch_removed=1`. First person is protected by the view gate, not by bolt size (the old rule expanded 1,181 of 7,368 first-person instances); the gate is checked by source asserts (gate before `plan_draw`, `ensure_bolt_buffer`, Lock and SetStreamSource; publish/mark wiring) and by the core test of the frame-stamped predicate (written, stale next frame, first-person visit, uninstalled). Core tests also cover the end-on streak on the line to its vanishing point, the stable disc (6 jittered frames, identical scales, W × W), no write that moves nothing, continuity at W and L, first-person sizes untouched byte for byte, both histograms, 3 random scenes and the 8e5 scene with clip z/w unchanged, launcher W,L cases. run273 (measured): burst bolts 2 per bullet frame, 3-4 x 3-5 px, area 9-14 px, peak 2.8-3.4 (after the old expansion); draw 9 differs from draw 90 only by SEPARATEALPHABLENDENABLE 1 (alpha ZERO/ZERO/ADD) and ALPHATESTENABLE 0, hence `reason=state`; after the new game 0 admitted / 0 refused / 0 chase fire events: no bullet draw reached the route, no proxy state carried over. No Wine, no launch; the in-game histogram, the streak look and first-person parity are open (next flight). |
| 2026-09-23 | Run 75 B (run279, 1920×1080, `--cull-small-parts 4`, chase view, fire held, F8 burst 6136–6143): why bolts "immediately disappear". Bullet nodes (body `v\00517`, model `0x205`, radius 784, 58,634 units of travel per frame) are 51–54 per frame; per frame 30–33 `culled_small` by the small-part stub (s = 1–3 against threshold 4), 18–20 `culled_min` by the engine's own measure, 0–3 kept (d ≤ 124k, s 4–6); the kept count equals the bullet draw's instance count in 6 of 8 frames; Run 271 with the cull off drew 33–35. A bolt leaves the muzzle and is culled one frame later, so this rule only ever saw the 0–3 survivors. Fix: projectile nodes (the engine's class-0 marker `+0x130 & 0x20000000`, `lod-selection.md` "Projectile nodes") are exempt from the cull by default (`--cull-small-parts-projectiles on`); expected about 31 more bullet instances (~750 primitives) per firing frame. Not measured in first person. | `verification/results/run279-bolts/bolt_census.py` (`bolt_census.txt`), `bolt_components.py`, `projectile_flag.py` (`projectile_flag.txt`); session `/tmp/x3-bottleX3-run279` | measured counts as stated; the exemption itself is not yet flown (next flight, inferred from these counts: `cull_small_parts_frame … exempt_bullet=` about 48–53 while firing (the 30–33 plus the 18–20 the engine then culls itself, all below the threshold), `cull_census_frame … culled_small_exempt_bullet=` the same, no `culled_small` row with body `v\00517`, about 33–36 kept bullet rows) |

| 2026-09-24 | Run 76 B (run282): user report "bolts are fixed now"; `culled_small_exempt_bullet=54` per frame while firing (measured), `bolt_footprint_hist` chase windows carry up to 18,084 instances per 300 frames. Accepted. |
