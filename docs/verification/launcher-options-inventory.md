# Launcher options and proxy environment inventory (2026-09-25)

Read-only survey at main `a505d758`. Sources: `tools/manage.py` (the `launch` parser, lines 583-849, and the
environment resolution, lines 1666-2160), the `GetEnvironmentVariableW(L"X3M_...")` reads under `src/proxy/` and
`src/ownership/`, `docs/goals.md`, `docs/status.md`, the cleanup inventory
(`docs/architecture/cleanup-inventory-2026-09-22.md`), the feature ledgers under `docs/verification/` and the
Run 84 A stand command in `docs/archive/user-runs-completed.md:2409`. Every line reference is `file:line` at
`a505d758`. "Measured" figures are grep counts; the classification is read from the help text, the resolution code
and the ledgers. Where the effect of an option is not clear from those, the row says "unclear" and names the read
site.

Updated 2026-09-25 after the stand-command promotion ("Defaults promoted" below): the tables and the count summary
describe the launcher after it; line references into `tools/manage.py` in the older rows still point at `a505d758`.

## Count summary

| Figure | Value | How |
|---|---|---|
| Options registered on the `launch` parser | 252 since 2026-09-25 (248 `add_argument('--` calls in the `launch` parser plus the 4 retired names registered by a loop; 24 of them are the `--no-<name>` opt-outs added with the promotion); before: 228 (224 calls in `tools/manage.py:583-849` plus the 4 retired names, `:676-677`); includes 3 shared with other actions (`--game-dir`, `--bottle`, `--dll-source`, `:583-585`) and 7 suppressed refusal stubs | measured (grep) |
| Options the launcher turns on or fills in by itself on a modded launch (section 1) | 92 options since 2026-09-25 (58 before, plus 29 promoted from the stand command that were not section-1 rows, plus the three capture values plus `--music-keep` and `--shadow-alpha-casters`), in 68 table rows; still 10 carry a `_DEFAULT` marker (`X3M_WINDOW_MONITOR_RECT_DEFAULT`, `X3M_ORIGINAL_FILL_DEFAULT`, `X3M_LOD_OCCLUSION_DEFAULT`, `X3M_TAA_BOX_RESOLUTION_DEFAULT`, `X3M_TAA_FAR_GATE_DEFAULT`, `X3M_TAA_FAR_CLIP_DEFAULT`, `X3M_TAA_THIN_VOTE_DEFAULT`, `X3M_FADE_RT2_OWNER_DEFAULT`, `X3M_TAA_THIN_REGION_SOURCE_DEFAULT`, `X3M_SUN_OCCLUSION_DEFAULT`) | counted from section 1 |
| Options in the user's stand command that are not launcher defaults (section 2) | 11 options, 14 argv tokens, plus the `X3M_MOTION_FRAME_LOG=1` prefix: the telemetry/debug options (the three capture options were promoted too, 2026-09-25). Before 2026-09-25: 52 options, 77 argv tokens, 7 of them redundant with a default already in effect | counted from the Run 84 A command |
| Opt-in, accepted or diagnostic, still useful (section 3) | 94 (96 before; `--music-keep` and `--shadow-alpha-casters` moved to section 1) | counted from section 3 |
| Obsolete, superseded or failed (section 4) | 36 rows (7 refusal stubs, 29 live options) | counted from section 4 |
| `X3M_*` variables the DLL reads that `manage.py` never sets (section 5) | 20 named plus the 27 `X3M_FOG_LOOK_<NAME>` and 6 `X3M_FOG_MOTES_<NAME>` tunables (of which `X3M_FOG_MOTES_MAX_PX` is set by the launcher) and the `X3M_FIXTURE_*` fault switches | measured (set difference of the grep lists) |
| Variables `manage.py` names that no DLL code reads | 8: `X3M_BOTTLE`, `X3M_SOURCE_COMMIT`, `X3M_VOICE_DECODER_REPO`, `X3M_FOG_FAMILIES` (launcher-side), `X3M_TAA_CURRENT_FILTER`, `X3M_TAA_LINE_FILTER`, `X3M_TAA_THIN_CLIP`, `X3M_TAA_ADAPTIVE_WEIGHT`, `X3M_TAA_SENTINEL_STABILISER_DEFAULT` (only popped, `tools/manage.py:1770-1772`) | measured |

Column key for the tables: **default state** is one of `launcher default` (the launcher sends it on every modded
launch, or derives it from other options), `DLL default` (on without any variable), `stand only` (the user passes it
every time; not a launcher default), `opt-in`, `opt-out via --no-...` or an explicit off value. **acceptance** cites
the `docs/goals.md` row or the ledger line with its date.

## 1. Defaults

Options the launcher turns on by itself on a modded launch (with or without a `_DEFAULT` marker), and DLL behaviours
that are on without any variable. A "modded launch" is `launch` without `--vanilla`; `--vanilla`
(`tools/manage.py:596`) loads the builtin d3d9 (`d3d9=b`, `:2149`) and the launcher drops or refuses the proxy
options (per-option notes below).

| Option (env variable) | What it does | Default state | Acceptance | Notes |
|---|---|---|---|---|
| `--window-monitor-rect on|off` (`X3M_WINDOW_MONITOR_RECT`, marker `X3M_WINDOW_MONITOR_RECT_DEFAULT`) | Moves the borderless game window from the work-area origin (under the macOS menu bar) to the monitor rectangle at CreateDevice/Reset | launcher default on (`tools/manage.py:603-611`, `:1692-1694`); DLL default off when unset (`src/proxy/window_mode.cpp:12-14`); `--no-window-monitor-rect` = off (`:612`) | Run 83 A: "window moved 0,31 -> 0,0 (menu bar gone, user)" (`docs/archive/user-runs-completed.md`, row 83 A/B, 2026-09-25) | Not sent under `--vanilla`; explicit `on` refused there (`:604`). Goal 15 (macOS menu bar) still says "Planned" in `docs/goals.md:26`; the goal row is stale against this default |
| `--original-fill K` (`X3M_ORIGINAL_FILL`, marker `X3M_ORIGINAL_FILL_DEFAULT`) | Linear-light fill inside the 108 original hull pixel programs, tinted by the sector sun | launcher default 0.01 on every modded `--hdr` launch (`:136`, `:780`, `:1455-1458`); DLL default 0 = off (`src/proxy/capture.cpp:3321-3331`); explicit 0 is the opt-out | User decision 2026-09-25, accepted in flight (`tools/manage.py:1451-1454`; Run 84 A row `original_fill_mode default=1`) | Requires `--hdr`; excludes `--linear-materials` (`:1447`) |
| `--lod-occlusion off|record0|all` (`X3M_LOD_OCCLUSION`, marker `X3M_LOD_OCCLUSION_DEFAULT`) | Bypasses the engine gate so merged-LOD coarse records keep the station's occlusion map | launcher default `all` (`:821`, `:1989-1990`); DLL default `record0` = engine behaviour (`src/proxy/lod_occlusion.cpp:67-76`) | User decision 2026-09-24 after Run 80 A run305 ("made the default `all`", `docs/verification/user-runs.md:30`) | Side effect documented: vanilla lower records may show misplaced occlusion (`:821`). Refused under `--vanilla` |
| `--taa-box-resolution full|half` (`X3M_TAA_BOX_RESOLUTION`, marker `X3M_TAA_BOX_RESOLUTION_DEFAULT`) | Half-resolution separable min/max box of the thin region's camera gate | launcher default `half` with `--taa` (`:695`, `:1055-1058`); DLL default `full` (`src/proxy/capture.cpp:3675-3680`) | Run 81 A run310 accepted, "default from Run 82" (`user-runs-completed.md` row 81 A, 2026-09-24) | `full` is the explicit opt-out |
| `--taa-far-gate camera|screen` (`X3M_TAA_FAR_GATE`, marker `X3M_TAA_FAR_GATE_DEFAULT`) | Motion gate of the far stabiliser weight: camera-relative speed instead of screen speed | launcher default `camera` with `--taa` (`:693`, `:1066-1069`); DLL default camera (`capture.cpp:3681-3687`) | Run 84 A row `far_gate=camera default=1` (2026-09-25); Run 84 A launch 2 A/B decided the default (`user-runs-completed.md:2404`) | `screen` = the Run 83 behaviour, kept as A/B |
| `--taa-far-clip 7x7|3x3` (`X3M_TAA_FAR_CLIP`, marker `X3M_TAA_FAR_CLIP_DEFAULT`) | History clip of far pixels outside the thin region against the 7x7 box | launcher default `7x7` with `--taa` (`:694`, `:1077-1080`); DLL default 7x7 (`capture.cpp:3688-3694`) | Run 86 A run333: plants sparkles rest 120 -> 1, accepted (`docs/status.md:19`, 2026-09-25) | `3x3` = the clip before 2026-09-25 |
| `--taa-thin-vote on|off` (`X3M_TAA_THIN_VOTE`, marker `X3M_TAA_THIN_VOTE_DEFAULT`) | Draw-time triangle-height vote that flags thin geometry into the TAA thin region | launcher default `on` whenever `--taa --motion-output --ownership --hdr --sun-shadow-lane` are all given, else "default on not sent" line (`:700`, `:1100-1112`); DLL default off (`capture.cpp:3699-3705`) | Run 80 A run306 "accepted as defaults" (`docs/verification/user-runs.md:30`, 2026-09-24) | Explicit `on` without the prerequisites is refused (`:1095`) |
| `--fade-rt2-owner on|off` (`X3M_FADE_RT2_OWNER`, marker `X3M_FADE_RT2_OWNER_DEFAULT`) | Fade-band draws write their depth into RT2 (and widened pair identity under original shading) | launcher default `on` with `--taa --motion-output --hdr` (`:699`, `:1100-1112`); DLL default off (`capture.cpp:3714-3720`) | Run 80 A run307 accepted as default (`user-runs.md:30`, 2026-09-24) | |
| `--taa-thin-region-source both|screen|vote` (`X3M_TAA_THIN_REGION_SOURCE`, marker `..._DEFAULT`) | What feeds the thin-region flag; `vote` skips the screen-space fragmented-depth search | launcher default `vote` when the thin vote and the thin region are on (`:701`, `:1130-1134`); DLL default `both` (`capture.cpp:3706-3712`) | Mask fold 2026-09-25: "Run 82 A showed no visible difference between both and vote" (`:701`; `docs/verification/temporal-resolve.md:3079`) | `screen` refused with the camera gate (`:1123-1126`); see section 4 |
| `--sun-occlusion` (`X3M_SUN_OCCLUSION`, marker `X3M_SUN_OCCLUSION_DEFAULT`; `X3M_SUN_OCCLUSION_CORE_F` sent as 1) | Partial sun occlusion: flare fades with the visible fraction of the disc instead of popping at half cover; core dimming | launcher default on every modded `--motion-output` launch without `--submit-phases` (`:835`, `:1579-1582`, `:2051-2055`); DLL: absent = nothing patched (`src/proxy/sun_occlusion.cpp:239-241`) | Goal 13 reopened, flown Run 83 "works", default the same day (`docs/goals.md:24`, 2026-09-25) | `--no-sun-occlusion` opts out (`:836`); refused with `--submit-phases` (`:1572`) |
| `--taa-far-stabiliser W[,A[,F0,F1[,LO,HI]]]` (`X3M_TAA_FAR_STABILISER`) | Far-gated history weight against shimmer of distant sub-pixel detail | launcher default `0.985` (weight only, ramp 60/68) with `--taa` (`:63`, `:678`, `:955-960`, `:970-978`); DLL default off (`capture.cpp:3088-3091`); `off`/`0` opts out | Run 81 default (Run 80 A stand), ramp 60/68 from Run 85/86 (`:975`; `docs/status.md:19`) | No marker: "the DLL row carries the value" (`:678`) |
| `--taa-thin-region W[,RELAX[,LO,HI]]` (`X3M_TAA_THIN_REGION`) | Thin-region stabiliser against lattice/strut jitter shimmer | launcher default `0.97` with `--taa` (`:64`, `:679`, `:955-960`); DLL default off (`capture.cpp:3100`) | Run 81 default; Run 82 B run320 with the region off "a little shimmer under pan (rejected, the hold stays)" (`user-runs-completed.md` row 82 B) | One speed gate shared with the far stabiliser (`:986-998`) |
| `--taa-thin-region-gate screen|camera` (`X3M_TAA_THIN_REGION_GATE`) | Gate mode of the thin region; camera runs the region hold (A') | launcher default `camera` when the thin region is on (`:681`, `:1001-1005`); DLL default camera when the region is on (`capture.cpp:3119-3122`) | Run 59 accepted (`:1003`); A' accepted Run 79 A (`user-runs.md:31`, 2026-09-24) | `screen` is the pre-Run59 opt-out |
| `--taa-thin-region-emissive E` (`X3M_TAA_THIN_REGION_EMISSIVE`) | Emissive local-peak vote into the thin region | launcher default `1` with `--taa --hdr` and the region on (`:62`, `:680`, `:1007-1013`); DLL default off (`capture.cpp:3111-3113`) | run236/run237 accepted 2026-09-22 (`:1008-1010`) | `0` opts out |
| `--taa-unmatched-static off|node|all` (`X3M_TAA_UNMATCHED_STATIC`) | A routed draw with a new motion key reprojects as static for one frame | launcher default `node` with `--taa` (`:688`, `:1036-1039`); DLL default node with TAA (`capture.cpp:3647-3652`) | run212 accepted (`:1036-1038`) | `off` = pre-run212 |
| `--taa-sky-history loose|strict` (`X3M_TAA_SKY_HISTORY`) | Sky pixels accept sentinel history only (SETA smear fix) | launcher default `strict` with `--taa` (`:689`, `:1042-1045`); DLL default strict with TAA (`capture.cpp:3653`) | Run 68 A accepted 2026-09-23 (`user-runs-completed.md` row 68 A/B/C) | `loose` = pre-SETA |
| `--taa-sky-history-exit-px` (`X3M_TAA_SKY_HISTORY_EXIT_PX`) | Exit reset of the strict sky history | launcher default 0.25 under strict with an age program, else explicit 0 (`:40`, `:691`, `:1148-1152`); DLL default .25 under strict (`capture.cpp:3669`) | Run 68 A accepted (`:1149`) | Always forwarded with `--taa` |
| `--taa-motion-weight F[,V0,V1]` (`X3M_TAA_MOTION_WEIGHT`) | Caps the history weight of fast-parallax pixels (SETA hull blur) | launcher default `0.7,2,8` with an age program and not `--taa-sentinel 1`, else explicit 0 (`:41`, `:702`, `:1178-1183`); DLL fallback 0.7,2,8 per the launcher comment (`:1180`, inferred; the read at `capture.cpp:3726` keeps "off" on a malformed value) | Run 70 A run262/run263 accepted 2026-09-23 (`user-runs-completed.md` row 70) | |
| `--taa-mip-bias`, `--taa-sharpen` (`X3M_TAA_MIP_BIAS`, `X3M_TAA_SHARPEN`) | Sampler mip bias under jitter; post-resolve sharpen | launcher defaults -0.5 / 0.75 with `--taa` (`:38-39`, `:673-674`, `:1742-1747`); DLL defaults identical with TAA (`capture.cpp:3081`, `:3086`) | Production defaults (help text); no ledger row found | 0 disables either |
| `--taa-sentinel auto|1|2` (`X3M_TAA_SENTINEL`) | Depth-sentinel policy of the resolve | launcher default `auto` (`:687`, `:1773`); DLL default Auto (`capture.cpp:3642-3645`) | `docs/architecture/temporal-integration.md:582` | `1` makes the motion weight inert (`:1183`) |
| `--scene-hook [on|off]` (`X3M_SCENE_HOOK`) | Engine scene-end hook at the compositing callsite 0x004721b1 | launcher sends 1 with `--motion-output`, 0 otherwise (`:705`, `:1783`); DLL default on with the route (`src/proxy/scene_hook.cpp:120-126`) | review 26 / iteration 10 214/214 agreement (`:705`) | `off` keeps the copy/selector boundary |
| `--motion-rt-mode perdraw|lazy` (`X3M_MOTION_RT_MODE`) | RT1/RT2 binding policy of the route | launcher default `lazy` with `--motion-output` (`:803`, `:1191-1193`); DLL default perdraw when unset (`capture.cpp:3634`) | "lazy RT remains accepted" (`docs/status.md:103`) | `perdraw` kept for A/B |
| `--state-shadow auto|on|off` (`X3M_STATE_SHADOW`) | Render-state configuration of the route (hybrid unhook) | launcher `auto` leaves the variable unset (`:802`, `:1950-1952`); DLL auto (`capture.cpp:3635-3639`) | `docs/architecture/state-call-fast-path.md:359` (default since 2026-09-16) | |
| `--hdr-dither on|off` (`X3M_HDR_DITHER`) | Static +-0.5 code display dither of the FP16-to-8-bit write | launcher default on with `--hdr` (`:786`, `:1933`); DLL default off (`capture.cpp:3157-3160`) | Run 78 A run295-298 "dither accepted (rings gone)" (`user-runs-completed.md` row 78 A, 2026-09-24) | |
| `--hdr-exposure auto`, `--hdr-ev-max 1.3`, `--hdr-ev-min -3`, `--hdr-ev 0`, `--hdr-ev-deadband 0.25`, `--hdr-edge-weight 0.35`, `--hdr-key-pull 0.25`, `--hdr-white-target 0.9`, `--hdr-meter-bg 1/512`, `--hdr-clamp 0`, `--hdr-decode gamma2.2`, `--hdr-look none` (`X3M_HDR_*`) | Auto exposure capped at +1.3 EV and the meter rules | launcher always forwards these values (`:787-801`, `:1931-1945`); DLL defaults Auto / 1.3 (`capture.cpp:3140-3145`) | Goal 5 Accepted (in game): "+1.3 EV ceiling is the launcher default" (`docs/goals.md:16`) | Changing any of them requires `--hdr-tonemap` (`:1517`) |
| `--hull-lightmap-gain G` (`X3M_HULL_LIGHTMAP_GAIN`) | Gain on the light-map term of the original hull programs | launcher default 4 with `--hdr` (`:51`, `:781`, `:1909`); DLL default 1 = off (`capture.cpp:3344`) | run128 "very very good", launcher default 4 in the run42 candidate (`docs/goals.md:14`) | Ctrl+Shift+F4 toggles |
| `--light-map-far-fade P0,P1[,G]` (`X3M_LIGHT_MAP_FAR_FADE`) | Fades the light-map gain with distance | launcher default `80,220,1` when the gain is active (`:52`, `:783`, `:1466-1468`); DLL unset = off (`capture.cpp:3358-3360`) | user flight preference 2026-09-20 (`:52`); `docs/status.md:107-108` | `--no-light-map-far-fade` opts out (`:784`) |
| `--hull-emissive-widening K[,B]` (`X3M_HULL_EMISSIVE_WIDENING`) | Widens the light-map fetch by its texel footprint, boosts thin emitters | launcher default 4 when the gain is active (`:57`, `:785`, `:1486-1492`); DLL unset = off (`capture.cpp:3385-3387`) | run236/run237 accepted, 4 chosen (`user-runs-completed.md:2088`, 2026-09-22) | `off` opts out |
| `--media-cue-cache on|off`, `--media-cue-retry-s 30` (`X3M_MEDIA_CUE_CACHE`, `X3M_MEDIA_CUE_RETRY_S`) | Negative cache for the sector selector's cue restart | launcher default on (`:643-644`, `:1706-1707`); DLL needs the value 1 (`src/proxy/media_cue.cpp:283`) | Default-on policy 2026-09-17 (`docs/verification/media-cues.md:271`) | Independent of `--telemetry` |
| `--bolt-footprint [W[,L]]` (`X3M_BOLT_FOOTPRINT`) | Minimum on-screen size of weapon bolts in the chase view | launcher default `3,12` on modded launches (`:134`, `:766`, `:1805-1811`); DLL unset = off (`capture.cpp:3451-3455`) | Run 76 B run282 "bolts fixed, accepted" (`user-runs-completed.md` row 76 B/C/D, 2026-09-24) | Acts only with the additive bullets and `--camera chase`; `0` = off |
| `--chase-fov-compensate on|off` (`X3M_CHASE_FOV_COMPENSATE`) | Scales the chase boom so the ship keeps its screen size under any FOV | launcher sends 1 on every modded launch (`:811`, `:2105-2108`); inert without `--camera chase` | Run 82 A accepted (`user-runs-completed.md` row 82 A, 2026-09-25) | Explicit value requires `--camera chase` |
| `--chase-hud-anchor forward|centre` (`X3M_CHASE_HUD_ANCHOR`) | HUD reticle at the ship-forward vanishing point | launcher default `forward` with `--camera chase` (`:817`, `:2100`); DLL read `src/proxy/chase_lead.cpp:801` | Goal 17 (user request 2026-09-23) (`docs/goals.md:28`) | |
| `--chase-pitch-down-deg 0.5`, `--chase-offset-y 0.50`, `--chase-distance-scale 1.05` (`X3M_CHASE_*`) | Chase camera framing constants | launcher always forwards these three (`:33`, `:2088-2095`); the other chase tunables fall through to the DLL's compiled defaults (`src/proxy/chase_camera.cpp:401-410`) | Goal 17 Accepted (in game); distance 1.05 user decision 2026-09-23 (`:33`) | Only meaningful with `--camera chase` |
| `--fov N|game` (`X3M_FOV`) | Field of view in X4-style units, default 90 | launcher default `90` on every modded launch (`:101`, `:823`, `:2002`); DLL unset = nothing patched (`src/proxy/fov.cpp:251-254`) | Run 82 A "FOV menu ... accepted by the user" (`user-runs-completed.md` row 82 A, 2026-09-25) | `game` = vanilla model; refused under `--vanilla` |
| `--terran-station-lod size|distance` (`X3M_TERRAN_STATION_LOD`) | Terran stations select LOD by screen size (DLL patch of the race-18 root bit) | launcher default `size` (`:820`, `:1981`); DLL default size = patched even when unset (`src/proxy/terran_station_lod.cpp:66-72`) | Run 79 A "Terran patch works" (`user-runs.md:31`, 2026-09-24); user chose the patch | `distance` never flown (`docs/verification/cull-census.md:72`) |
| `--sun-flare-fix on|off` (`X3M_SUN_FLARE_FIX`) | Saturates the lens collector's overflowing bound so the sun flare survives near the view centre on wide displays | launcher default `on` (`:822`, `:1996`); DLL unset = off, engine bytes (`src/proxy/sun_flare_fix.cpp:98-104`) | Run 82 A "sun at 100 ... accepted" (row 82 A, 2026-09-25) | |
| `--cull-small-parts PX`, `--cull-small-parts-scope all`, `--cull-small-parts-projectiles on` (`X3M_CULL_SMALL_PARTS_PX/_SCOPE/_PROJECTILES`) | Culls mesh nodes under PX projected pixels; projectiles exempt | launcher default (2026-09-25): 4 px (2 px 2026-09-19 to 2026-09-25) / all / on (`:78-83`, `:846-848`, `:2076-2083`); DLL unset = off, scope all, projectiles on (`src/proxy/cull_small_parts.cpp:125-133`) | Default 2026-09-19 after Run 43 B (`docs/verification/cull-small-parts.md:22`); projectile exemption Run 76 B accepted | `--cull-small-parts 0` = off |
| `--collide-box-cull`, `--collide-sat-sse2`, `--collide-memo` (`X3M_COLLIDE_BOX_CULL`, `X3M_COLLIDE_SAT_SSE2`, `X3M_COLLIDE_MEMO`) | Engine collision fast paths: integer box early-out, SSE2 SAT, no-contact memo | launcher defaults on modded launches (`:828`, `:831`, `:833`, `:2008-2030`); DLL absent = nothing patched (`src/proxy/collide_box_cull.cpp:137-138` and siblings) | Goal 14: "collision fast paths ... are defaults and the box cull becomes one on 2026-09-23" (`docs/goals.md:25`); Run 72 A "collisions accepted" | `--no-collide-*` opt out; not forwarded under `--vanilla` unless given |
| `--pause-key-only` (`X3M_PAUSE_KEY_ONLY`, `X3M_PAUSE_KEY`) | Only the Pause key or a click ends the flight pause | launcher default on (`:842`, `:2034`); DLL absent = nothing patched (`src/proxy/pause_key_only.cpp:86-87`) | Run 72 A run270 "pause ... accepted" (row 72 A/B, 2026-09-23) | `--no-pause-key-only` opts out; `--pause-key CODE` for a rebound key (`:843`) |
| `--voice-decoder [DIR|none]` (`GST_PLUGIN_PATH_1_0`, `GST_REGISTRY_1_0`, `X3M_VOICE_DMO_FALLBACK`) | Process-local WMA decoder plugin plus the DMO wrapper fallback hook | launcher discovers `<game>/x3m/voice-decoder`, then `tools/voice-decoder/v4`, on every modded launch (`:818`, `:2113-2140`); DLL hook only with the variable (`src/proxy/voice_dmo_fallback.cpp:180`) | Goal 14: speech (process-local WMA decoder v4) accepted (`docs/goals.md:25`); Run 72 A "decoder discovery" | `none` = off |
| `--shadow-sun-poll on|off` (`X3M_SHADOW_SUN_POLL`) | Sun direction from the engine's brightest directional light node | default on with `--shadow-cascades` (`:728`); DLL default true (`src/proxy/sun_light_poll.cpp:37`) | Goal 8 Accepted (in game) (`docs/goals.md:19`) | On with the promoted cascades; the stand passes it explicitly (redundant) |
| `--shadow-cascade-min-footprint P` (`X3M_SHADOW_CASCADE_MIN_FOOTPRINT`) | Per-part minimum light-space footprint of the cascades | launcher default 8 whenever `--shadow-cascades` is given (`:71`, `:732`, `:1302-1307`); DLL default 8 (`capture.cpp:2748-2749`) | Run 67 C run251 footprint 8 accepted as default (row 67, 2026-09-22) | 0 = off |
| `--shadow-cascade-backface-from K|none` (`X3M_SHADOW_CASCADE_BACKFACE_FROM`) | Far cascades replay back faces | DLL default: every cascade whose world texel is >= 8 u (`:726`, `capture.cpp:2734-2740`); launcher forwards only when given | `docs/architecture/shadow-cascades.md:270` implemented 2026-09-18, default on by texel | |
| `--shadow-cascade-drop-order submission|importance` (`X3M_SHADOW_CASCADE_DROP_ORDER`) | What a cascade drops above its cap | launcher default (2026-09-25): `importance` whenever the cascades are on; DLL default submission (`capture.cpp:2728`) | Goal 8 "drop order by importance" (`docs/goals.md:19`) | `--shadow-cascade-drop-order submission` = off |
| `--fog-handover-step`, `--fog-handover-coldfill`, `--fog-handover-prefill`, `--fog-docked` (`X3M_FOG_HANDOVER_STEP/_COLDFILL/_PREFILL`, `X3M_FOG_DOCKED`) | Cold-start hand-over of the stored fog after a sector change; docked view keeps the fog | launcher default on, always explicit (`:753-760`, `:1867-1870`); DLL `fog_default_on` = on unless 0 (`capture.cpp:3529-3532`) | Run 75 A run278 hand-over accepted; Run 77 B run288 docked load closed (rows 75 A, 77 B, 2026-09-23/24) | `--no-...` forms opt out; stored range only |
| `--fog-dust-motes N[,SIZE[,STREAK]]` (`X3M_FOG_DUST_MOTES`, `X3M_FOG_MOTES_MAX_PX`) | Near-camera dust motes in the stored fog | launcher default `1300,3,128` + `MAX_PX=8` under `--volumetric-fog-range stored` (`:42-43`, `:749`, `:1875-1879`); DLL default under the stored range (`capture.cpp:3598-3610`) | Run 70 B/B2 run264 accepted (row 70, 2026-09-23); goal 12 | `0` opts out; Ctrl+Alt+F11 toggles |
| `--fog-march-scale 2|4` (`X3M_FOG_MARCH_SCALE`) | Quarter-resolution fog march | launcher default 4, always written (`:44`, `:752`, `:1861-1864`); DLL default "4" (`capture.cpp:3581-3583`) | Run 77 C2 accepted scale 4 (`user-runs-completed.md:2196`, 2026-09-24) | Clamped to 2 with `--fog-shadow-pass on` |
| `--fog-far-bins 40|24` (`X3M_FOG_FAR_BINS`) | Far march bins of the stored fog | launcher default 40, always written (`:751`, `:1858`) | Run 76 D: 40 stays (row 76 B/C/D, 2026-09-24) | `24` in section 4 |
| `--volumetric-fog-cards keep`, `--volumetric-fog-range legacy` (`X3M_VOLUMETRIC_FOG_CARDS/_RANGE`) | Fog card policy and field | launcher default (2026-09-25): `replace` / `stored` with `--volumetric-fog`; keep / legacy are the off values | Goal 12: the accepted look is `replace` + `stored`, "still opt-in flags" (`docs/goals.md:23`) | Resolved by the 2026-09-25 promotion |
| `--frame-end-stride 300`, `--capture-start 999999`, `--capture-frames 8`, `--capture-delay 300` (`X3M_FRAME_END_STRIDE`, `X3M_CAPTURE_START/_FRAMES/_DELAY`) | `frame_end` log cadence; the automatic first capture frame and the F8 burst length | launcher default (2026-09-25): start 999999 = no automatic capture, F8 captures 8 frames on demand after 300 frames (before: 120 / 1 / 0, still the values under `--vanilla`); stride 300 unchanged (`:587-594`, `:599`, `:1670-1677`, `:1683`); DLL reads `capture.cpp:3020-3030`, `:3039` | Capture controls; no verdict needed | `--capture-start 120` restores the automatic capture; the stand adds stride 1 (section 2) |
| `--game-phase-threshold-ms 20`, `--gz-buffer-kb 256`, `--profile-interval-us 2000`, `--camera-cut-deg 20`, `--camera-log 300`, `--shadow-replay-size 1024 / -extent 250 / -depth-half 512 / -cap 512`, `--sun-shadow-bias-units 0.53571875 / -clamp-texels 20.97152 / -slope-texels 0.2` | Numeric defaults the launcher always forwards | `tools/manage.py:632`, `:657`, `:664`, `:703-704`, `:661`, `:654`, `:804`, `:715-719`, `:739-741`; env at `:1680`, `:1954`, `:1961`, `:1780-1781`, `:1958`, `:1722`, `:1969`, `:1824-1827`, `:1847-1849` | Tuning values; ledgers per feature | |
| (no option) `X3M_MOTION_CUT_MEDIAN_PX=1e30`, `X3M_MOTION_CUT_MISSING=1` | Global camera-cut heuristics of the resolve disabled by default | launcher `setdefault` (`:1728-1733`), so a shell value survives as the diagnostic opt-in; DLL read `capture.cpp:3061-3062` | Run57 "global heuristic cuts disabled by default" (`docs/status.md:69-73`, 2026-09-21) | No launcher option exists for these two |
| (no option) DLL-only defaults | Fade-band motion arm threshold 500 permille (`X3M_FADE_ROUTE`, `capture.cpp:170`, `:3240`); `motion_frame_log` cadence 60 (`capture.cpp:244`); Halton jitter samples 8 (`:346`); chase `max_dt` 0.10 s (`src/proxy/chase_camera_math.h:168`); exposure key 0.18, adapt taus 0.4/1.2, `meter_min_lit` 0.01 (`src/renderer/exposure.h:35-46`); fog anisotropy 0.3 (`src/renderer/fog_pass_math.h:12`); TAA history taps 5 (`capture.cpp:3670`); sun occlusion `core_f` on (`capture.cpp:3779`); session identity logging at attach (`capture.cpp:3018`) | DLL default | see section 5 for the variables | |
| `--direct` | Skips the launcher and intro with X3 command-line switches (`tools/manage.py:595`) | launcher default (2026-09-25); `--no-direct` = off | convenience | X3 switches `-noabout -skipintro -runinbg` on every modded launch |
| `--camera chase` (`X3M_CAMERA=chase`) | Critically damped chase camera for the external back view (`:804`; `src/proxy/chase_camera.cpp:390`) | launcher default (2026-09-25): `chase` (was `vanilla`) | Goal 17 Accepted (in game) (`docs/goals.md:28`) | `--camera vanilla` = off; the chase framing, FOV compensation and bolt footprint defaults follow it |
| `--chase-view-restore` (`X3M_CHASE_VIEW_RESTORE`) | Restores the chase view after a gate jump or jumpdrive (`:816`, `:2101`) | launcher default (2026-09-25) with `--camera chase` | Goal 17: "accepted and the stand command carries it (launcher default off)" (`docs/goals.md:28`) | `--no-chase-view-restore` = off; `--camera vanilla` drops it |
| `--ownership`, `--object-trace`, `--object-lifetime` (`X3M_OWNERSHIP`, `X3M_OBJECT_TRACE`, `X3M_OBJECT_LIFETIME`) | Ownership wrapper, verified submission identity, render-registry lifetimes: the prerequisites of the TAA history, thin vote, sun lane and shadows (`:648`, `:651-652`, `:668`) | launcher default (2026-09-25); trace and lifetime only with `--ownership` (the trio moves together) | Goal 11 TAA Accepted (in game) depends on them (`docs/goals.md:22`) | `--no-ownership` / `--no-object-trace` / `--no-object-lifetime` = off; each turns `--taa` off (sentinel-only route) |
| `--motion-output`, `--taa` (`X3M_MOTION_OUTPUT`, `X3M_TAA`) | The motion route and the temporal resolve (`:668`, `:670`; `capture.cpp:3055`, `:3066`) | launcher default (2026-09-25); `--taa` with `--motion-output` and the history trio | Goal 11 Accepted (in game) | `--no-motion-output` / `--no-taa` = off; every TAA default of this section follows `--taa` |
| `--hdr`, `--hdr-tonemap` (`X3M_HDR`, `X3M_HDR_TONEMAP=agx`) | FP16 scene and the AgX write-back (`:706-707`) | launcher default (2026-09-25); `--hdr` with `--motion-output`, `--hdr-tonemap` with `--hdr` | Goal 2 Installed, goal 1 Closed with "the FP16 scene ... stay as installed" (`docs/goals.md:12-13`) | `--no-hdr` / `--no-hdr-tonemap` = off |
| `--hdr-bloom`, `--bloom-source-clamp 1.0` (`X3M_HDR_BLOOM`, `X3M_BLOOM_SOURCE_CLAMP`) | Bloom from the FP16 scene; source ceiling (`:788-789`) | launcher default (2026-09-25); bloom with `--hdr-tonemap` and the scene hook, clamp 1.0 with bloom | Goal 4 Installed, clamp 1.0 accepted run 30 (`docs/goals.md:15`) | `--no-hdr-bloom` / `--no-bloom-source-clamp` = off |
| `--crypt-cache`, `--gz-buffer`, `--resource-read fast`, `--dat-handles`, `--mesh-adjacency fast` (`X3M_CRYPT_CACHE`, `X3M_GZ_BUFFER`, `X3M_RESOURCE_READ`, `X3M_DAT_HANDLES`, `X3M_MESH_ADJACENCY`) | Loading fast paths (`:653-662`) | launcher default (2026-09-25) | Goal 14: "Crypto cache, reader and adjacency fast paths ... accepted" (`docs/goals.md:25`) | `--no-crypt-cache`, `--no-gz-buffer`, `--no-dat-handles`, `--resource-read native`, `--mesh-adjacency native` = off. The DLL reads `X3M_MESH_ADJACENCY` only in its telemetry branch (`src/proxy/loading_trace.cpp:1107-1126`), so the adjacency default is inert without `--telemetry`; an explicit `--mesh-adjacency verify|fast` still requires `--telemetry` |
| `--screen-emission-additive 2`, `--screen-emission-additive-alpha 0` (`X3M_SCREEN_EMISSION_ADDITIVE`, `..._ALPHA`) | Additive bullets above 1.0 with bloom alpha 0 (`:765`, `:767`) | launcher default (2026-09-25): 2 with `--motion-output --hdr` and without `--screen-emission`; alpha 0 with the additive bullets | Goal 3: bolts accepted run 26; alpha 0 accepted run 30 (`docs/goals.md:14-15`) | `--no-screen-emission-additive` / `--no-screen-emission-additive-alpha` = off |
| `--emission-source-gain 2` (`X3M_EMISSION_SOURCE_GAIN`) | Gain over the twenty effects pairs (`:772`) | launcher default (2026-09-25): 2 with `--hdr` and without `--linear-emissions` | Goal 3: engines respond, run 30 (`docs/goals.md:14`) | `--emission-source-gain 1` = off |
| `--sun-shadow-lane`, `--shadow-replay-depth`, `--sun-shadow-apply` (`X3M_SUN_SHADOW_LANE`, `X3M_SHADOW_REPLAY_DEPTH`, `X3M_SUN_SHADOW_APPLY`) | Sun-share lane, caster depth replay, scene-end shadow application (`:711-714`) | launcher default (2026-09-25); lane with `--motion-output --taa --hdr`, replay with `--motion-output --ownership`, apply with both | Goal 8 Accepted (in game): "Still opt-in flags in the launcher ... not launcher defaults" (`docs/goals.md:19`) | `--no-sun-shadow-lane` / `--no-shadow-replay-depth` / `--no-sun-shadow-apply` = off; without the replay the cascades, retention, alpha casters and fog stay off |
| `--shadow-cascades 250,1500,7500,37500,150000`, `--shadow-cascade-sizes 2048 x5`, `--shadow-cascade-records 1024,1024,2048,4096,4096`, `--shadow-cascade-drop-order importance`, `--shadow-cascade-adaptive-c0 1.5`, `--shadow-caster-retention` | The accepted five-cascade set, importance drop, own-ship adaptive near cascade, static caster retention (`:720-735`) | launcher default (2026-09-25): the five-cascade set with `--shadow-replay-depth`; records `1024,1024,2048,4096,4096` only with five cascades; sizes `2048,4096,4096,2048,2048` with five cascades (user decision 2026-09-25: 4096 maps for cascades 1 and 2, 15 cm and 73 cm per texel; 176 MiB of maps, +96 MiB against the stand's 2048 x5), 2048 each otherwise | Goal 8: "five cascades (250-150,000 units, 2048^2 each, drop order by importance), caster retention and footprint 8 flies in every stand command and was accepted through runs 65-67" (`docs/goals.md:19`) | `--no-shadow-cascades`, `--shadow-cascade-drop-order submission`, `--no-shadow-cascade-adaptive-c0`, `--no-shadow-caster-retention` = off |
| `--volumetric-fog 0.02`, `--volumetric-fog-cards replace`, `--volumetric-fog-range stored` (`X3M_VOLUMETRIC_FOG`, `_STRENGTH`, `_CARDS`, `_RANGE`) | Stored-range fog replacing the vanilla cards (`:743-745`) | launcher default (2026-09-25) with `--motion-output --taa --hdr --shadow-replay-depth --shadow-cascades`; cards and range follow the fog | Goal 12 Accepted (in game): "Still opt-in flags" (`docs/goals.md:23`) | `--no-volumetric-fog`, `--volumetric-fog-cards keep`, `--volumetric-fog-range legacy` = off |
| `--music-keep` (`X3M_MUSIC_KEEP`) | Keeps sector music playing at its position across alt-tab, save and pause (`:645`) | launcher default (2026-09-25) on every modded launch; not sent under `--vanilla` | Run 73 B run273 "music keep accepted (6 alt-tabs, all skip_all)" (row 73 B/C, 2026-09-23); goal 14 still lists "music restart on alt-tab/save" as open (`docs/goals.md:25`) | `--no-music-keep` = off. The ~1 s DirectSound gap on alt-tab (row 72 B) is the open caveat |
| `--shadow-alpha-casters on|off` (`X3M_SHADOW_ALPHA_CASTERS`) | Alpha-tested routed draws cast shadows with their alpha test (`:718`; `capture.cpp:2684`) | launcher default (2026-09-25): `on` with `--shadow-replay-depth` (the variable is `1`) | Run 81 A run309 "alpha casters clean (refused_pool 0)" (row 81 A, 2026-09-24); no default decision recorded (`docs/verification/directional-shadows.md:3732`) | `--shadow-alpha-casters off` = off |

## 2. Telemetry/debug options of the stand command (still opt-in; the functional rest became launcher defaults on 2026-09-25)

The Run 84 A stand command (`docs/archive/user-runs-completed.md:2409`, unchanged in Run 85/86,
`:2442`, `:2481`) is `env -u CX_DEBUGMSG X3M_FIXTURE_BOTTLE=X3 X3M_MOTION_FRAME_LOG=1 /Users/asvetl/x3-mod/x3run`
followed by 77 argv tokens = 52 options. `x3run` adds only `--bottle X3` and the Wine lock (`x3run:7-8`).
"Fresh user" below means a modded launch with none of these options. Since 2026-09-25 every functional option of the command is a launcher default (section 1, "Defaults promoted"); the rows left here are the telemetry/debug options, which stay opt-in. The stand command is now `x3run --direct` plus these flags (`docs/verification/user-runs.md`, "Stand command").

| Option (env variable) | What it does | Default state | Acceptance | Would a launcher default change anything for a fresh user? |
|---|---|---|---|---|
| `--loading-intervals` (`X3M_LOADING_INTERVALS`) | Retains per-thread loading call intervals until `save_load_complete`, 24 MiB payload (`:659`) | stand only, diagnostic | `docs/verification/loading-intervals.md:5`: "This diagnostic has no game capture yet" (2026-09-15 ledger head); no later verdict found | No benefit for a fresh user; a diagnostic the stand carries at 24 MiB per launch. Candidate to drop from the stand, not to default |
| `--shadow-retention-census` (`X3M_SHADOW_RETENTION_CENSUS`) | Retention census, replays nothing (`:734`) | stand only, diagnostic | `--shadow-caster-retention` "wins over `--shadow-retention-census`" (`:735`) | No: redundant beside `--shadow-caster-retention`; costs the census rows |
| `--fps-overlay` (`X3M_FPS_OVERLAY`) | On-screen FPS line (`:601`) | stand only | user preference; Ctrl+Alt+F7 hides it | Yes (visible overlay). Keep opt-in for a fresh user |
| `--frame-end-stride 1` (`X3M_FRAME_END_STRIDE`) | One `frame_end` line per frame, ~100 B/frame (`:600`) | stand only, diagnostic | | No benefit for a fresh user; log volume only |
| `--volumetric-fog-timing` (`X3M_VOLUMETRIC_FOG_TIMING`) | One `volumetric_fog_frame` line per frame (`:762`) | stand only, diagnostic | | No: log volume only |
| `--telemetry` (`X3M_TELEMETRY`) | Bounded loading, presentation and cursor diagnostics (`:597`); prerequisite of `--frame-timing`, `--mesh-adjacency`, `--loading-*`, `--window-trace` | stand only | `docs/verification/telemetry.md` | Unclear whether a fresh user needs it; the loading fast path `--mesh-adjacency fast` requires it (`:654`). A default would carry the diagnostic cost of every window report |
| `--camera-log 1` (`X3M_CAMERA_LOG`) | `camera_state` line every frame (`:704`) | stand only, diagnostic (default 300) | | No |
| `--frame-timing`, `--frame-phases`, `--object-bounds-log`, `--cull-census` (`X3M_FRAME_TIMING`, `X3M_FRAME_PHASES`, `X3M_OBJECT_BOUNDS_LOG`, `X3M_CULL_CENSUS`) | Frame-time windows, engine phase stamps, projected bounds on F8 frames, cull census on F8 frames (`:598`, `:636`, `:826`, `:825`) | stand only, diagnostics | | No benefit for a fresh user; `--frame-timing` installs the state hooks (`:802`) and the phase stamps cost per frame (`:636`) |
| `X3M_MOTION_FRAME_LOG=1` (env prefix) | `motion_output_frame` line every frame instead of every 60 (`capture.cpp:244`, `:3641`) | stand only, no launcher option | inventory: "FLOWN, in the run commands" (`cleanup-inventory-2026-09-22.md:162`) | No; a launcher option (`--motion-frame-log N`) would replace the shell prefix |

Redundant in the stand since 2026-09-25: every functional option of the command, including the five that were already defaults before (`--hdr-exposure auto`, `--shadow-replay-candidates`, `--shadow-sun-poll on`, `--light-map-far-fade 80,220`, `--motion-rt-mode lazy`); `--shadow-retention-census` is overridden by the retention.

## Defaults promoted (2026-09-25)

User decision 2026-09-25: the Run 84 A stand command becomes the launcher's default on a modded launch, without its
telemetry/debug options, plus `--music-keep` (accepted run273) and `--shadow-alpha-casters on` (flown clean run309).
`python3 tools/manage.py launch --bottle X3 --dry-run` with no other option now produces the stand environment minus
the telemetry/debug variables, plus `X3M_MUSIC_KEEP=1` and `X3M_SHADOW_ALPHA_CASTERS=1`; the old stand command still
works and yields the same environment plus the telemetry ones; `--vanilla` sends nothing modded (unchanged, measured).
Obsolete options were not touched (a later cleanup). Implementation: `apply_promoted_defaults` in `tools/manage.py`.

Classification of the 52 stand options:

| Class | Options | Launcher behaviour |
|---|---|---|
| Functional, promoted (36) | `--direct`, `--camera chase`, `--chase-view-restore`, `--ownership`, `--object-trace`, `--object-lifetime`, `--motion-output`, `--taa`, `--hdr`, `--hdr-tonemap`, `--hdr-bloom`, `--bloom-source-clamp 1.0`, `--crypt-cache`, `--gz-buffer`, `--resource-read fast`, `--dat-handles`, `--mesh-adjacency fast`, `--screen-emission-additive 2`, `--screen-emission-additive-alpha 0`, `--emission-source-gain 2`, `--sun-shadow-lane`, `--shadow-replay-depth`, `--sun-shadow-apply`, `--shadow-cascades 250,1500,7500,37500,150000`, `--shadow-cascade-drop-order importance`, `--shadow-cascade-records 1024,1024,2048,4096,4096`, `--shadow-cascade-sizes` (promoted as `2048,4096,4096,2048,2048`, not the stand's 2048 x5: user decision 2026-09-25), `--shadow-cascade-adaptive-c0 1.5`, `--shadow-caster-retention`, `--volumetric-fog 0.02`, `--volumetric-fog-cards replace`, `--volumetric-fog-range stored`, `--cull-small-parts 4`, `--capture-start 999999`, `--capture-frames 8`, `--capture-delay 300` (no automatic capture; F8 captures on demand) | Filled in when not given and when their own prerequisites hold after the earlier defaults (section 1 rows); help texts say "launcher default since 2026-09-25" |
| Functional, already defaults (5) | `--hdr-exposure auto`, `--shadow-replay-candidates` (implied by the replay), `--shadow-sun-poll on`, `--light-map-far-fade 80,220`, `--motion-rt-mode lazy` | Unchanged |
| Telemetry/debug, stay opt-in (11 + prefix) | `--telemetry`, `--camera-log 1`, `--loading-intervals`, `--shadow-retention-census`, `--fps-overlay`, `--frame-end-stride 1`, `--volumetric-fog-timing`, `--frame-timing`, `--frame-phases`, `--object-bounds-log`, `--cull-census`, `X3M_MOTION_FRAME_LOG=1` | Section 2; the stand command is now `x3run --direct` plus these |
| Added (2) | `--music-keep`, `--shadow-alpha-casters on` | `X3M_MUSIC_KEEP=1` on every modded launch; alpha casters with the replay |

Rules: a promoted default is filled only when the option is absent, the launch is modded and its prerequisites hold,
so opting out of a prerequisite (`--no-taa`, `--no-shadow-replay-depth`, `--camera vanilla`, ...) switches its
dependants off instead of refusing; an explicit dependant is still refused as before. Every promoted boolean has a
`--no-<name>`; valued options use their existing off value (`--camera vanilla`, `--resource-read native`,
`--mesh-adjacency native`, `--emission-source-gain 1`, `--shadow-cascade-drop-order submission`,
`--volumetric-fog-cards keep`, `--volumetric-fog-range legacy`, `--cull-small-parts 0`, `--shadow-alpha-casters off`)
or a new `--no-<name>` where none existed (`--no-bloom-source-clamp`, `--no-screen-emission-additive`,
`--no-screen-emission-additive-alpha`, `--no-shadow-cascades`, `--no-shadow-cascade-adaptive-c0`,
`--no-volumetric-fog`). No new `_DEFAULT` marker: none of the promoted options' DLL rows logs a `default=` field.
`CULL_SMALL_PARTS_DEFAULT_PX` is 4 (was 2).

Caveats: `--mesh-adjacency fast` is sent by default but the DLL reads `X3M_MESH_ADJACENCY` only inside its telemetry
branch (`src/proxy/loading_trace.cpp:1107-1126`), so without `--telemetry` the adjacency fast path stays inert (the
other loading fast paths do not need telemetry); an explicit `--mesh-adjacency verify|fast` without `--telemetry` is
still refused.

Evidence: `verification/results/launcher-defaults/compare_dry_runs.py` (host dry runs on bottle X3, output
`comparison.json`, recorded pre-change stand `stand_env_before.json`): empty vs stand differ in 13 `X3M_*` variables,
12 telemetry/debug plus the intended `X3M_SHADOW_CASCADE_SIZES` (`2048,4096,4096,2048,2048` by default, the stand's
explicit 2048 x5 wins when passed); the stand differs from its pre-change environment only by `X3M_MUSIC_KEEP`
(absent -> 1) and `X3M_SHADOW_ALPHA_CASTERS` (0 -> 1); the short stand command of `user-runs.md` differs from the old
one only by the map sizes; the X3 switches are the same; `--vanilla` is byte-identical to before (measured
2026-09-25). `verification/analysis/test_launcher_defaults.py` asserts the empty-command and stand environments, the
vanilla launch, every opt-out and the explicit refusals hermetically.

## 3. Opt-in, accepted, still useful

Diagnostics, A/B switches and alternative modes kept on purpose. Grouped; each row cites its definition.

| Option (env variable) | What it does | Default state | Acceptance | Notes |
|---|---|---|---|---|
| `--vanilla` | Builtin d3d9, no proxy (`tools/manage.py:596`, `:2149`) | opt-in | the reference launch (Run87 dry run D, `docs/status.md:30`) | Refuses every proxy-only option |
| `--dry-run` | Validates and prints the command and `X3M_*` environment as JSON (`:849`, `:2170-2185`) | opt-in | used by every qualification record | |
| `--bottle`, `--game-dir`, `--dll-source` | Bottle / game directory / DLL to install (`:583-585`) | defaults X3, bottle path, `build/d3d9.dll` | | Shared with install/uninstall/rollback/recover/status/fog-families (`:579-582`) |
| `--gpu-sync-timing` (`X3M_GPU_SYNC_TIMING`) | Serialised GPU cost per proxy pass (`:602`) | opt-in, always explicit 0 otherwise (`:1685`) | Runs 73 C, 74 B, 75 C, 77 C used it; "the flag doubles the frame" (row 82 B) | Attribution only, never a frame-rate figure |
| `--frame-timing`, `--frame-timing-state-stamps N`, `--telemetry-draw` (`X3M_FRAME_TIMING`, `..._STATE_STAMPS`, `X3M_TELEMETRY_DRAW`) | Frame-time windows; sampled state-call stamps; per-draw route cost (`:598`, `:629-630`, `:634-635`) | opt-in, require `--telemetry` (`:1604-1609`) | `docs/verification/sampling-profiler.md` "Frame timing diagnostic" | |
| `--game-phases`, `--game-phase-threshold-ms`, `--frame-phases`, `--pass-phases`, `--residual-phases`, `--light-phases`, `--submit-phases`, `--loop-phases` (`X3M_GAME_PHASES`, `X3M_GAME_PHASE_THRESHOLD_MS`, `X3M_FRAME_PHASES`, `X3M_PASS_PHASES`, `X3M_RESIDUAL_PHASES`, `X3M_LIGHT_PHASES`, `X3M_SUBMIT_PHASES`, `X3M_LOOP_PHASES`) | Byte-verified engine stamps partitioning the frame, the material pass loop, the residual, R7 callers, `view_submit` candidates, the per-sector update (`:631-641`) | opt-in; `--residual-phases` implies frame+pass phases (`:882`) | `docs/verification/sampling-profiler.md` sections named in the help; `docs/reverse-engineering/frame-loop-phases.md` | `--submit-phases` conflicts with the sun occlusion default (`:1572`): the default is then not sent |
| `--media-cue-trace`, `--music-trace` (`X3M_MEDIA_CUE_TRACE`, `X3M_MUSIC_TRACE`) | Trace every media-record build; trace the music state machine (`:642`, `:646`) | opt-in | `docs/verification/media-cues.md` "Gate"; `docs/verification/music-keep.md:52` | |
| `--audio-sites`, `--profile-raw` (`X3M_AUDIO_SITES`, `X3M_PROFILE_RAW`) | Load-hang witnesses on the audio path and raw thread contexts (`:647`, `:665`) | opt-in | The load hang's root cause was found (`docs/architecture/voice-decoder-adapter.md:264`) | See section 4 (their purpose is closed) |
| `--profile`, `--profile-interval-us` (`X3M_PROFILE`, `X3M_PROFILE_INTERVAL_US`) | In-process sampling profiler (`:663-664`; `src/proxy/sampling_profiler.cpp:471-475`) | opt-in | "blind to engine code under FEX" (`docs/verification/sampling-profiler.md:955`); goal 14 "attributes nothing under FEX" (`docs/goals.md:25`) | Useful only on native Windows or for proxy-side samples; see section 4 |
| `--mesh-adjacency verify`, `--mesh-adjacency-dump`, `--resource-read verify` (`X3M_MESH_ADJACENCY`, `X3M_MESH_ADJACENCY_DUMP`, `X3M_RESOURCE_READ`) | Verification modes of the two fast paths, logging any difference (`:654-655`, `:661`) | opt-in | `docs/verification/mesh-adjacency-fast.md`, `docs/verification/resource-reader.md:14-17` | Keep: they qualify the fast paths |
| `--loading-probes` (`X3M_LOADING_PROBES`) | IAT rows and entry counters on twelve loading functions (`:660`) | opt-in, requires `--telemetry` | `docs/verification/loading-probes.md` | |
| `--sector-background` (`X3M_SECTOR_BACKGROUND`) | Read-only active-sector background diagnostic (`:742`) | opt-in | `docs/reverse-engineering/sector-fog.md` section 11 | |
| `--shadow-sun-trace`, `--shadow-retention-timing` (`X3M_SHADOW_SUN_TRACE`, `X3M_SHADOW_RETENTION_TIMING`) | Per-frame sun trace; per-draw cost of the retention hook (`:729`, `:738`) | opt-in | | |
| `--shadow-cascade-caps`, `--shadow-cascade-static-from`, `--shadow-cascade-large-min`, `--shadow-cascade-budget`, `--shadow-cascade-ladder-ratio`, `--shadow-caster-retention-age`, `--shadow-caster-retention-eps` (`X3M_SHADOW_CASCADE_*`, `X3M_SHADOW_CASTER_RETENTION_*`) | Cascade tuning knobs (`:722-725`, `:730`, `:733`, `:736-737`) | opt-in, DLL defaults per help | `docs/architecture/shadow-cascades.md:269` (implemented 2026-09-18, default off) | No flight of `static-from`/`large-min`/`ladder-ratio` found in the run archive (grep, measured) |
| `--shadow-replay-size`, `--shadow-replay-extent`, `--shadow-replay-depth-half`, `--shadow-replay-cap` (`X3M_SHADOW_REPLAY_*`) | The single-map (no cascades) replay geometry (`:715-719`) | opt-in defaults 1024 / 250 / 512 / 512 | pre-cascade fixture path; every stand since Run 62 uses cascades (`cleanup-inventory-2026-09-22.md:66`) | Keep as the fixture and fallback path |
| `--sun-shadow-bias-units`, `--sun-shadow-bias-clamp-texels`, `--sun-shadow-bias-slope-texels` (`X3M_SUN_SHADOW_BIAS_*`) | Shadow compare biases (`:739-741`) | always forwarded at their defaults (`:1847-1849`) | `docs/verification/directional-shadows.md` | |
| `--taa-debug`, `--capture-start`, `--capture-frames`, `--capture-delay` (`X3M_TAA_DEBUG`, `X3M_CAPTURE_*`) | Raw resolve dumps in capture frames; F8 burst controls (`:671`, `:587-594`) | opt-in | Run 85/86 diagnostics flown with `--taa-debug` (`user-runs-completed.md:2442`) | ~40 MB per frame at 1280x768 (`:589`) |
| `--taa-k`, `--taa-history-weight`, `--taa-alpha-history` (`X3M_TAA_K`, `X3M_TAA_HISTORY_WEIGHT`, `X3M_TAA_ALPHA_HISTORY`) | Fixed luminance-weight k; history weight 0.5..0.98; time-accumulated alpha for bloom (`:672`, `:685-686`) | opt-in (k derived, weight 0.9 in the DLL `capture.cpp:3128`) | `--taa-alpha-history` kept by user verdict 2026-09-22 without a flight judgement (`cleanup-inventory-2026-09-22.md:145-149`, `:413-414`) | `--taa-alpha-history` is flagged "keep, never judged" |
| `--taa-sky-history-band-px` (`X3M_TAA_SKY_HISTORY_BAND_PX`) | Band threshold of the strict sky history, DLL default 3 (`:690`; `capture.cpp:3658`) | opt-in | `docs/architecture/seta-motion.md` section 4 | |
| A/B opt-outs of the section-1 defaults: `--taa-far-stabiliser off`, `--taa-thin-region off`, `--taa-thin-region-gate screen`, `--taa-thin-region-emissive 0`, `--taa-unmatched-static off|all`, `--taa-sky-history loose`, `--taa-sky-history-exit-px 0`, `--taa-motion-weight 0`, `--taa-box-resolution full`, `--taa-far-gate screen`, `--taa-far-clip 3x3`, `--taa-thin-vote off`, `--fade-rt2-owner off`, `--taa-thin-region-source both`, `--scene-hook off`, `--state-shadow on|off`, `--motion-rt-mode perdraw`, `--hdr-dither off`, `--no-window-monitor-rect`, `--lod-occlusion off`, `--terran-station-lod distance`, `--fov game`, `--sun-flare-fix off`, `--no-sun-occlusion`, `--sun-occlusion-core-f off`, `--cull-small-parts 0`, `--cull-small-parts-projectiles off`, `--no-collide-box-cull`, `--no-collide-sat-sse2`, `--no-collide-memo`, `--no-pause-key-only`, `--media-cue-cache off`, `--no-light-map-far-fade`, `--hull-emissive-widening off`, `--hull-lightmap-gain 1`, `--original-fill 0`, `--bolt-footprint 0`, `--chase-fov-compensate off`, `--chase-hud-anchor centre`, `--voice-decoder none`, `--no-fog-handover-*`, `--no-fog-docked`, `--fog-dust-motes 0`, `--fog-march-scale 2`, `--shadow-cascade-backface-from none`, `--shadow-cascade-min-footprint 0` | Each restores the documented earlier behaviour bit for bit (help text of each option) | opt-out | the cleanup inventory keeps them as "documented bit-identical rollback" (`cleanup-inventory-2026-09-22.md:76-96`) | `--taa-thin-region-gate screen` and `--taa-thin-region-source both|screen` carry their own program chain (section 4) |
| `--taa-sentinel 1|2` (`X3M_TAA_SENTINEL`) | Current-only or strict sentinel policy (`:687`) | opt-in | `docs/architecture/temporal-integration.md:582` | `1` disables the motion weight (`:1183`) |
| `--hdr-ev-manual`, `--hdr-exposure fixed`, `--hdr-ev`, `--hdr-ev-min/max`, `--hdr-meter-bg`, `--hdr-white-target`, `--hdr-ev-deadband`, `--hdr-edge-weight`, `--hdr-key-pull`, `--hdr-clamp`, `--hdr-decode srgb|none` (`X3M_HDR_*`) | Exposure and meter knobs (`:790-801`) | opt-in values | Goal 5: "the fixed-EV comparison stays on Ctrl+Shift+F9" (`docs/goals.md:16`) | |
| `--hdr-look golden|punchy` (`X3M_HDR_LOOK`) | AgX look variants (`:787`; `capture.cpp:3152-3155`) | opt-in, default none | No flight of either look found in the run archive (grep, measured); goal 2 "a custom X3 look is later tuning" (`docs/goals.md:13`) | Unclear value; never flown |
| `--sun-occlusion-log`, `--sun-occlusion-radius`, `--sun-occlusion-curve` (`X3M_SUN_OCCLUSION_LOG/_RADIUS/_CURVE`) | Probe/visibility diagnostic with a 1x1 readback per frame; disc radius; visibility exponent (`:837-839`) | opt-in | `docs/verification/sun-occlusion.md:109` (run223) | `--sun-occlusion-log` is refused with `--submit-phases` (`:1572`) |
| `--cull-census`, `--lod-switch-log [N]`, `--object-bounds-log` (`X3M_CULL_CENSUS`, `X3M_LOD_SWITCH_LOG`, `X3M_OBJECT_BOUNDS_LOG`) | Engine cull/LOD census on F8 frames; LOD switch rows; projected bounds (`:825-826`, `:845`) | opt-in | `docs/verification/cull-census.md:24` (2026-09-24) | The merged-LOD overlay tooling reads them |
| `--cull-small-parts-scope bodies` (`X3M_CULL_SMALL_PARTS_SCOPE`) | Cull only parentless nodes (`:847`) | opt-in | Run 43 B: "`bodies` culled 36 nodes/frame and saved nothing" (`docs/verification/cull-small-parts.md:22`) | Low value A/B; keep or drop |
| `--collide-narrow-census`, `--collide-query-phases`, `--collide-memo-verify` (`X3M_COLLIDE_NARROW_CENSUS`, `X3M_COLLIDE_QUERY_PHASES`, `X3M_COLLIDE_MEMO_VERIFY`) | Narrow-phase census; query/descent timings; memo verification (`:829`, `:834`, `:844`) | opt-in | `docs/verification/sampling-profiler.md:2143` (Run 44 A); collision "paused by user request" (`docs/status.md:102`) | |
| `--point-light-root-admission` (`X3M_POINT_LIGHT_ROOT_ADMISSION`) | Admits a point light when the root object is in range (`:824`; `src/proxy/point_light_admission.cpp:177`) | opt-in | run68 negative ("docking modules partly dark"); user verdict 2026-09-22: keep (`cleanup-inventory-2026-09-22.md:140-144`, `:413`) | Kept by explicit verdict; no positive flight |
| `--window-trace`, `--cursor-reassert` (`X3M_WINDOW_TRACE`, `X3M_CURSOR_REASSERT`) | Window/cursor message trace; balanced cursor re-assert after activation (`:613-628`) | opt-in (`:1695-1698`) | Run 84 A: the re-assert "fires correctly on all 20 fires ... cause inside the Cocoa driver, parked by the user" (`docs/verification/user-runs.md:25`; `docs/architecture/window-and-cursor.md:161`) | `--cursor-reassert` is a failed fix kept opt-in by user decision (also in section 4) |
| `--fog-shadow-pass on`, `--fog-far-bins 24`, `--volumetric-fog-everywhere`, `--volumetric-fog-timing` (`X3M_FOG_SHADOW_PASS`, `X3M_FOG_FAR_BINS`, `X3M_VOLUMETRIC_FOG_EVERYWHERE`, `X3M_VOLUMETRIC_FOG_TIMING`) | Separate shaft-visibility pass; cheaper far march; force fog everywhere (debug); per-frame timing (`:748`, `:751`, `:761-762`) | opt-in | see section 4 for the first two | |
| `--fog-dust-motes N,SIZE,STREAK` with `X3M_FOG_MOTES_<NAME>`, `--volumetric-fog STRENGTH` | Mote and density tuning (`:743`, `:749`) | opt-in values | Goal 12 | Ctrl+Alt+F9/F10/F11 toggle in game (`:743`, `:749`) |
| `--lod-switch-log`, `--terran-station-lod distance`, `--lod-occlusion off` | LOD diagnostics and A/Bs (`:820-821`, `:845`) | opt-in | Runs 79/80 A | |
| `--pause-key CODE` (`X3M_PAUSE_KEY`) | Engine key code of a rebound Pause key (`:843`; `src/proxy/pause_key_only.cpp:93`) | opt-in | `docs/verification/pause-key-only.md:13` | |
| `--chase-rot-tau`, `--chase-pos-tau`, `--chase-lag-clamp-deg`, `--chase-pos-lag-clamp`, `--chase-combat-tightness` (`X3M_CHASE_*`) | Chase spring and clamp tunables (`:806-814`) | opt-in, DLL compiled defaults (`src/proxy/chase_camera.cpp:401-409`) | Goal 17 | `combat-tightness` is "unverified in game" (`:814`) |
| `--screen-emission-additive G`, `--emission-source-gain G`, `--hull-lightmap-gain G`, `--original-fill K`, `--bloom-source-clamp C` values | Gains the user tunes (`:765`, `:772`, `:781`, `:780`, `:789`) | see sections 1-2 | Goals 3-6 | Ctrl+Shift+F4/F5/F6 toggle each family in game (`:765`, `:772`, `:781`) |
| `--gz-buffer-kb`, `--media-cue-retry-s`, `--game-phase-threshold-ms`, `--profile-interval-us`, `--camera-cut-deg`, `--camera-log`, `--frame-end-stride` | Numeric knobs (`:657`, `:644`, `:632`, `:664`, `:703-704`, `:599`) | values | | |
| `manage.py fog-families [--check|--install|--dry-run]`, `status`, `install`, `uninstall`, `rollback`, `recover` | The non-launch actions (`:579-582`, `:263-276`, `:1626-1665`) | | `docs/status.md:21-23` (fog family loader, Run87) | `--dll-source` applies to `install` only (`:585`) |

## 4. Obsolete, superseded or failed experiments

Options and variables still in the code whose feature was rejected, replaced or closed. Recommendation column:
**launcher** = remove the option (and its refusal/env line), **DLL** = the DLL read and code can go too, **A/B** =
keep as a documented opt-out.

| Option (env variable) | What it does | Default state | Ledger line that closed it | Recommendation and notes |
|---|---|---|---|---|
| `--taa-current-filter`, `--taa-line-filter`, `--taa-thin-clip`, `--taa-adaptive-weight` | Refusal stubs only (`tools/manage.py:675-677`, `:947-950`); env names popped (`:1770-1772`); no DLL read | refused by name | Cleanup batch 6, 2026-09-23: rejected / superseded resolve variants (`cleanup-inventory-2026-09-22.md:38-41`, `:384`) | launcher (drop the stubs once no old command lines circulate) |
| `--taa-sentinel-stabiliser` (`X3M_TAA_SENTINEL_STABILISER`, `..._DEFAULT`) | Refusal stub (`:682-684`, `:1025-1027`); the DLL logs `ignored=1 reason=retired` (`src/proxy/capture.cpp:3125`) | refused by name | TAA mask fold 2026-09-25 (`docs/architecture/taa-mask-fold.md` section 7; `:1026`) | launcher + DLL ignore line. Run 84 A still told the user to `unset X3M_TAA_SENTINEL_STABILISER` (`user-runs-completed.md:2391`) |
| `--taa-region-hold` (`X3M_TAA_REGION_HOLD`) | Refusal stub (`:696-698`, refused at `:1081`); DLL logs `ignored=1 reason=removed` (`capture.cpp:3695-3698`) | refused by name | A' accepted after Run 79 A, 2026-09-24 (`:696-697`) | launcher + DLL ignore line |
| `--volumetric-fog-look` (`X3M_VOLUMETRIC_FOG_LOOK`) | Refusal stub (`:746-747`, `:1856`); DLL logs `volumetric_fog_look_ignored` (`capture.cpp:3538`) | refused by name | Presets L0/L1/L3 retired batch 8, 2026-09-22 (`cleanup-inventory-2026-09-22.md:116`, `:466`) | launcher + DLL ignore line |
| `--lod-scale FACTOR` (`X3M_LOD_SCALE`) | Scales the engine LOD switch distances by a proxy-owned mirror (`:819`; `src/proxy/lod_scale.cpp:31`) | opt-in, default absent | "`--lod-scale` is closed default-off" (`docs/goals.md:36-37`); inventory D REJECTED, batch 4 not marked done (`cleanup-inventory-2026-09-22.md:37`, `:353-354`); memory: lod-scale stays off after the merged-LOD pilot, which is now accepted (goal 18) | launcher + DLL (~552 lines per the inventory). The merged-LOD overlay replaced its purpose |
| `--linear-materials` with `--material-fill`, `--material-direct-gain`, `--material-emissive-gain`, `--lightmap-emissive-gain`, `--linear-distance-fade` / `--no-linear-distance-fade` (`X3M_LINEAR_MATERIALS`, `X3M_MATERIAL_*`, `X3M_LIGHTMAP_EMISSIVE_GAIN`, `X3M_LINEAR_DISTANCE_FADE`) | Converted hull material law in linear light and the Asteroid source-over fade (`:775-779`, `:708-709`; `capture.cpp:3185`, `:3201-3206`, `:3228`) | opt-in | Goal 6 In progress but "the user plays original hulls" (`docs/goals.md:17`); goal 1 closed "unused because the user plays the original hulls" (`:12`); inventory H "RETIRED subject, entangled", not scheduled (`cleanup-inventory-2026-09-22.md:47`, `:264-289`, `:409`) | launcher: remove the six options (they refuse most defaults: `--original-fill`, `--hull-lightmap-gain`, `--light-map-far-fade` all exclude it, `:1447`, `:1461`). DLL: the transformer shares `linear_material.cpp` with the flown `--original-fill` / `--hull-lightmap-gain` (`:264-274`), so it stays until that split is done |
| `--linear-emissions`, `--emission-gain` (`X3M_LINEAR_EMISSIONS`, `X3M_EMISSION_GAIN`) | Full-surface additive emission bracket in linear light (`:770-771`; `capture.cpp:3219-3227`) | opt-in | "rejected 2026-09-15" (`docs/goals.md:32-33`); inventory I REJECTED, not scheduled (`cleanup-inventory-2026-09-22.md:48`, `:290-300`) | launcher: remove. DLL: `linear_emission_pass.cpp` is load-bearing for the flown `--emission-source-gain` (`:48`, `:297`); leave until split |
| `--screen-emission` with `--screen-emission-gain`, `--screen-emission-timing` (`X3M_SCREEN_EMISSION`, `X3M_SCREEN_EMISSION_BOUND`, `X3M_SCREEN_EMISSION_GAIN`, `X3M_SCREEN_EMISSION_TIMING`) | Packed screen emission of the bullet draws inside a region bracket, steps C-E (`:764`, `:768-769`; `capture.cpp:3258-3272`, `:3472`; `src/proxy/motion_output.cpp:2428-2429`) | opt-in; mutually exclusive with the accepted `--screen-emission-additive` (`:765`) | Runs 20/21: "the bolts are unchanged" (`docs/verification/screen-emission.md:22-23`, 2026-09-15); the additive form was accepted instead (goal 3, run 26; `docs/goals.md:14`, `:31-32`) | launcher + DLL candidate: superseded by the additive bullets. The locked-prefix bound (`X3M_SCREEN_EMISSION_BOUND`) is also read by `--bolt-footprint`'s Unlock scan (`:766`), so check that dependency first |
| `--fade-witness [K]` (`X3M_FADE_WITNESS`) | Reads back the M coverage target every K frames for the fade region (`:710`; `capture.cpp:3475-3483`) | opt-in, requires `--linear-distance-fade` or `--screen-emission` (`:1209`) | Its only subjects are the two rows above; last flown run 21 (`screen-emission.md:23`, 2026-09-15) | launcher + DLL with the packed emission |
| `--shimmer-trace` (`X3M_SHIMMER_TRACE`) | Per-frame Asteroid-class draw trace (`:763`; `capture.cpp:3486`) | opt-in | "documented against the retired fade-region note" (`cleanup-inventory-2026-09-22.md:109-110`) | launcher + DLL |
| `--hull-emitters`, `--hull-emission-gain G` (`X3M_HULL_EMISSION_GAIN`) | Gain over the twelve hull-program ADD ONE/ONE emitters (`:773-774`; `capture.cpp:3305`) | opt-in | Run 38 C run114: "User saw no visible difference toggling `--hull-emitters`"; the gain reaches only 2 of 12 programs (`screen-emission.md:38`; `docs/goals.md:14`); the light-map gain took over the windows and hull lights | launcher candidate; the inventory listed it under "keep" (`cleanup-inventory-2026-09-22.md:86`) without a verdict. Unclear whether the two guide-light programs are wanted; ask |
| `--fog-shadow-pass on` (`X3M_FOG_SHADOW_PASS`) | Sun-shadow shaft visibility in its own quarter-resolution pass (`:748`; `capture.cpp:3524`) | opt-in, default off | Run 68 C run256: "no visible or median-cost difference, pass stays off" (row 68 A/B/C, 2026-09-23); Run 74 B run276: costs ~0.3 ms, "stays off" (row 74 B); goal 12 "fog shadow pass stays off (no win)" (`docs/goals.md:23`) | launcher + DLL candidate. It also forces the march scale to 2 (`:1368-1369`, `:1861`) and refuses `--fog-far-bins 24` (`:751`), so it constrains two live options |
| `--fog-far-bins 24` (`X3M_FOG_FAR_BINS`) | 24 far march bins instead of 40 (`:751`; `capture.cpp:3567`) | opt-in, default 40 always written | Run 76 D run284-286: "save only 0.2-0.5 ms ... no visible difference; 40 stays the default" (row 76 B/C/D, 2026-09-24) | launcher + DLL: keep the 40-bin programs only |
| `--taa-history-taps 16` (`X3M_TAA_HISTORY_TAPS`) | The 16-tap point history reconstruction of the earlier builds (`:692`; `capture.cpp:3670-3674`) | opt-in, default 5 | Run 78 A: "TAA taps 5, no refusals, look accepted" (row 78 A, 2026-09-24); 16 turns a camera-gated thin region off (`:692`) | launcher + DLL: 16 disables the accepted thin-region chain, so it is no longer an equal A/B. Keep only if a device without FP16 filtering matters (`:692`) |
| `--taa-thin-region-gate screen` and `--taa-thin-region-source screen|both` (`X3M_TAA_THIN_REGION_GATE=screen`, `X3M_TAA_THIN_REGION_SOURCE`) | The pre-Run59 screen-speed gate with its 11x11 / 17x17 dilation draws and the screen-space fragmented-depth search (`:681`, `:701`) | opt-out A/B; `source screen` is refused with the camera gate (`:1123-1126`) | Camera gate accepted Run 59 (`:1003`); A' accepted Run 79 A; mask fold 2026-09-25 "no visible difference between both and vote" (`:701`). The `screen` source "compiled but off": reachable only through `--taa-thin-region-gate screen` | A/B today; candidate launcher + DLL removal of the screen-gate chain (its own program set) once the user confirms the camera gate is final. Removing the gate removes both `source` values with it |
| `--cursor-reassert` (`X3M_CURSOR_REASSERT`) | Balanced SetCursor/ShowCursor sequence after activation (`:621-628`; `src/proxy/cursor_reassert.cpp:30`) | opt-in | Run 84 A: fires correctly, cause in the Cocoa driver, "parked by the user" (`docs/verification/user-runs.md:25`; `window-and-cursor.md:161` "stays opt-in") | failed fix; keep opt-in per the user's decision or remove launcher + DLL. Goal 15 stays Planned |
| `--chase-scene-fix` (`X3M_CHASE_SCENE_FIX`) | Re-expresses the layer-0 cockpit-scene camera through the smoothed view (`:815`; `src/proxy/chase_camera.cpp:411`) | opt-in, "default off until the first run shows an external-view HUD element rendered there" | Review 31 A5 (`docs/verification/review-31-chase-camera-architecture.md:39`); first chase run flew with it off (`docs/verification/chase-first-run.md:30`); no later flight found (grep, measured) | launcher + DLL candidate: the condition it waits for never occurred across Runs 31-86 |
| `--mesh-cache` (`X3M_MESH_CACHE`) | "experimental verified native adjacency reuse" (`:653`; `src/proxy/loading_trace.cpp:1124`) | opt-in, requires `--telemetry` | Flown in iteration 05 (`docs/verification/iteration-05.md:54`); superseded by `--mesh-adjacency fast` (accepted, goal 14) (inferred from `docs/verification/handoff-mesh-adjacency-fast.md:42`, which lists the cache hook as history) | launcher + DLL candidate; "unclear" whether any fixture still exercises `run_mesh_cache_hook.py` (`handoff-mesh-adjacency-fast.md:58`) |
| `--depth-copy`, `--scene-depth-capture`, `--motion-capture`, `--finite-positions` (`X3M_DEPTH_COPY`, `X3M_SCENE_DEPTH_CAPTURE`, `X3M_MOTION_CAPTURE`, `X3M_FINITE_POSITIONS`) | Original-preserving depth copy; scene depth in capture frames; private rigid-motion diagnostics; position validation from uploads (`:649-650`, `:666-667`; `src/proxy/loader.cpp:54-56`, `capture.cpp:3050-3052`) | opt-in, chained prerequisites (`--motion-capture` needs all three) | Iteration-05 era capture chain (`iteration-05.md:54`); "predate the" same-draw motion route (`cleanup-inventory-2026-09-22.md:110`); direction memory: replay/admission is reference only | launcher + DLL candidates; the finite-upload observer and buffer evidence ledgers (`docs/verification/finite-*.md`) are their history |
| `--loading-intervals` (`X3M_LOADING_INTERVALS`) | 24 MiB per-thread loading interval recorder (`:659`; `loading_trace.cpp:1075`) | stand only (section 2) | Ledger head still says "no game capture yet" (`docs/verification/loading-intervals.md:5`, 2026-09-15); no consumer found after that date (grep, measured) | Drop from the stand; launcher + DLL candidate unless the loading-time investigation (AGENTS.md "Investigate loading time") still wants it |
| `--audio-sites`, `--profile-raw` (`X3M_AUDIO_SITES`, `X3M_PROFILE_RAW`) | Load-hang witnesses (`:647`, `:665`) | opt-in | Root cause of the load hang found and fixed by the decoder plugin (`docs/architecture/voice-decoder-adapter.md:222-264`); voice decision memory: resolved | launcher + DLL candidates |
| `--profile`, `--profile-interval-us` (`X3M_PROFILE`, `X3M_PROFILE_INTERVAL_US`, `X3M_PROFILE_REPORT_S`) | Sampling profiler (`:663-664`) | opt-in | "blind to engine code under FEX" (`docs/verification/sampling-profiler.md:955`); the leaf/RVA method "closed out for this FEX build" (`docs/reverse-engineering/view-submit-hot-path.md:426`) | A/B: keep for native Windows; on CrossOver it produces nothing the phase stamps do not |
| `--cull-small-parts-scope bodies` | see section 3 | opt-in | Run 43 B saved nothing (`cull-small-parts.md:22`) | A/B, low value |
| `--point-light-root-admission` | see section 3 | opt-in | run68 negative, kept by verdict | A/B by user verdict (2026-09-22); flag for a second look |
| `--taa-alpha-history` | see section 3 | opt-in | never judged, kept by verdict | A/B by user verdict |
| `--hdr-look golden|punchy` | see section 3 | opt-in | never flown (grep, measured) | A/B; unclear value |
| `--shadow-retention-census` | see section 2 | stand only | overridden by `--shadow-caster-retention` (`:735`) | Drop from the stand; keep the option as the stage-1 calibration tool |
| `--volumetric-fog-cards keep`, `--volumetric-fog-range legacy` as launcher defaults | The launcher's defaults are the non-accepted fog (`:744-745`) | launcher default | Goal 12: the accepted look is replace + stored (`docs/goals.md:23`) | Done 2026-09-25: the fog, `replace` and `stored` are launcher defaults (section 1) |
| `X3M_VOLUMETRIC_FOG_ANISOTROPY` | Henyey-Greenstein g of the legacy fog, DLL read only (`capture.cpp:3514`; default 0.3 `fog_pass_math.h:12`) | no launcher option since 2026-09-22 | Inventory J: "SUPERSEDED (inert), removed 2026-09-22" from the launcher (`cleanup-inventory-2026-09-22.md:39`, `:303-307`) | DLL: remove the read (leftover) |
| `X3M_FADE_ROUTE` | Fade-band motion arm threshold, permille or `off` (`capture.cpp:170`, `:3240-3244`) | DLL default 500, never set by the launcher | Inventory: "Retired subject ... but it also gates the flown screen-emission arm" (`cleanup-inventory-2026-09-22.md:167-169`); `--fade-rt2-owner` reads the same threshold (`:699`, `capture.cpp:2634`) | Keep the constant; the environment read is a leftover knob (no launcher option) |

## 5. Environment variables without a launcher option

Every `X3M_*` the DLL reads that `tools/manage.py` never sets (set difference of the two grep lists, measured).
"Leftover" = no launcher option, no fixture and no documented use found.

| Variable | Read at | What it does / DLL default | Leftover? |
|---|---|---|---|
| `X3M_CHASE_MAX_DT` | `src/proxy/chase_camera.cpp:410` | dt clamp after pauses/loads, default 0.10 s (`chase_camera_math.h:168`) | Tuning knob, inventory "keep" (`cleanup-inventory-2026-09-22.md:159-161`) |
| `X3M_HDR_ADAPT_UP`, `X3M_HDR_ADAPT_DOWN` | `capture.cpp:3170-3171` | Exposure adaptation time constants, defaults 0.4 / 1.2 s (`src/renderer/exposure.h:39-40`) | Tuning knob, keep (inventory) |
| `X3M_HDR_DT_MS` | `capture.cpp:3184` | Fixed frame dt for the meter, default 0 = live (`hdr_pass.h:88`) | Fixture/deterministic knob, keep |
| `X3M_HDR_EV_OFFSET` | `capture.cpp:3165` | Alias of `X3M_HDR_EV` (same read) | Leftover alias; the launcher sends `X3M_HDR_EV` (`tools/manage.py:1935`) |
| `X3M_HDR_KEY` | `capture.cpp:3166` | Meter key, default 0.18 (`exposure.h:35`) | Tuning knob, keep |
| `X3M_HDR_METER_MIN_LIT` | `capture.cpp:3179` | Lit fraction below which the target is neutral, default 0.01 (`exposure.h:46`) | Tuning knob, keep |
| `X3M_MOTION_FRAME_LOG` | `capture.cpp:3641` (default 60, `:244`) | `motion_output_frame` cadence; the stand passes `=1` as a shell prefix | In use every launch without an option: candidate `--motion-frame-log N` |
| `X3M_MOTION_JITTER_SAMPLES` | `capture.cpp:3060` (default 8, `:346`) | Halton sample count 2..64 | Tuning knob; unclear whether any value other than 8 was ever flown (no run mention found, measured) |
| `X3M_PROFILE_REPORT_S` | `src/proxy/sampling_profiler.cpp:476` | Profiler report period, default 5 s | Goes with `--profile` (section 4) |
| `X3M_VOLUMETRIC_FOG_ANISOTROPY` | `capture.cpp:3514` | Legacy fog phase g, default 0.3 | Leftover (section 4) |
| `X3M_FOG_LOOK_<NAME>` (27 fields: COVERAGE, EXPONENT, SIGMA_SCALE, FORWARD_G, FORWARD_WEIGHT, BACK_G, ALBEDO_WHITE, AMBIENT_GAIN, EXTINCTION_TINT, SCATTER_LIFT, LIFT_FLOOR, SHADOW_FLOOR, SKY_CAP, TAPER_START, SELF_SHADOW, POWDER, TAP_DISTANCE, TAP_LENGTH, COVERAGE_VARIATION, WARP_CYCLES_NEAR, WARP_NEAR, WARP_CYCLES_FAR, WARP_FAR, SHADOW_JITTER, PENUMBRA, PENUMBRA_MIN, PENUMBRA_MAX) and `X3M_FOG_LOOK_AMBIENT_SUN/_AWAY` (r,g,b) | `capture.cpp:3541-3554`; names in `src/renderer/fog_look_math.h:52-69` | Stored-range look tuning, read once; defaults are the accepted L2 look | Tuning knobs named in `--volumetric-fog-range` help (`tools/manage.py:745`); `SHADOW_JITTER=0` was flown in Run 77 C2 (`user-runs-completed.md:2196`). Keep; document the list in the fog note |
| `X3M_FOG_MOTES_<NAME>` (RADIUS, NEAR, MAX_PX, GAIN, SOFT, DRIFT) and `X3M_FOG_MOTES_SEED` | `capture.cpp:3614-3621`; names in `src/renderer/fog_mote_math.h:24-28` | Mote tuning; the launcher sets `MAX_PX=8` by default (`tools/manage.py:1878`) | Keep (goal 12 accepted at MAX_PX 8) |
| `X3M_LOCKED_PREFIX_LOG` | `src/proxy/motion_output.cpp:2430` | Per-draw `locked_prefix` line on every frame, for fixtures (`motion_output.h:2179`) | Fixture diagnostic, keep with the fixtures |
| `X3M_ADMISSION` | `src/ownership/application_admission_abi.cpp:34` | Publishes the process admission monitor when `=1` (`capture.cpp:521-524`, `motion_output.cpp:8277`) | Inventory: kept (`cleanup-inventory-2026-09-22.md:165-166`); unclear whether any fixture sets it today |
| `X3M_TAA_REGION_HOLD`, `X3M_TAA_SENTINEL_STABILISER`, `X3M_VOLUMETRIC_FOG_LOOK` | `capture.cpp:3698`, `:3125`, `:3538` | Read only to log one "ignored" line | Leftovers of section 4; remove with their stubs |
| `X3M_FIXTURE_SUN_LANE_FAULT`, `X3M_FIXTURE_SHADOW_EXTENT`, `X3M_FIXTURE_SHADOW_CASCADES`, `X3M_FIXTURE_SLICE_NEAR`, `X3M_FIXTURE_STRETCH_FAULT`, `X3M_FIXTURE_FADE_RECT`, `X3M_FIXTURE_SCREEN_RECT`, `X3M_FIXTURE_SCREEN_CAPS_FAULT`, `X3M_FIXTURE_MOTION_DEPTH` | `src/proxy/motion_output.cpp:2276-2504`, `src/proxy/sun_share_lane_inc.h:38-274` (narrow `GetEnvironmentVariableA` reads, each inside an `#ifdef X3M_MOTION_OUTPUT_FIXTURE` block at `:2275-2445`) | Fault injection for the fixtures | Keep with the fixtures (inventory `:154-158`); compiled out of the production DLL by the `#ifdef` |

Launcher-side variables the DLL never reads: `X3M_BOTTLE` (bottle override, `tools/manage.py:584`), `X3M_SOURCE_COMMIT`,
`X3M_VOICE_DECODER_REPO` (launcher-only test override, popped at `:2117`), `X3M_FOG_FAMILIES` (`:197-202`), and the
five popped retired names (`:1770-1772`). `X3M_FIXTURE_BOTTLE` in the stand prefix is read by
`verification/probe/wine_lock.py` and the runners, not by the launcher or the DLL.

## Top removal candidates (in the order a cleanup batch would take them)

1. `--lod-scale` + `src/proxy/lod_scale.cpp` (closed, superseded by the merged-LOD overlay; batch 4 never ran).
2. `--screen-emission`, `--screen-emission-gain`, `--screen-emission-timing`, `--fade-witness`, `--shimmer-trace` (packed emission and its two diagnostics; the additive bullets replaced it 2026-09-15/16).
3. `--fog-shadow-pass` (no win twice; it constrains the march scale and far bins).
4. `--fog-far-bins 24` (no visible difference, 0.2-0.5 ms).
5. `--taa-history-taps 16` (turns the accepted thin-region chain off; taps 5 accepted).
6. `--depth-copy`, `--scene-depth-capture`, `--motion-capture`, `--finite-positions`, `--mesh-cache` (iteration-05 capture chain).
7. `--audio-sites`, `--profile-raw` (load hang resolved); `--profile` stays for native Windows only.
8. `--chase-scene-fix` (its trigger condition never occurred).
9. The seven refusal stubs and the three DLL "ignored" reads, plus `X3M_VOLUMETRIC_FOG_ANISOTROPY` and the `X3M_HDR_EV_OFFSET` alias.
10. `--linear-materials` family and `--linear-emissions` + `--emission-gain` from the launcher only (the DLL code is entangled with the flown fill, light-map gain and source gain; inventory H/I).

Decisions this survey cannot make: `--hull-emitters` (no visible difference, no verdict), `--cursor-reassert` (parked
by the user, kept opt-in), `--taa-thin-region-gate screen` chain (the last A/B of the camera gate). The
section-2 accepted set
(`--camera chase --chase-view-restore`, TAA with its three prerequisites, HDR + tonemap + bloom, the loading fast paths,
the additive bullets and source gain, the shadow set, the stored fog, `--music-keep`, `--shadow-alpha-casters on`,
capture-start off)
became the launcher default on 2026-09-25.
