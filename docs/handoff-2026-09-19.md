# Handoff 2026-09-19

Short current handoff; [status](status.md) is the authoritative summary, the
[goals table](goals.md) the acceptance state, [user-runs.md](verification/user-runs.md)
the run queue. Previous: [archive/handoff-2026-09-18-late-night.md](archive/handoff-2026-09-18-late-night.md).

## Where things stand

- **Installed:** run46 candidate `fc3dd232…` from main `8ba92765`
  ([build](../verification/results/run46-candidate-build.json),
  [install](../verification/results/run46-candidate-install.json)); rollback run45
  `563a40f5…` in `/tmp/x3-candidate-Uvr1Rh/rollback`. Install only through
  `python3 tools/manage.py install --dll-source <dll>`.
- **Run 45 is read** (run table). A: SSE2 box test 27 → 12.7 ms collide, 24 → ~38 fps;
  83 % of node visits are pairs unchanged since the previous frame with no contact →
  `--collide-memo` (merged, two reviews). Whole-descent SSE2 rewrite: no gain, dropped
  (sector-collide.md §13). B: adaptive weight rejected by the user.
- **Shimmer:** (1) lattice crawl → `--taa-line-filter A[,W]` on a general depth line mask
  (replay: presented crawl 7.46 → 3.83–4.32 codes on run148); (2) distant station lines →
  `--taa-far-stabiliser W[,A]` (replay: weight only ×0.19 still / ×0.55 drifting with
  0.96 / 0.80 of the detail kept); the node fade band and far sharpen removal are dropped.
- **Run 46 is queued** (§46): A memo verify then memo FPS; B line filter 1,2 vs 2,2;
  C far stabiliser weight-only vs weight + filter; D frame attribution
  (`--pass-phases --frame-timing`) for the 11 ms `view_submit` share.
- **Merged for run 47:** per-draw lever 3 (hook-free lazy RT; default `perdraw`).
- **Closed by evaluation:** SSR (NO-GO; one close-range docking-bay capture would reopen a
  near-field emissive variant), flare occlusion (MINOR), depth-aware bloom and DOF (user).
  Volumetric nebula/fog: on hold until the user decides. Soft particles: not wanted.
- **User configuration:** as before, plus `--cull-small-parts 2` scope `all` by default.

## When run 46 is reported

1. A: `collide_memo` rows: `verify_mismatches` must be 0 with a large `verified`;
   `foreign_thread`, `reentered`, `stuck_busy` 0; then collide ms and FPS with the memo.
   If clean: make `--collide-sat-sse2 --collide-memo` launcher defaults. The 66 vs 31 ns
   per-visit gap (flight vs fixture) is the next collide question.
2. B/C: the user's picks; then defaults for the line filter and the far stabiliser
   (`resolve_far` has 4 and `resolve_age_line` 3 free slots: no further additions to
   those variants).
3. D: split `view_submit`; choose the next engine-side target (traversal patch vs proxy).
4. Build the run 47 candidate with lever 3; flight: lazy vs perdraw at the busy station,
   read `lazy_flushes`, `set_rt`, `lazy_mask_writes`, `restore_failures`.

## Open, lower priority

- Pre-existing on main: `verify_ownership.py` pins 553 vs 563; `run_ownership_integration_fallback.py` stub does not link (now four more exports missing); `run_application_admission_abi.py` wants a missing `-initial-summary.json`; shader generator full `--check` stale for ao_*/hdr_*/taa_sharpen/quad_vertex/rigid_motion; `cull-small-parts-cpu.json` still records `scope=bodies`.
- Lever 1 follow-up: a seam-ownership case with a Reset and a simulated DEVICENOTRESET.
- Collide: P2 margin under 24-bit x87 precision control; partial-install rollback
  fixture; `fix_[16]` bound; verifier's interior-branch scan is function-wide
  (sector-collide.md §10.1). The collide verifier's claim scan does not list
  the cull-small-parts site (no overlap).
- Fade route: no fixture for Reset between a routed draw and its overlay; no
  host test of `origin_distance_front` (covered by the pool-hull case).
- Shadows: ±0.2 u along-ray receiver offset; own-ship C0 re-roll; retention age
  cap; `sort_us` hook site; `--original-fill` value flight.
- Telemetry gaps from run 42: no `loading_phase` marker on a second save load;
  no self-cost field on `frame_phases`/`motion_output_frame`.

## Decisions (do not reopen without a reason)

As in the archived late-night handoff, plus: cull default 2 px scope all; mip bias
−0.5 stays; no MSAA, SSAA, global TAA blur, jitter resequencing or luminance clamp;
clustered lighting deferred; per-draw lever order 1+2a, then 3, 2b closed.

## Housekeeping

- Snippet mocks drifted again (capture bloom/device-creation, sun-share latch
  pin): run the full host suite before every candidate, not only three modules.
- `/tmp/x3-bottleX3-run117…132` preserved sessions; `/tmp/x3-candidate-7kxCZ4`
  holds the rejected build. Merged worktrees of this session can be removed;
  62 older worktrees (4.5 GB) predate it.
