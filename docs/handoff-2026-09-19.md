# Handoff 2026-09-19

Short current handoff; [status](status.md) is the authoritative summary, the
[goals table](goals.md) the acceptance state, [user-runs.md](verification/user-runs.md)
the run queue. Previous: [archive/handoff-2026-09-18-late-night.md](archive/handoff-2026-09-18-late-night.md).

## Where things stand

- **Installed:** run43 candidate `2cac537a…` from main `09a3b8d9`
  ([build](../verification/results/run43-candidate-build.json),
  [install](../verification/results/run43-candidate-install.json)); rollback run42
  `1a5dd46c…` in `/tmp/x3-candidate-HeaDRN/rollback`. Install only through
  `python3 tools/manage.py install --dll-source <dll>`.
- **Nothing is in flight.** The four worktrees of 2026-09-18 are merged
  (shimmer fixes, cull small parts + scope, collide box cull, route bench/trim).
- **Run 43 is queued** (§43, five complete commands): A collide A/B with
  `--loop-phases`; B cull scope `bodies` vs `all` at 2 px (optional `bodies` 4);
  C shimmer check.
- **User configuration:** unchanged (original hull shading; light-map gain 4,
  effects and guide lights 2; five cascades 2048², K 1.5; FPS overlay Ctrl+Alt+F7).

## When run 43 is reported

1. A: `collide_census` pair/reject counters (only in the option-on launch) and
   the `loop_phases` collide time off vs on decide: keep the box check, or build
   the broadphase rewrite (sort-and-sweep calling the engine's pair body). Kept
   pairs cost 4–5 % more, so a low reject share is a net loss.
2. B: cull default from the readings and the pop-in report: off / bodies 2 /
   bodies 4 / all 2. Coarsest-LOD forcing stays the fallback for pop-in.
3. C: shimmer gone at the solar plant (both legs) and the distant object? Read
   `unmatched=` reasons on the F8 frames; `overlay_node`/`overlay_refused` also
   count ordinary blended draws that were never overlay candidates.
4. Then lever 1 stage A + 2a of [route-per-draw-cost.md](architecture/route-per-draw-cost.md)
   (ratified 2026-09-19; Fable implement, fixtures listed in the note).

## Open, lower priority

- Collide: P2 margin under 24-bit x87 precision control; partial-install rollback
  fixture; `fix_[16]` bound; verifier's interior-branch scan is function-wide
  (sector-collide.md §10.1). The collide verifier's claim scan does not list
  the cull-small-parts site (no overlap).
- Fade route: no fixture for Reset between a routed draw and its overlay; no
  host test of `origin_distance_front` (covered by the pool-hull case).
- Shadows: ±0.2 u along-ray receiver offset; own-ship C0 re-roll; retention age
  cap; `sort_us` hook site; `--original-fill` value flight.
- Telemetry gaps from run 42: no `loading_phase` marker on a second save load;
  no self-cost field on `frame_phases`/`motion_output_frame`.

## Decisions (do not reopen without a reason)

As in the archived late-night handoff, plus: cull scope is an A/B decided by
run 43; per-draw lever order 1+2a, then 3, 2b closed.

## Housekeeping

- Snippet mocks drifted again (capture bloom/device-creation, sun-share latch
  pin): run the full host suite before every candidate, not only three modules.
- `/tmp/x3-bottleX3-run117…132` preserved sessions; `/tmp/x3-candidate-7kxCZ4`
  holds the rejected build. Merged worktrees of this session can be removed;
  62 older worktrees (4.5 GB) predate it.
