# Handoff 2026-09-19

Short current handoff; [status](status.md) is the authoritative summary, the
[goals table](goals.md) the acceptance state, [user-runs.md](verification/user-runs.md)
the run queue. Previous: [archive/handoff-2026-09-18-late-night.md](archive/handoff-2026-09-18-late-night.md).

## Where things stand

- **Installed:** run45 candidate `563a40f5…` from main `96892be7`
  ([build](../verification/results/run45-candidate-build.json),
  [install](../verification/results/run45-candidate-install.json)); rollback run44
  `3caa48a0…` in `/tmp/x3-candidate-YcICrW/rollback`. Install only through
  `python3 tools/manage.py install --dll-source <dll>`.
- **Run 44 is read.** A (run141): the 24 fps area is ONE station-vs-ship pair doing
  ≈ 2.3e5 RAPID OBB node-pair visits/frame at ≈ 114 ns, no contact
  ([sector-collide.md](reverse-engineering/sector-collide.md) §11–12). B (run142–146):
  the global current filter blurs and is rejected; history weight alone does not help.
  C (run147): cull default 2 px scope all gives 478 draws, ≈ 50 fps, no pop-in.
  run148/149: the −0.5 mip bias is not the driver and stays.
- **Shimmer is two problems** ([taa-lattice-crawl.md](architecture/taa-lattice-crawl.md)):
  (1) lattice crawl = roping of ≈ 0.7 px geometry lines 13–19° off the pixel axis, must be
  fixed: masked current-sample filter (replay on run148: presented crawl ×0.58–0.68;
  sharpen exclusion rejected, ×1.14 only), in progress in worktree
  `agent-a54e2a9edd6001810` with a general-vs-plant-specific mask comparison first;
  (2) distant thin-line shimmer, dimming accepted: per-node fade band on the cull
  trampoline, needs a draw-index diagnostic capture and a design first.
- **Run 45 is queued** (§45): A `--collide-sat-sse2` A/B with a collision sanity fly;
  B baseline vs `--taa-thin-clip 0.75 --taa-adaptive-weight 0.97 --taa-alpha-history`.
- **User configuration:** as before, plus `--cull-small-parts 2` scope `all` by default.

## When run 45 is reported

1. A: collide phase with the SSE2 box test on/off (census pairs, visits, triangle tests,
   `narrow_us` net of the census cost); collision sanity. Decide default-on, the
   whole-descent SSE2 rewrite (5–10×) and the parked-ship no-contact memo. Never cap the
   descent or stride the tests.
2. B: the user's verdict on the adaptive weight (still / drifting / distant station;
   ghost trails, blinking lights); choose defaults. Replay oracle:
   `tools/analysis/taa_resolve_replay.py` (within 0.4 codes of the installed resolve).
3. Land the masked filter (Opus review, merge), run46 candidate, one flight: baseline vs
   strength 1 and 2 at the plant with the lattice at the crawling angle, ship drifting.
4. Then problem 2, then lever 3 of [route-per-draw-cost.md](architecture/route-per-draw-cost.md)
   and a fresh phase attribution at ≈ 50 fps.

## Open, lower priority

- Pre-existing on main: `verify_ownership.py` pins 553 vs 563; `run_ownership_integration_fallback.py` stub does not link (now four more exports missing); `run_application_admission_abi.py` wants a missing `-initial-summary.json`; shader generator full `--check` stale for ao_*/hdr_*/taa_sharpen/quad_vertex/rigid_motion; `cull-small-parts-cpu.json` still records `scope=bodies`.
- Lever 1 follow-up: a seam-ownership case with a Reset and a simulated DEVICENOTRESET.
- Collide: P2 margin under 24-bit x87 precision control; partial-install rollback
  fixture; `fix_[16]` bound; verifier's interior-branch scan is function-wide
  (sector-collide.md §10.1). The collide verifier's claim scan does not list
  the cull-small-parts site (no overlap).
- Fade route: no fixture for Reset between a routed draw and its overlay; no
  host test of `origin_distance_front` (covered by the pool-hull case).
- Shadows: ±0.2 u along-ray receiver offset; own-ship C0 re-roll; retention age
  cap; `sort_us` hook site; `--original-fill` value flight.
- Telemetry gaps from run 42: no `loading_phase` marker on a second save load;
  no self-cost field on `frame_phases`/`motion_output_frame`.

## Decisions (do not reopen without a reason)

As in the archived late-night handoff, plus: cull default 2 px scope all; mip bias
−0.5 stays; no MSAA, SSAA, global TAA blur, jitter resequencing or luminance clamp;
clustered lighting deferred; per-draw lever order 1+2a, then 3, 2b closed.

## Housekeeping

- Snippet mocks drifted again (capture bloom/device-creation, sun-share latch
  pin): run the full host suite before every candidate, not only three modules.
- `/tmp/x3-bottleX3-run117…132` preserved sessions; `/tmp/x3-candidate-7kxCZ4`
  holds the rejected build. Merged worktrees of this session can be removed;
  62 older worktrees (4.5 GB) predate it.
