# Resume handoff — 2026-09-14 (late evening, autonomous session while the user was away)

Written for the next orchestrator session. Read this file, `AGENTS.md`, `CLAUDE.md`,
`docs/status.md`, `docs/goals.md`, then `git log --oneline -80`, then only the owning notes
named per task. `archive/handoff-2026-09-14c.md` (afternoon) is historical; everything it listed
is merged, run or superseded.

## Installed build and bottle

- Installed DLL: `ab6e17bacbb525bc4f80fb6361498133cbf377704c9ff2f1fe309ab4a9395262` (14,209,414
  bytes), a clean rebuild of `5d06316`; record `verification/results/ao-stepe-install.json`.
  Rollback: `5b92484a…` (`066e18f`) DLL + bottle manifest + record under
  `/var/folders/l6/…/T/x3-ao-stepe2-candidate-9JQx/rollback-*`; older `608b35d8…` (`76d7750`)
  under `…/x3-voice-hook-candidate-H2e1/`.
- Launcher ownership manifest `drive_c/X3/x3-modern-install.json` names the installed hash; a raw
  DLL copy without it makes the launcher refuse ("Install the proxy before launch").
- EXE `fdbf3418…` and `cxbottle.conf` `cc5d6c00…` unchanged. Voice plugin in use:
  `/tmp/x3-wma-plugin-v4` (backup `~/x3-mod-resume-2026-09-14/artifacts/wma-plugin-v4/`).
- Builds are not byte-reproducible (PE timestamp); fixture `--build` steps can overwrite a
  candidate DLL: re-hash and re-audit (x87, exports) the exact bytes before install.

## Decisions taken today (do not reopen)

Fade route default-on (launcher); AO fixture cost accepted, step 2 merged default-off; no
screen-emission bracket cap (bound is the cost driver); step E composition contract (accumulate
native B, decode once, gain default 1 = presented-frame parity); cutout-miss exemption for exact
source-over blended draws (shimmer root cause); specular anti-aliasing NOT ratified (port
brightens with distance in the detached measurement).

## Resolved today

Run 13 crash (devirtualised pure-virtual call in the DMO hook → C vtable, fault witness,
hook-executing replica); voice crackle (stock converter wrap → plugin v4 float limit); selection
pause gone (no publication over 10 ms, runs 16/18); bolts nearly invisible under
`--screen-emission` (per-fragment decode law → step E); AO post-Reset re-attach (hysteresis
cleared at Reset); host suite (1756 OK) and motion-output live suite (100 + 26) repaired.

## Pending at handoff

1. **User run 19** (`docs/verification/user-runs.md` §19, one combined run): shimmer check,
   same-port far/near F8 pair, bolts at gain 1, AO off/on with Ctrl+Shift+F11 and frame rate.
   Analyse per the entry; optional second run with `--screen-emission-gain 2`.
2. **Step D** (per-draw bullet vertex hull, `docs/architecture/screen-emission-bullet-bound.md`)
   in implementation on Fable (worktree); review, merge, next candidate.
3. **Docking-port darkening**: no owner. Minification refuted by
   `verification/probe/run_port_distance.py` (real pair + textures: brightens when lit);
   remaining candidates cube term, LOD/subset at range, lighting/fog path; the run-19 same-port
   pair is the evidence.
4. **AO v2** inputs settled by RE (`camera-state-and-frame-routine.md` round 2: no ambient term
   exists; sun direction world-space per node; zn 6 / zf 2e6); directional variant possible after
   the run-19 cost/appearance verdict.
5. Minor: exposure meter self-test refuses only under `X3M_HDR_DECODE=none`; `frame_end dt_ms`
   is a ~300-frame window total in ms (docs corrected).

## Process notes

Review-fix commits that touch fixture-covered behaviour must rerun the live fixture (the AO
Reset defect slipped through a "not rerun, toggle twin unchanged" commit and was caught only by
the candidate qualification). Concurrent worktrees on `motion_output.cpp` produced additive
conflicts three times; resolve additively and rerun the affected live fixtures on the merged tree.
