# Handoff 2026-09-17

Short current handoff for the next session; [status](status.md) stays the
authoritative summary and the [goals table](goals.md) the acceptance state.
Previous handoff: [archive/handoff-2026-09-16.md](archive/handoff-2026-09-16.md).

## Where things stand

- **Installed:** run34 candidate `7102a2f1…` from main `ee5a406`
  ([build](../verification/results/run34-candidate-build.json),
  [install](../verification/results/run34-candidate-install.json)); rollback
  run33 `03c0c9f4…` in `/tmp/x3-candidate-uemMxM/rollback`. Every session log
  opens with `proxy_identity` / `proxy_options`, then `clock_anchor` and
  `loaded_module` (D3D9 backend, `d3dx9_37`); Wine's stderr is teed into the
  session directory as `launcher-stderr.log` (UTC-prefixed) and preserved.
- **User configuration:** original hull shading (no linear materials).
  Defaults on main and installed: camera 0.5°/0.50, EV ceiling +1.3, mip bias
  -0.5, sharpen 0.75, fill 0.05 (linear only), bolts
  `--screen-emission-additive 2 --screen-emission-additive-alpha 0`, engines
  `--emission-source-gain 2`, `--bloom-source-clamp 1.0`, launcher
  `--state-shadow auto` (SetRenderState/SetSamplerState unhooked in
  production) and `--media-cue-cache on` (retry 30 s). Decoder runtime
  `--voice-decoder /tmp/x3-wma-plugin-v4` (v5 parked, see below).
- **Bottle setting to check first:** `cxbottle.conf` still carries
  `"CX_GRAPHICS_BACKEND" = "dxvk"` from run 34 session B; DXVK's D3D9 renders
  black on this CrossOver Preview (MoltenVK refuses DXVK's dual texture
  binding). Switch the bottle back to the default backend before any run; the
  install record's `cxbottle.conf` hash will differ until then.
- **Runs 28 to 34 are complete** (run74–81, run83–85, run87–102); outcomes in
  [status](status.md) and the ledgers. No run is queued.

## Results this session (2026-09-17)

1. **Busy-sector frame:** 37.5 ms (run89, hooks and frame timing on) to
   26.5 ms (run92) after the light CPU envelope, the setter dispatch trim and
   the hybrid unhook (the proxy is out of the state path; `state-call-fast-path.md`).
   run95 split the remaining 20.1 ms of view submission per draw: Wine's draw
   call 8.7 µs, D3DX BeginPass 6.7 µs, engine work between passes 4.6 µs,
   EndPass 0.1 µs (`--pass-phases`). Counters ruled out an engine-side state
   filter (≤ 1 ms, `engine-state-filter.md`) and proxy instancing/sorting
   (5 %/6 % candidates). Ratified next steps in
   [effect-pass-replay.md](architecture/effect-pass-replay.md): bottle
   experiments first (DXVK: broken on this CrossOver; D3DX: the **native**
   `d3dx9_37` loads, so the remaining no-code test is forcing Wine's builtin),
   then host prerequisites (compiled-effect state classification in
   `effect_passes.py`, two residual stamps), then the pass-replay
   `ID3DXEffect` wrapper at the EXE's `D3DXCreateEffect` import (3–5 ms bound)
   only if still wanted.
2. **Quiet-sector stall solved:** ~390 ms frames were the per-sector
   media-cue selector `0x0045b720` restarting cue id 2 (`mov\00002.dat`, a
   533 MB MPEG-1 video ES) every frame while its DirectShow graph build failed
   (no MPEG video decoder under Wine; same retry on Windows with a missing
   codec). Diagnosed with `--frame-phases` → `--game-phases` →
   `--loop-phases` → `--media-cue-trace` (run93–98); bounded by the negative
   cache at `0x00498140` (run99: one real attempt per 30 s, sector at 7–9 ms,
   **user confirmed**), launcher default on. Notes:
   [sector-post-pass.md](reverse-engineering/sector-post-pass.md),
   [media-cue-playback.md](reverse-engineering/media-cue-playback.md), ledger
   [media-cues.md](verification/media-cues.md). The v5 decoder runtime
   (`/tmp/x3-wma-plugin-v5`, adds MPEG decoders) makes the graphs build and
   then the first comm dialog hangs in Wine's video path (run100/101): parked;
   recipe kept in `voice-decoder-recipe.md`.
3. **Shadow lane:** available on every frame under linear materials; the two
   cutout pairs draw ~90–108 per frame in the busy Argon view and are admitted
   through the tested-opaque arm with the lane share written
   (`cutout_opaque_*`, run97); depth replay ~40 µs, no consumer; sun
   application contract ratified (`legacy-sun-application.md`). No shadow
   applied. Linear materials + lane cost about +3 ms in the busy view.
4. **Emitters and halo:** single `--emission-source-gain` (engines respond),
   `--bloom-source-clamp 1.0` with additive alpha 0 accepted (run87). Emitter
   plan phase 3 (position lights, signs on hull programs) not started.
5. **Tooling:** stamp groups `--frame-phases`, `--pass-phases`,
   `--loop-phases`, `--game-phases`, gate `--media-cue-trace/-cache` (shared
   lean stub, arena 20,480 B, ~630 B free before the media gate; grow it for
   another group); `--frame-timing` count-only with redundancy, program-pair
   and batchability counters; `run_state_hook_benchmark.py --dll`; host suite
   green (1,993 tests) at `ee5a406`+.

## Next steps, in the order recommended

1. Switch the bottle's graphics backend back from DXVK (user).
2. **Shadows:** the original-program share producer and a lane latch without
   the linear-material prerequisite, then the per-seed sun application
   (`legacy-sun-application.md`); Fable implementation with the sun-share and
   motion-output fixtures; awaiting the user's go since run 30.
3. **Busy frame:** the D3DX builtin-vs-native experiment (bottle DLL override
   `d3dx9_37=builtin`, one busy-view session with `--pass-phases`), then the
   two host prerequisites of the pass replay; the replay wrapper only if the
   user still wants the 3–5 ms. Attribution stamps for the engine's 4.5 ms
   between passes and the ~6 ms outside submission need the arena grown.
4. **Media cues:** add an entry-side `media_cue_enter` line to the trace so a
   hung build is attributable; the v5 video path only if avatar/sector videos
   are wanted (Wine quartz video sink work, out of scope so far).
5. Emitter plan phase 3; structural fingerprint fallback for mods
   (`mod-compatibility.md`).

## Decisions (do not reopen without a reason)

- Linear hulls optional and off; no blanket conversion, PBR, selective
  exposure or code-value fill; emitters above 1.0 via source gains.
- Proxy over engine trampolines for rendering; trampolines only for
  engine-side fixes (frame/pass/loop stamps, media-cue gate).
- No engine-side state filter; no proxy instancing or draw sorting.
- Media-cue negative cache default on; v5 runtime parked.
- Point-light root admission default-off and out of the run command.
- Three concurrent agents unless the user raises it.

## Housekeeping

- The five extracted-snippet mocks (`linear_material_live`,
  `motion_wrap_states`, `capture_bloom_lifetime`, `motion_hdr_scene`,
  `linear_cutout_contract`) plus `capture_device_creation` drift at every
  feature commit that adds members; run them with each source commit.
- Motion-output runner pins fill 0 / EV max 1.5 on the cases whose record
  predates `3df7b9f`; regenerate the record at production defaults if that
  coverage is wanted.
- Finished agent worktrees under `.claude/worktrees/` can be pruned;
  `/tmp/x3-candidate-*` keep the rollback DLLs; `/tmp/x3-bottleX3-run*` are
  the preserved sessions (run98–102 include `launcher-stderr.log`).
