# Resume handoff — compaction checkpoint, 2026-09-14 (evening)

Written for the next orchestrator session after a context compaction. Read this
file, `AGENTS.md`, `CLAUDE.md`, `docs/status.md`, `docs/goals.md`, then only the
owning notes named per task. The morning handoff `docs/handoff-2026-09-14.md`
is now historical (moved to `docs/archive/`); all three of its tasks progressed
and their state is superseded by this file.

## Installed build and bottle

- Installed DLL is unchanged: checkpoint `8442f43`, SHA-256
  `1f6a473dd035894bb868adc113be8f76c7dfde4731157595def614d0411a42ba`. A
  one-session diagnostic DLL (`diag/voice-hang-witness`, `369776f` =
  `8442f43` + launcher `--voice-decoder` + witness hooks) was installed for
  run 36 and then restored; the previous DLL and record are backed up under
  `/Users/asvetl/x3-mod-resume-2026-09-14/artifacts/installed-8442f43/`.
- Bottle X3 is byte-identical to its pre-experiment state after the WMP11
  attempt and full revert (`docs/verification/bottles.md`). Do not repeat the
  native-codec route: it breaks the DirectShow path the game uses.
- Current main is far ahead of the installed build (cutout runtime, fade
  prototypes, region steps, witnesses, voice tooling), all default-off or
  fixture-only. **Do not install main automatically**; a new install needs the
  candidate procedure in `AGENTS.md` and a user decision.

## Main branch state (all merged, reviewed, committed)

| Area | Commits | Evidence |
| --- | --- | --- |
| Fade prototype 1 / 1b (composite drops the B sample; policy-aware binding) | `d8e189b`, `3c3479a` | detached 71 cases / live 15 proc / 302 frames / 140 TAA; 1080p 16 DIPs 7.64–8.46 → 6.61–7.11 ms |
| Render-node AABB and buffer-rewrite RE | `8f7e611`, `c978089` | `docs/reverse-engineering/render-node-bounds.md`, `mesh-buffer-rewrite.md` |
| Fade region design (ratified) | `67baa55` | `docs/architecture/linear-distance-fade-region.md` |
| Region step 1 (bound derivation) | `4bcedea` | 29 region cases, 0 violations |
| Region live witness (`--fade-witness`) | `97a0aa8` | 0 outside pixels, negative control fails on 16/18/20/24/28 |
| Region step 2 (in-place bracket in the pass) | `e2eccfb` | 586 bit-exact twins; 1080p 16 DIPs f=0.06 7.58 → 1.93 ms |
| Two-pair alpha-tested cutout runtime | `b9ae8dd` | x87 audit 223; live 13 proc / 531 frames / 2,111,274 checks |
| Voice decoder adapter: docs, recipe, launcher `--voice-decoder`, real-file probe, timing correction v3, startup replica, startup/cue-timing RE, load-hang witness | `3733531` … `0042ce5` | `docs/architecture/voice-decoder-adapter.md` and linked notes |

Large live results are kept local with copies in the resume backup
`artifacts/` (`linear-cutout-live.json`, `linear-distance-fade-live-proto1b.json`);
the tracked JSONs are the compact ones.

## Pending at compaction

1. **Fade region step 3 (runtime route)** — merged on main (`2f6ed17`,
   merge `42d3197`) after review: admitted fade draws use the in-place policy
   with the step-1 rectangle, exchange as fail-closed fallback; 48 host
   tests, x87 PASS 223, live PASS 26 processes / 536 frames / 224 TAA
   readbacks, 1080p 16 DIPs ≈2.4–3.3 ms vs ≈8.0 ms for prototype 1 in the
   same run; per-bracket floor ≈0.08–0.16 ms at 16 DIPs. The option stays
   default-off and nothing is installed. Accepted risk: in-place correctness
   depends on the step-1 rectangle being conservative; the fixture witness and
   the `--fade-witness` capture are the only detectors. Next: the
   performance decision is now a user-run question. Build one candidate from
   main per the `AGENTS.md` install procedure only after a user decision, and
   queue one combined enhanced run with `--linear-distance-fade
   --fade-witness` plus the cutout option (see item 3); read the witness
   lines (zero outside pixels required) and the f histogram before any
   acceptance claim.
2. **Voice load hang with the plugin** — the v3 plugin
   (`/tmp/x3-wma-plugin-v3`, backup `artifacts/wma-plugin-v3`) decodes both
   voice files with exact cue timestamps in fixtures, but the game hangs on
   the loading screen when launched with `--voice-decoder`. Run 36 with the
   witness build produced `/tmp/x3-diag-witness-session.log` (copy of the
   session log): `game_phase_audio` shows the first stream's
   `SetState(RUN)` returned `E_FAIL` (`last_setstate_hr=80004005`,
   `setstate_after=1`, `pause_after=0`, `pump_entry=1`, `pump_body=0`,
   `manager_asset=524`), and 65 `profile_raw*` dumps. Triage of that log:
   every audio counter freezes after the single failed `SetState`
   (`pause_after`, `pump_body`, `refill_entry`, `poll`, `update`, `cue_play`
   all stay 0); per `voice-startup-sequence.md` the constructor `0x004cf460`
   destroys the object via `0x004d1d40` on that failure; the init/main thread
   (tid 216) then samples 100 % busy with its only raw stack inside
   `combase.dll` frames and EIP at Wine's syscall placeholder, i.e. a COM
   cross-apartment wait pumping messages, while the run-31/32 macOS samples
   showed the decoder thread blocked in `winegstreamer.so` pushing a buffer.
   Working hypothesis (evidence, not proof): graph teardown after the failed
   `SetState(RUN)` waits for a streaming thread that is parked in
   wg_parser's sink push, a deadlock the fast-failing decoder never reached.
   Root cause to settle first: why `SetState(RUN)` returns `E_FAIL` in the
   game but succeeds in `verification/probe/voice_startup_replica.cpp`
   (difference candidates: the game's DirectSound device passed at
   Initialize/AddMediaStream, the DSOUND thread tid 348 never sampled, exact
   flags). Next tasks: (a) Fable `implement` to make the replica use the
   game's actual DirectSound initialisation from the RE note and reproduce
   the `E_FAIL`; (b) if it reproduces, iterate on the plugin/pipeline side
   (never CrossOver/bottle) until `SetState(RUN)` succeeds, then a user run;
   (c) if it does not, a second witness run with `profile_raw` forced on the
   DSOUND thread and markers on the five main-loop calls the triage names.
   Never launch the game.
3. **Cutout runtime** (the morning handoff's "alpha-tested materials"):
   merged (`b9ae8dd`), not installed; owning note
   `docs/architecture/alpha-tested-materials.md`. Its gameplay evaluation
   should ride the next combined enhanced user run together with the fade
   region option once step 3 is merged, not a run of its own; the run needs
   the cutout option on, the F8 capture over an Argon station approach, and
   the user's report on the docking-port darkening, which this runtime is not
   expected to fix.

## Standing decisions carried from the morning handoff

- Persistent backup outside Git: `/Users/asvetl/x3-mod-resume-2026-09-14/`
  (README, task handoffs, `artifacts/`). Its `cutout-source/`, fade and
  decoder handoffs are superseded by the merges above; keep the directory for
  provenance and for the large local results and plugin builds.
- Run 28 (user run 9, `/tmp/x3-bottleX3-run28/`) is still the latest
  gameplay evidence; save it before any new run. Runs 29–36 hold no gameplay
  evidence.
- Fade performance: never hide cost by capping draws or mixing composition
  policies partway through a frame; batching by historical adjacency is not
  approved. The region route composes each admitted draw fully, so it does not
  violate this; unknown bounds fall back to the full viewport.
- Cutout: the two-pair runtime does not prove all cutouts and does not explain
  the docking-port darkening; near-coplanar and changing-alpha metrics
  (frames 65–69 of the live fixture) need honest evaluation, not automatic
  appearance approval. Nonzero mip bias remains refused for the arm.
- Voice: the optional CrossOver adapter is not a Windows prerequisite; the
  documented COM path stays for native Windows. No custom DirectShow
  source/filter and no production game hook for audio is approved; the
  witness hooks are diagnostic only. Decoder success alone is not restored
  speech or selection latency; only a user run decides that. Negative caching
  of failed streams would hide speech and stays rejected.
- The full modernization objective (`docs/user-objective.md`, `docs/goals.md`)
  remains binding and incomplete: HDR display, whole-scene linear lighting,
  AO, shadows, reflections, particles, volumetrics, lens effects, clustered
  lighting, window/cursor.

## Worktrees and branches

- Keep: `/tmp/x3-diag-witness` (`diag/voice-hang-witness`). All
  `worktree-agent-*` branches under `.claude/worktrees/` are merged into main and
  can be pruned (`git worktree remove`, `git branch -d`). Cutout worktrees
  `/tmp/x3-cutout-runtime`, `/tmp/x3-cutout-main`, `/tmp/x3-cutout-fix` are
  merged (branches `qualification/linear-cutout-*`) and can be removed.
- Older `/tmp/x3-*` worktrees predate this session; leave them.

## Process notes for the next session

- Routing that worked: Fable `implement` for hook/GPU/lifetime work, Opus
  `implement` for tooling/docs, Opus `review` per logical change with a Fable
  second review only for the cutout hooks, Sonnet `triage`/`verify` for logs
  and fixture runs, Opus-high `disassemble` for RE, Fable `design` for the two
  design notes. Every agent report was held to the 25-line contract.
- Only one Wine command at a time through `wine_lock.py`; the game and agent
  fixtures never overlapped (agents wait while the game is up).
- Run numbering: runs 29–36 were the voice-decoder hang attempts (no
  gameplay evidence); run 28 remains the latest gameplay evidence.
