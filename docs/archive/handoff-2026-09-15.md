# Resume handoff — 2026-09-15

Read `AGENTS.md`, [status](../status.md), [goals](../goals.md), the
[run queue](../verification/user-runs.md), then the owning note for the task.
[Status](../status.md) is the only installed-build description; its install record
also retains the previous rollback identity. The superseded midday handoff is
preserved verbatim in [the archive](handoff-2026-09-15-midday.md).

## Checkpoint

- Material fill fixes, detached/live GPU evidence and the reviewed clean Sol
  build are complete. The candidate is installed; Run 23 is received as snapshot run54, with fill
  the user now choosing fill 0.03 and Auto EV ceiling +1.0 as defaults.
  The default-only source/launcher change is reviewed and ready for the next DLL;
  unmatched colour and reprojection evidence still limit run54 acceptance.
- The user reports that fill works and asks about exposure and a 0.04 bracket.
  Run24 gate telemetry is received as run56; jumpdrive was not run because
  the user has no suitable save. Never launch the game.
- HUD anchor source is committed at `da2f17b`, separately from the fill
  candidate. Its candidate integration waits for the appropriate checkpoint.
- Sun-share source artifact `0234178` is host-qualified only: no GPU or live
  integration claim.

## Ratified decisions

- AO remains default-off; no AO v2. `--lod-scale` remains default-off.
- Bolts at gain 1 and the fade-band arm are accepted. The double cursor after
  alt-tab is vanilla.
- The station darkness is caused by per-node player-headlight admission with no
  ambient term. Fill is the chosen first response; the point-light-range patch
  is rejected.
- Shadow route B stays ordered: fill, sun-lit-share lane, replay feasibility,
  then cascades. Screen-space shadows are fallback only if replay fails.

## Pending order

1. Include the reviewed fill 0.03 / Auto EV ceiling +1.0 fallback change in
   the next clean candidate; launcher defaults already apply. The user-approved
   base-hull EV0 / exposed-highlight split is under implementation design. Run24/run56 proves gate cockpit recreation
   and mode-1 script writes at `0x000f0794`. The same-lifetime restore proposal
   would cancel. Consolidated identity/provenance/geometry diagnostics are now
   reviewed and CPU-qualified in source; install them with the next candidate
   and obtain the missing continuity evidence before restoration implementation.
   Jumpdrive remains unverified; the user has no suitable save.
2. Integrate the HUD-anchor candidate after the appropriate checkpoint.
3. The ordinary-material sanitizer constant-port repair is reviewed and
   GPU-parity qualified in source; include it in the next candidate. Native
   Windows runtime remains unverified.
4. Qualify the live G32 sun-share lane in `/tmp/x3-sun-share-runtime`.
5. Continue replay feasibility and then cascades only after that lane is live.
6. Consider optional root-object point-light admission only after the fill
   verdict. It is not the rejected range/falloff patch.
7. Continue loading attribution: 21.702 s is bounded, but overlapping timers
   leave residual wall-time attribution unknown.

## Active worktrees

- `/tmp/x3-material-constant-port` — frozen qualified sanitizer fixture inputs.
- `/tmp/x3-sun-share-runtime` — active live G32 lane work.

Do not redo either task in main. Keep one Wine queue owner; no fixture or
install work may overlap an active game session. Use the lock and check process
state before resuming fixtures. Run24 evidence gates view restoration.
