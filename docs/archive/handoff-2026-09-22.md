# Handoff 2026-09-22 — Run64 installed, Run 64 in flight

Replaces the [2026-09-21 night handoff](handoff-2026-09-21-night.md); its
operating rules still bind (never launch the game; every Wine command through
`X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py`; one candidate owner;
install only with `tools/manage.py install --bottle X3 --dll-source <dll>`; native
Windows is a required, unverified target). Installed-build identity lives only in
[status](../status.md). Main is clean at `3925cd75`. No agent, Wine process or build is
running. Run authority: [user-runs.md](../verification/user-runs.md).

## State

- **Installed:** Run64 DLL `e839dc7c…` from `6837a968`
  ([qualification](../../verification/results/run64-candidate-qualification.json),
  [install](../../verification/results/run64-candidate-install.json)). Rollback
  Run63 `/tmp/x3-run63-candidate/build/d3d9.dll`, then Run62, Run61, Run60, Run59, Run56.
- **Run 64 in flight**, three sessions (commands in user-runs.md): A sun occlusion
  (`--sun-occlusion --sun-occlusion-log`), B thin glow lines
  (`--hull-emissive-widening 3,2,8`), C stored-fog regression. Before compaction the
  user had started reporting session A (run228, plus run229 with
  `X3M_SUN_OCCLUSION_CORE_F=1`): "sun is working now"; three observations to triage:
  (1) visible shimmer at the place where geometry occludes the sun; (2) a black smear
  when flying fast toward a station (SETA), visible against bright nebula, unknown
  whether new; (3) CORE_F=1 showed no visible difference. Five F8 bursts: sun fully
  visible, partially occluded by station, fully occluded by station, fully occluded by
  a stationary ship, flying toward the station (F8 cancelled SETA). The user will
  repost the full report after compaction; wait for it, then triage run228 with the
  new `lens_*` back-buffer dumps and `sun_probe`/`sun_lens_draw`/`sun_visibility` log
  lines (the ledger's flight checklist in [sun-occlusion.md](../verification/sun-occlusion.md)).

## Accepted since the previous handoff (all on main, all flown)

1. Solar-plant lattice crawl: sentinel-depth panel glass no longer votes in the camera
   gate (§32.5–§32.6 of [taa-lattice-crawl.md](../architecture/taa-lattice-crawl.md));
   thin-region weight stays **0.97** by user decision; approach flash closed with
   `--taa-unmatched-static node` as default.
2. Distant-station pan flicker: far-plane `expectedDepth` kept below 1 (z row scaled by
   1−2⁻¹⁶) + sentinel stabiliser, now **default 0.7** with TAA and the camera gate
   ([temporal-resolve.md](../verification/temporal-resolve.md)).
3. Stored fog look: accepted "very good, beautiful"; L2 default at 1.0x
   (`--volumetric-fog 0.02`), 13→22.5 km fade, oblique 5.35 km coverage waves (ribs
   fixed), shadow-shaft lookup jittered only (comb fixed), caster retention engaged
   (shafts persist) ([volumetric-fog.md](../verification/volumetric-fog.md),
   [direction review](../architecture/fog-visual-direction-review-2026-09-21.md)).
4. Shadow caster retention never worked in flight before: exact inverse of the 16.16
   view basis at every world-recovery site ([directional-shadows.md](../verification/directional-shadows.md)).
   Retention risks (stale shadows, 640-issue budget) are to be read from
   `shadow_retention_summary` / resight lines in the next logs.
5. Tooling: `verification/probe/run_host_suite.py` (parallel, ~90 s; `--include-retired`)
   is the canonical suite; linear-material/emission-bracket tests retired from default
   discovery ([host-suite.md](../verification/host-suite.md)). Rule kept: full suite +
   production build + `check_no_x87.py` after every production merge.
6. Cleanup batches 1, 2 and 7 done (−~14,500 lines: dead launcher options, media
   residue, the lattice capture diagnostic, `rpm` engine reads). Verdicts recorded in
   [cleanup-inventory-2026-09-22.md](../architecture/cleanup-inventory-2026-09-22.md):
   keep `--point-light-root-admission` and `--taa-alpha-history`; remaining batches
   (lod-scale, AO chain, dead TAA variants, retired tests, managed-upload helpers) not
   started.

## In flight / default-off, awaiting the Run 64 verdict

- **Sun occlusion** (steps 1+2): override eligible on the main view's re-probe of the
  background-owned sun record; absolute radius `--sun-occlusion-radius` (default
  0.04 u); core disc clipped per pixel against RT2 with a 4-px soft edge, glare scaled
  by the visible fraction; `X3M_SUN_OCCLUSION_CORE_F=1` also dims the clipped disc.
  Flight checklist and open questions (uv vs picture, radius vs glow, body classes,
  second 0x400000 view → `multi_record_frames`) in [sun-occlusion.md](../verification/sun-occlusion.md).
  The reported occlusion-edge shimmer is the first thing to look at: candidates are the
  9-tap clip edge under TAA jitter (lens draws are post-resolve so TAA cannot smooth
  them), the per-frame smoothed f, or RT2 depth aliasing at the silhouette.
- **Hull emissive widening** (`--hull-emissive-widening K,Q0,Q1`): all 100 gained SM3
  light-map programs; fixture floor 0.50→0.87 at K=3; unverified in flight: TAA-on
  flicker, real DXT1 mip normalisation, the Q band
  ([hull-emissive-widening.md](../architecture/hull-emissive-widening.md),
  [thin-glow-lines.md](../architecture/thin-glow-lines.md); run225/run227 evidence).

## Next steps

- Triage run228/run229 (sun) when the user reposts; then session B (glow lines) and C.
- Fog presets collapsed to one look (the L2 law, env tuning kept): **done 2026-09-22**, bit-identical
  (`docs/verification/volumetric-fog.md`, "Single look").
- Then: near-camera dust
  motes; shadow lookup in its own fog pass (cascade cross-fade, finest cascade,
  penumbra); a GPU timer for fog cost; remaining cleanup batches; fix
  `run_motion_output.py`'s fresh-worktree build (missing `-DPython3_EXECUTABLE`,
  `verification/probe/build/`); `generate_bloom_programs.py` `--d3dx` default still
  points at the Steam bottle.
- Known one-off: a Wine fixture (fog in Run63, sun in Run64) stalled at 0 % CPU once
  and passed on retry; cause unknown.

## Worktrees

`/tmp/x3-lattice-capture-lifecycle` and `/tmp/x3-lattice-payload-writer` hold the
uncommitted lattice diagnostic work that is now deleted from main: archive their
diffs to a git branch before removing them (not done). `/tmp/x3-fog-finite-banks` may
go (its two files are on main). Twenty-eight `.claude/worktrees/agent-*` trees are
fully merged and can be removed with `git worktree remove`; nothing pending in them.
The `/tmp/x3-run5x/6x-candidate` directories hold the retained DLLs.
