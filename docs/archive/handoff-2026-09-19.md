# Handoff 2026-09-19

Short current handoff; [status](../status.md) is the authoritative summary, the
[goals table](../goals.md) the acceptance state, [user-runs.md](../verification/user-runs.md)
the run queue. Previous: [archive/handoff-2026-09-18-late-night.md](handoff-2026-09-18-late-night.md).

## Where things stand

- **Installed:** run47 candidate `95d6306b…` from main `0c55c55b`
  ([build](../../verification/results/run47-candidate-build.json),
  [install](../../verification/results/run47-candidate-install.json)); rollback run46
  `fc3dd232…` in `/tmp/x3-candidate-Fk7EJk/rollback`. Install only through
  `python3 tools/manage.py install --dll-source <dll>`.
- **Run 46 is read** (run table). Collision: SSE2 box test + memo are defaults (24 → ≈ 43 fps
  in the hot spot; verify 0 mismatches); the memo was relaxed on the running minimum for
  no-leaf entries, unflown. Lattice crawl: resolve-side spatial filters exhausted
  ([taa-lattice-crawl.md](../architecture/taa-lattice-crawl.md) §10); one experiment left
  (line filter + history 0.97, 64 frames), else a source-side fade. Distant lines: far
  stabiliser weight-only works; blur under slow camera motion addressed by the speed gate.
- **Run 47 is queued** (§47): A memo verify + defaults FPS; B perdraw / lazy / lazy+profiler
  at the busy station; C far stabiliser with the new gate (two alternates spelled out);
  D lattice 64-frame pair.
- **Designs waiting for the user:** volumetric fog (mock-ups `build/fog-mock/*_sheet.png`;
  the usual green sector has NO fog, 35 sectors do: capture wanted in Atreus' Clouds,
  Great Reef or Paranid Prime). SSR/flare occlusion/bloom-depth/DOF closed.
- **User configuration:** as before, plus cull 2 px scope all and both collision options by default.

## When run 47 is reported

1. A: `collide_memo` row: `verify_mismatches` 0; `min_relaxed_hits`, `miss_*` classes with
   visits → if `xform_a/b` dominate, the rest is genuinely moving pairs (then the 66 vs 31 ns
   per-visit gap is the only collide lever left).
2. B: FPS perdraw vs lazy; `lazy_flushes`, `set_rt`, `lazy_mask_writes`, `restore_failures`;
   no `what=lazy_flush` line; if clean make lazy the default. B3: profiler leaves vs the
   candidates R1–R8 of [view-submit-hot-path.md](../reverse-engineering/view-submit-hot-path.md) §6
   (x3ap vs d3dx leaf share first).
3. C: the user's gate pick → default for `--taa-far-stabiliser`? (it is default-off today).
4. D: replay the 64-frame pair with the creep metric; decide line-gated history vs source fade.
5. Housekeeping: re-pin stale `motion-output-summary.json` and `collide-memo-cpu.json`;
   the AO fixture once hung after PASS (builtin d3d9, not the candidate).

## Open, lower priority

- Pre-existing on main: `verify_ownership.py` pins 553 vs 563; `run_ownership_integration_fallback.py` stub does not link (now four more exports missing); `run_application_admission_abi.py` wants a missing `-initial-summary.json`; shader generator full `--check` stale for ao_*/hdr_*/taa_sharpen/quad_vertex/rigid_motion; `cull-small-parts-cpu.json` still records `scope=bodies`.
- Lever 1 follow-up: a seam-ownership case with a Reset and a simulated DEVICENOTRESET.
- Collide: P2 margin under 24-bit x87 precision control; partial-install rollback
  fixture; `fix_[16]` bound; verifier's interior-branch scan is function-wide
  (sector-collide.md §10.1). The collide verifier's claim scan does not list
  the cull-small-parts site (no overlap).
- Fade route: no fixture for Reset between a routed draw and its overlay; no
  host test of `origin_distance_front` (covered by the pool-hull case).
- Shadows: ±0.2 u along-ray receiver offset; own-ship C0 re-roll; retention age
  cap; `sort_us` hook site; `--original-fill` value flight.
- Telemetry gaps from run 42: no `loading_phase` marker on a second save load;
  no self-cost field on `frame_phases`/`motion_output_frame`.

## Decisions (do not reopen without a reason)

As in the archived late-night handoff, plus: cull default 2 px scope all; mip bias
−0.5 stays; no MSAA, SSAA, global TAA blur, jitter resequencing or luminance clamp;
clustered lighting deferred; per-draw lever order 1+2a, then 3, 2b closed.

## Housekeeping

- Snippet mocks drifted again (capture bloom/device-creation, sun-share latch
  pin): run the full host suite before every candidate, not only three modules.
- `/tmp/x3-bottleX3-run117…132` preserved sessions; `/tmp/x3-candidate-7kxCZ4`
  holds the rejected build. Merged worktrees of this session can be removed;
  62 older worktrees (4.5 GB) predate it.
