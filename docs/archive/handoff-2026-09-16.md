# Handoff 2026-09-16

Short current handoff for the next session; [status](../status.md) stays the
authoritative summary and the [goals table](../goals.md) the acceptance state.
Previous handoff: [archive/handoff-2026-09-15.md](handoff-2026-09-15.md).

## Where things stand

- **Installed:** run34 candidate `7102a2f1…` from main `ee5a406`
  ([build](../../verification/results/run34-candidate-build.json),
  [install](../../verification/results/run34-candidate-install.json)); rollback
  run33 `03c0c9f4…` in `/tmp/x3-candidate-uemMxM/rollback`. Every session log
  opens with `proxy_identity` / `proxy_options`.
- **User configuration:** original hull shading (no linear materials). Defaults
  on main and installed: camera 0.5°/0.50, EV ceiling +1.3, mip bias -0.5,
  sharpen 0.75, fill 0.05 (linear materials only). Bolts at
  `--screen-emission-additive 2`, engines at `--emission-source-gain 2`,
  bolt halo held by `--bloom-source-clamp 1.0` with additive alpha 0 (one
  gain over all twenty pairs, `linear-emission-cost.md` "Screen substitution").
- **Runs 28 to 33 are complete** (run74–81, run83–85, run87–97; outcomes in
  [status](../status.md)). **Run 34 is queued**
  ([user-runs](../verification/user-runs.md) §34): the stalling sector with the
  media-cue trace (A1), the negative cache (A2) and the v5 decoder runtime
  (A3); the DXVK experiment in the busy view (B).
  Superseded run 29 text:
  ([user-runs](../verification/user-runs.md) §29): emitter hotkeys F5/F6/F4 at
  gain 5, bolt alpha 0 halo check, gate transits for the pose fix, a busy-scene
  FPS hold with `--frame-timing` (A2 adds `--profile`), session B for sun-lane
  availability.

## Run 30 answers (2026-09-17)

1. Engines respond to the single gain; bolt halo accepted at clamp 1.0 with
   additive alpha 0 (user keeps both).
2. FPS: run87's 28.5 ms busy frame at 457 draws carried ~30k hooked state
   calls; the proxy's share was mostly its own diagnostic timing and the full
   CPU envelope, both replaced on main (light envelope `e8bac89`, dispatch
   trim `4adf3dd`, count-only timing `dcbe43c`); `--frame-phases` (`fcbddb2`)
   attributes the game's remaining time in run 31.
3. Sun lane available on every run88 frame; cutout draws still absent.

## Run 28 answers (2026-09-16 late)

1. Halo: bloom of the >1.0 bolts, not EV or sharpen; bloom bytes = sharpen stage
   buffer. Fix under test: `--screen-emission-additive-alpha 0` (`6c8a375`).
2. Chase: restore consumed; the jump was the unbound-view refusal, fixed `e4fd22a`.
3. Original fill: works; 0.05 preferred by appearance; cost via `--frame-timing`.
4. Sun lane: cutout pairs vetoed under the mip-bias default, fixed `e62722a`.
5. Depth replay 42 µs, no skips; sun application contract ratified
   ([note](../architecture/legacy-sun-application.md)).
6. Point-light admission: unchanged, out of the run command.
7. New: user reports low FPS with many objects; `--frame-timing` (`8954cff`)
   plus `--profile` in run 29.

## Open questions run 28 was to answer (kept for reference)

1. **Bolt halo** (run68, reproduced with the bolt option alone): exposure
   ceiling 1.3 vs sharpen/mip bias; bloom working set differed 21 vs 13 MB
   between run65 and run68, unexplained (resolution?).
2. **Chase restore on return transits**: run68 consumed on transits 1 and 3,
   never re-armed on the fresh generation; fixed (`220d2e9`, bounded retry,
   `chase_view_restore_arm_refused` / `transfer_refused` samples). Read
   `chase_view_restore_state`, `_seam`, `_arm_refused` lines.
3. **Original fill** (`--original-fill`, merged `d864246`): appearance and the
   on/off `frame_end` delta (9 POW per pixel) decide whether it stays.
4. **Sun lane** (session B): frames should report `available=1` after the
   lane-only state-gate fix (`444478a`); `non_depth_writers` absorbs effects.
5. **Depth replay** (`--shadow-replay-depth`, `46dc822`): per-frame `us` and
   `skipped_*`; no consumer yet. Next design step: the legacy-shading sun
   application contract (the lane's attenuation law is linear; original
   shading needs its own law) before any shadow is applied.
6. **Point-light root admission**: kept default-off with telemetry
   (`7521b79`); out of the run command by user decision (cliff barely visible
   under original shading). If ever needed: one F8 pair with the option on.

## Decisions this session (do not reopen without a reason)

- Linear hulls optional and off; no blanket conversion, PBR, per-material art
  review, selective exposure (closed) or code-value fill (rejected).
- Emitters above 1.0 via source gains, not brackets; `--linear-emissions`
  bracket shape not pursued ([cost note](../architecture/linear-emission-cost.md)).
- Goals file is a single status table; execution order lives in status.
- Loading: run65 stall 21.5 s is per-item overhead (752 FileOpen at 4.8 ms,
  4,359 meshes), not one phase ([note](../reverse-engineering/loading-observations.md)).

## Housekeeping

- Host suite is green at `57de1b3` (1926 tests, 2 build-artifact skips). The
  extracted-snippet mocks (`linear_material_live`, `motion_wrap_states`,
  `capture_bloom_lifetime`, `motion_hdr_scene`, `linear_cutout_contract`) drift
  at every feature commit that adds members; run those five modules with each
  source commit and mirror new members inertly in the mocks.
- Finished agent worktrees under `.claude/worktrees/` and the many
  `/tmp/x3-*` worktrees from earlier sessions can be pruned; `/tmp/x3-candidate-*`
  keep the rollback DLLs.
- Runs 61–64 and DLL `3f1b9941…` remain of unrecorded provenance (pre-identity
  logging); nothing to do.
