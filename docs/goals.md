# Goals and acceptance state

**All goals were marked completed on 2026-09-26 by the user's decision** (the project reached its
acceptance state): the accepted build is Run88 `6fe194bd` (accepted in Run 88 A); the installed build is
Run90 `6f6be732` (Run 90 A queued), described in [status](status.md).

This file tracks acceptance state only, one row per goal. The Status column carries the completion; the
Current state column is kept as each goal's history and acceptance evidence at the time it was completed
(earlier states: Planned, Accepted (in game), Installed, In progress, Held, Closed, Rejected, Reopened).
The original scope note (`docs/user-objective.md`) was removed from the tree on 2026-09-26 and remains in
git history. The material scope clarification of 2026-09-15 lives in the
[material allocation assessment](architecture/material-investment.md).

| # | Goal | Status | Current state (history) | Owning note |
|---|---|---|---|---|
| 1 | True HDR | Completed 2026-09-26 (was Closed) | Closed by the user 2026-09-25. HDR display output: the CrossOver D3D9 path has no HDR swap chain and the D3D11 post chain was closed by the probe (2026-09-24), so wide-format output is not reachable here and untestable on native Windows; the FP16 scene, above-1.0 emitters, bloom and exposure stay as installed with the AgX SDR write-back. Scene-referred (linear-light) lighting exists as the converted material path (goal 6) and is unused because the user plays the original hulls. Reopen only with a Windows test machine or an HDR-capable swap chain in CrossOver. | [hdr-scene-path.md](architecture/hdr-scene-path.md) |
| 2 | Modern tonemapping | Completed 2026-09-26 (was Installed) | AgX SDR write-back is installed and seen in game; a custom X3 look is later tuning. | [hdr-transfer.md](architecture/hdr-transfer.md) |
| 3 | FP16 lighting and HDR emissive | Completed 2026-09-26 (was In progress) | The opt-in linear material path stays installed but the user plays original hulls (2026-09-15); emitters above 1.0 use `--screen-emission-additive` (bolts, accepted run 26) and one `--emission-source-gain` over the effects program (engines, gate, beams, flares; screen-blended materials drawn additively under the gain, merged 2026-09-17 after the effect-shader census showed the engines were refused); the user confirmed the engines respond in run 30 (run87); position lights and signs on hull programs (`--hull-emitters`, removed 2026-09-25 with `--hull-emission-gain`) fired on 2 of 12 programs in run 38 C with no visible change under the shared F6; own toggle Ctrl+Shift+F4 and `--hull-emission-gain` installed in the run39 candidate; run 40 C (run121/122) showed that gain reaches only the two additive guide-light programs, while windows and hull lights are the light-map term inside 100 of the 108 opaque hull programs; `--hull-lightmap-gain` (one multiply on that term, plain/fill/sun-share variants) was accepted in run 41 C (run128, "very very good") and is the launcher default (4) in the run42 candidate; guide lights follow the effects key and gain; brackets rejected. | [linear-emission-cost.md](architecture/linear-emission-cost.md) |
| 4 | HDR bloom | Completed 2026-09-26 (was Installed) | Live integration with authored-glow correction at gain 0.375 / scatter 0.65 is installed and accepted for halo strength in run 9; the bolt halo (run 28/29) was the unbounded bloom source saturating the tonemapper; `--bloom-source-clamp 1.0` with additive alpha 0 was accepted by the user in run 30 (run87). | [hdr-bloom-boundary.md](architecture/hdr-bloom-boundary.md) |
| 5 | Exposure / optional adaptation | Completed 2026-09-26 (was Accepted (in game)) | Auto exposure with the +1.3 EV ceiling is the launcher default (`--hdr-ev-max 1.3`) and has flown in every run since run 26; fixed EV 0 stays reachable through `--hdr-exposure fixed` only (the Ctrl+Shift+F9 in-game switch was removed on 2026-09-26). | [space-exposure-policy.md](architecture/space-exposure-policy.md) |
| 6 | New material shaders | Completed 2026-09-26 (was In progress) | 168 reviewed pairs / 137 originals are installed with the distance-fade route default-on under linear materials, but the user plays original hulls, so fill 0.05 (main, after run 26) applies only to converted materials; selective exposure is closed and code-value fill rejected, while the linear-light fill inside the original programs and root-object point-light admission are scheduled default-off after the replay fixture (ratified 2026-09-16). | [original-shading-critique.md](architecture/original-shading-critique.md) |
| 7 | GTAO/SSAO | Completed 2026-09-26 (was Closed) | The default-off half-resolution chain works (≈180–210 µs CPU, zero when off) but is invisible at X3 viewing distances in runs 19–21; the user closed it on 2026-09-15 and it stays off by default. | [ambient-occlusion-scale.md](architecture/ambient-occlusion-scale.md) |
| 8 | Better directional/self shadows | Completed 2026-09-26 (was Accepted (in game)) | The sun-share lane with five cascades (250–150,000 units, 2048² each, drop order by importance), caster retention and footprint 8 flies in every stand command and was accepted through runs 65–67 (launcher default maps since 2026-09-25: 2048/4096/4096/2048/2048) (sun edge fix, core dimming, widening 4, footprint 8); the farthest cascade stays ≥ 90–100 km. Launcher defaults since 2026-09-25 (stand-command promotion: lane, replay, apply, the five-cascade set, retention, and alpha-tested casters flown clean in run309); `--no-shadow-replay-depth` and the other `--no-...` forms opt out ([inventory](verification/launcher-options-inventory.md#promoted-defaults-2026-09-25)). | [directional-shadows.md](verification/directional-shadows.md) |
| 9 | Reflections/SSR | Completed 2026-09-26 (was Closed) | Evaluated on real frames 2026-09-19: 0.3–1.5 % of the frame would visibly change, glass writes no depth, most reflected content is off-screen sky the game's cube map already covers: NO-GO. A near-field emissive variant can be reopened with one close docking-bay capture. | [screen-space-reflections.md](architecture/screen-space-reflections.md) |
| 10 | Improved/soft particles | Completed 2026-09-26 (was Closed) | Not wanted by the user (2026-09-19); the game's fog cards are not depth tested, so they show no hard intersections. | [sector-fog.md](reverse-engineering/sector-fog.md) |
| 11 | TAA | Completed 2026-09-26 (was Accepted (in game)) | Same-draw motion, camera reprojection and the scene-end resolve are installed; lattice crawl, pan flicker and the approach flash are closed; SETA smear closed by the strict sky history + exit reset defaults (Run 68 A) and SETA hull blur by `--taa-motion-weight 0.7,2,8` (Run 70 A, default in the Run70 DLL); cleanup batch 6 removed four dead variants. Remaining: a little ghosting the user accepts. | [temporal-resolve.md](verification/temporal-resolve.md) |
| 12 | Volumetric nebula/fog | Completed 2026-09-26 (was Accepted (in game)) | Stored-range fog (L2 look, 1.0× density, 22.5 km fade) replacing the vanilla cards is the accepted look with shafts and shadow retention; the fog shadow pass stayed off (no win, Run 68 C) and was removed on 2026-09-25; fog dust motes are on by default at 1300,3 / MAX_PX 8 (Run 70 B). Launcher defaults since 2026-09-25 (`--volumetric-fog 0.02 --volumetric-fog-cards replace --volumetric-fog-range stored`; `--no-volumetric-fog` opts out). | [volumetric-fog.md](verification/volumetric-fog.md) |
| 13 | Depth-aware lens effects | Completed 2026-09-26 (was Reopened) | Reopened by the user 2026-09-25 (Run 83 A: the vanilla CPU probe hides the whole sun flare at about half cover). The partial sun occlusion of `--sun-occlusion` (steps 1 and 2: the probe answered visible, a 32-tap 1x1 depth visibility pass, per-pixel clip and core dimming) has been in the DLL since 2026-09-22 and flew in Runs 64/65 (run228-235, core dimming accepted) but stayed opt-in; flown on Run83 2026-09-25 ("works") and made the launcher default the same day with the core dimming; `--no-sun-occlusion` restores the vanilla probe. Depth-aware bloom and DOF stay dropped. | [lens-flare-visibility.md](reverse-engineering/lens-flare-visibility.md) |
| 14 | Additional improvements | Completed 2026-09-26 (was In progress) | Crypto cache, reader and adjacency fast paths, speech (process-local WMA decoder v4) and media stability are accepted; collision fast paths (SAT SSE2, no-contact memo) are defaults and the box cull becomes one on 2026-09-23; frame time at the stand is draw-count bound (24 ms / 447 draws uncontended), addressed by goal 18; the sampling profiler attributes nothing under FEX. Music keep across alt-tab/save/pause is accepted (run273) and a launcher default since 2026-09-25, as are the loading fast paths (the adjacency path only takes effect with `--telemetry`); a ~1 s DirectSound gap on alt-tab remains. Open: key handling while paused, decoder auto-discovery and a Linux decoder build (user requests 2026-09-23). | [engine-frame-time.md](architecture/engine-frame-time.md) |
| 15 | macOS menu bar | Completed 2026-09-26 (was Planned) | Not started; the separate double cursor after alt-tab reproduces in vanilla (run 4, 2026-09-14) and is not a proxy regression. | [window-and-cursor.md](architecture/window-and-cursor.md) |
| 16 | Clustered forward lighting | Completed 2026-09-26 (was Closed) | Closed by the user on 2026-09-23 (not needed: the game has few dynamic lights). | [roadmap.md](architecture/roadmap.md) |
| 17 | Modern third-person chase camera | Completed 2026-09-26 (was Accepted (in game)) | Raised camera (pitch-down 0.5°, offset 0.50) is the default and accepted; `--chase-view-restore` is accepted and the stand command carries it (launcher default off, the x3run wrapper does not add it); 2026-09-23 the user asked for the HUD reticle anchored at the true forward point by default and the boom 15–20 % longer (corvettes did not fit), being flipped to `forward` / distance scale 1.05. | [chase-camera.md](architecture/chase-camera.md) |
| 18 | Merged-LOD overlay (draw reduction) | Completed 2026-09-26 (was Accepted (in game)) | Pilot accepted in Run 71 (run269): the four stand bodies draw their full LOD 0 mesh as one atlas draw plus an alpha group (ships 2 draws instead of 31–38, outpost 4 instead of 34) below 80 / 150 px with no visible transition; the overlay is reversible game data (`addon/NN.cat`), collision and weapons unaffected. The fleet batch is installed (install-fleet4: 620 bodies in `addon/05`-`06`, run321 flown); numbered mod catalogues are census sources. Tooling for selected `addon/mods` packages (derived package copy, `--mod`), the launch line `lod overlay:` and the rebake policy (`--trust-tool`, `--budget-bytes`) is implemented on synthetic trees (2026-09-25, [lod-overlay-mods.md](architecture/lod-overlay-mods.md)); open: a mod-tree bake and a launch with a derived package selected; `--lod-scale` stays off. | [merged-lod-feasibility.md](architecture/merged-lod-feasibility.md) |

Cross-cutting state: emitters above 1.0 use `--emission-source-gain` and
`--screen-emission-additive` (installed, `--linear-emissions` in its
full-surface bracket shape rejected 2026-09-15, launcher option removed 2026-09-25); run65 accepted bolts at
additive gain 2 while engines were unchanged because the source gain refused
separate-alpha blends, fixed on main ([cost note](architecture/linear-emission-cost.md),
[emission ledger](verification/screen-emission.md)). `--lod-scale` was closed
default-off and removed on 2026-09-25 ([note](architecture/lod-scale.md)). Native Windows runtime
behavior is unverified ([gaps](architecture/platform-portability.md)), and the
session identity logging merged as `5eda356` rides the next candidate.
