# Current goals and acceptance state

This file tracks acceptance state only, one row per goal, and changes only when
that state changes; ordering, the installed build and the current handoff live
in [status](status.md), the original scope in the
[objective](user-objective.md). Status is one of Planned, Accepted (in game),
Installed, In progress, Held, Closed or Rejected. The material scope clarification of 2026-09-15
now lives in the [material allocation assessment](architecture/material-investment.md).

| # | Goal | Status | Current state | Owning note |
|---|---|---|---|---|
| 1 | True HDR | In progress | FP16 RT0 redirection and identity write-back run in game, but the scene still carries compatibility-decoded gamma-space lighting; scene-referred lighting and verified HDR display output remain open (status 2026-09-15, "HDR scope"). | [hdr-scene-path.md](architecture/hdr-scene-path.md) |
| 2 | Modern tonemapping | Installed | AgX SDR write-back is installed and seen in game; a custom X3 look is later tuning. | [hdr-transfer.md](architecture/hdr-transfer.md) |
| 3 | FP16 lighting and HDR emissive | In progress | The opt-in linear material path stays installed but the user plays original hulls (2026-09-15); emitters above 1.0 use `--screen-emission-additive` (bolts, accepted run 26) and one `--emission-source-gain` over the effects program (engines, gate, beams, flares; screen-blended materials drawn additively under the gain, merged 2026-09-17 after the effect-shader census showed the engines were refused); the user confirmed the engines respond in run 30 (run87); position lights and signs on hull programs (`--hull-emitters`) fired on 2 of 12 programs in run 38 C with no visible change under the shared F6; own toggle Ctrl+Shift+F4 and `--hull-emission-gain` installed in the run39 candidate; run 40 C (run121/122) showed that gain reaches only the two additive guide-light programs, while windows and hull lights are the light-map term inside 100 of the 108 opaque hull programs; `--hull-lightmap-gain` (one multiply on that term, plain/fill/sun-share variants) was accepted in run 41 C (run128, "very very good") and is the launcher default (4) in the run42 candidate; guide lights follow the effects key and gain; brackets rejected. | [linear-emission-cost.md](architecture/linear-emission-cost.md) |
| 4 | HDR bloom | Installed | Live integration with authored-glow correction at gain 0.375 / scatter 0.65 is installed and accepted for halo strength in run 9; the bolt halo (run 28/29) was the unbounded bloom source saturating the tonemapper; `--bloom-source-clamp 1.0` with additive alpha 0 was accepted by the user in run 30 (run87). | [hdr-bloom-boundary.md](architecture/hdr-bloom-boundary.md) |
| 5 | Exposure / optional adaptation | Installed | Auto exposure with a fixed-EV comparison is installed; the user raised the ceiling to **+1.3 EV** after run 26 (2026-09-16) and that default sits on main, while the installed `5726a37b…` build still caps at +1.0 EV. | [space-exposure-policy.md](architecture/space-exposure-policy.md) |
| 6 | New material shaders | In progress | 168 reviewed pairs / 137 originals are installed with the distance-fade route default-on under linear materials, but the user plays original hulls, so fill 0.05 (main, after run 26) applies only to converted materials; selective exposure is closed and code-value fill rejected, while the linear-light fill inside the original programs and root-object point-light admission are scheduled default-off after the replay fixture (ratified 2026-09-16). | [original-shading-critique.md](architecture/original-shading-critique.md) |
| 7 | GTAO/SSAO | Closed | The default-off half-resolution chain works (≈180–210 µs CPU, zero when off) but is invisible at X3 viewing distances in runs 19–21; the user closed it on 2026-09-15 and it stays off by default. | [ambient-occlusion-scale.md](architecture/ambient-occlusion-scale.md) |
| 8 | Better directional/self shadows | In progress | The sun-share lane is integrated default-off; run81 (2026-09-16) still had zero availability because the mip-bias default left the exact cutout arm off and the two cutout pairs vetoed, fixed lane-only on main (`e62722a`, installed in the run29 candidate); run88/run90/run93 (2026-09-17) reported the lane available on nearly every frame under linear materials, and run91/run93 showed the cutout pairs drawing ~108 per frame in the busy Argon view, routed through the tested-opaque arm with the lane share written (code path documented; `cutout_opaque_*` counters ride the run33 candidate); the one-cascade depth replay runs at 42 µs with no consumer, and the legacy-shading sun application contract is ratified. **First shadows applied in game (run 37 B, run109, 2026-09-17):** the lane on original shading, the original share producer, geometry-chosen cascade-0 casters and the scene-end apply quad; the ship is shadowed by station parts and shadows nearby parts; station-on-station shadows wait on a larger box (run 38 options) and then cascades. **Run 38 (run111/112) showed popping, wrong-direction shadows: root cause the sun read from PS c4 regardless of program; fixed on main with per-program register resolution, positional sun from the engine light, N ≤ 4 cascades, caster retention and an at-rest toggle, installed in the run39 candidate; run 39 A (run115) showed the sun and cascades working but a camera-following band from a half-pixel receiver offset in the apply quad, fixed; the run40 candidate `c47f039c…` (2026-09-18; run116 A confirmed the band fix and 30 km reach, its distant flicker fixed) adds five cascades to 30 km, static-plus-capital far cascades and the ship-size-adaptive ladder, default-off; run 40 (run117–122) confirmed the blink fix, 2048² maps and K 1.5, and found the residual far-station flicker to be RT2 fp32 receiver precision (fixed behind `--sun-shadow-receiver-depth linear`, witnessed offline on the captures) and a near flicker on sun-grazing planes (slope margin, default on); run 41 (run123/124) confirmed the distant flicker gone under the linear encoding (±1 ULP flips 0 vs 7–20 %), now the only encoding; shadows ≈ 1.4 ms; retention clean, K 1.5; installed in the run42 candidate `1a5dd46c…`.** | [directional-shadows.md](verification/directional-shadows.md) |
| 9 | Reflections/SSR | Closed | Evaluated on real frames 2026-09-19: 0.3–1.5 % of the frame would visibly change, glass writes no depth, most reflected content is off-screen sky the game's cube map already covers: NO-GO. A near-field emissive variant can be reopened with one close docking-bay capture. | [screen-space-reflections.md](architecture/screen-space-reflections.md) |
| 10 | Improved/soft particles | Closed | Not wanted by the user (2026-09-19); the game's fog cards are not depth tested, so they show no hard intersections. | [sector-fog.md](reverse-engineering/sector-fog.md) |
| 11 | TAA | Installed | Same-draw motion, engine camera reprojection and the scene-end resolve are verified in game with RCAS sharpen and mip bias; distant asteroid/station shimmer (run 11) remains open and the 0.75 / −0.5 candidate defaults are modeled, not captured; run 41 B (run125) found the fade-band route inert under original shading since 2026-09-15 (solar-panel shimmer, likely the antenna shimmer too), fixed in the run42 candidate; run 42 B (run130) found leg 1 fixed and a residual (module behind the camera plane; a node's translucent sub-mesh refused by the opaque chain), both fixed in the run43 candidate; runs 43–47 narrowed the residual shimmer to two causes with fixes merged: distant thin lines → `--taa-far-stabiliser` (accepted by the user in run 47 C, weight only) plus `--light-map-far-fade` for the glowing windows (run 48 A, run178: acceptable, minor residual camera-motion flicker); the solar-plant lattice crawl → root cause found from the user's screen recording (edge-on arm pixels toggling geometry/sky over the 8 jitter phases, history discarded by the variance clip), fix `--taa-thin-region` confirmed by the user at rest in run 48 A (run177), but camera-rotation crawl remains open. The user subsequently accepted light-map fade 80,220,1 as the launcher default (2026-09-20); far stabiliser/thin-region launcher defaults are still off. | [temporal-integration.md](architecture/temporal-integration.md), [taa-lattice-crawl.md](architecture/taa-lattice-crawl.md), [taa-distant-line-fade.md](architecture/taa-distant-line-fade.md) |
| 12 | Volumetric nebula/fog | In progress | User approved 2026-09-19 (configurable strength, likes 0.02). Run 48 B (run180) confirms stage 1 in flight: the user prefers 0.02, reports about 2 FPS cost, and supplied The Hole/Atreus' Clouds 16-card-family captures. The user now wants volumetric fog to replace the vanilla cloud cards; that change is being implemented. The read-only engine sector-record diagnostic is committed; using it for activation/density awaits live chain validation. Clear-sector fade timing remains unverified. | [volumetric-fog.md](architecture/volumetric-fog.md) |
| 13 | Depth-aware lens effects | Closed | Flare occlusion evaluated 2026-09-19: the engine uses a CPU collision probe (flares pop, shine through when the probe misses); judged MINOR and dropped; depth-aware bloom and DOF dropped by the user. | [lens-flare-visibility.md](reverse-engineering/lens-flare-visibility.md) |
| 14 | Additional improvements | In progress | Crypto cache, reader and adjacency fast paths are accepted (runs 17–19), speech and the selection pause are accepted (runs 16/18); run 29 measures a busy view at 14.7 ms p50 (~240 draws) vs 8.1 ms facing away with Present never the wait, the sampling profiler attributes nothing under FEX; run 30 (run87) split a 28.5 ms busy frame at 457 draws into 8.9 ms of hooked state calls (two thirds the diagnostic's own timing) and game time, and run 31 (run89) attributed a 37.5 ms busy frame at 987 draws to the engine's view submission (about 63 state calls per draw from its unfiltered effect state manager, scene update 65 µs); run 32 (run92) measured the same view at 26.5 ms with the proxy unhooked from the state path; the run 32 counters ruled out an engine-side state filter (≤ 1 ms) and proxy instancing (5 % candidates), the quiet-sector stall (~390 ms per frame) was the media-cue selector retrying a failing MPEG-1 video graph (`mov\\00002.dat`) every frame and is solved by the media-cue negative cache at `0x00498140`, default on (run99, 2026-09-17, user confirmed); the busy frame's remaining cost is split per draw (Wine draw 8.7 µs, BeginPass 6.7, engine 4.6) with the pass-replay wrapper (3–5 ms) the only lever left; run 36 (run104/105) closed the D3DX experiment (builtin +32 %), run 37 (run107/108) closed FEX TSO and wined3d CSMT, and the native BeginPass fixture showed D3DX's own walk is ~1.5 µs of the 6.6 with nothing to dedup: the pass replay is not pursued and the busy frame stays at ~22 ms (decision 2026-09-17); run 38 B bundled `prepare` at 6.36 µs/draw and the technique lookup measured 0.007 ms/frame offline, so the technique-cache trampoline is dropped (2026-09-18). | [loading-observations.md](reverse-engineering/loading-observations.md) |
| 15 | macOS menu bar | Planned | Not started; the separate double cursor after alt-tab reproduces in vanilla (run 4, 2026-09-14) and is not a proxy regression. | [window-and-cursor.md](architecture/window-and-cursor.md) |
| 16 | Clustered forward lighting | Deferred | Deferred by the user on 2026-09-19: the game has few dynamic lights, so clustered lighting buys little; material and light reconstruction would precede it anyway. | [roadmap.md](architecture/roadmap.md) |
| 17 | Modern third-person chase camera | In progress | Run 26 accepted the raised camera (`--chase-pitch-down-deg 0.5 --chase-offset-y 0.50`), now the default on main; `--chase-view-restore` restored both run78 transits; the 0.4–1.1 s centre-then-jump after a transit is fixed on main (`e4fd22a`, unbound-phase admission on the active-control proof) and installed in the run29 candidate, awaiting a gate run. Vanilla stays the default and docking is untested. | [chase-view-transition.md](reverse-engineering/chase-view-transition.md) |

Cross-cutting state: emitters above 1.0 use `--emission-source-gain` and
`--screen-emission-additive` (installed, `--linear-emissions` in its
full-surface bracket shape rejected 2026-09-15); run65 accepted bolts at
additive gain 2 while engines were unchanged because the source gain refused
separate-alpha blends, fixed on main ([cost note](architecture/linear-emission-cost.md),
[emission ledger](verification/screen-emission.md)). `--lod-scale` is closed
default-off ([note](architecture/lod-scale.md)). Native Windows runtime
behavior is unverified ([gaps](architecture/platform-portability.md)), and the
session identity logging merged as `5eda356` rides the next candidate.
