# Resume handoff — paused for usage/account switch

Paused at the user's request on 2026-09-14. Do not resume until the user requests
it. All agents and project build/Wine jobs were stopped or completed. No game
was launched by an agent. The overall graphics objective remains incomplete.

Read this file, `AGENTS.md`, `docs/status.md`, `docs/goals.md`, and
`docs/user-objective.md`, then only the owning notes needed for a selected task.
The September 13 handoff is historical; do not restart its completed work.
The goal tool's short “read the objective file” text is not the completion scope:
the full modernization objective and later user decisions remain binding.

## Main and installation

- Main checkpoint **`a85bcef`** merges reviewed distance-fade source and actual
  qualification. This handoff is committed afterwards. `0b57e09` contains the
  reviewed voice diagnostic sources/results; `ac8c9ac` reconciles portability.
- **Installed gameplay source remains `8442f43`**, DLL SHA256
  `1f6a473dd035894bb868adc113be8f76c7dfde4731157595def614d0411a42ba`.
  The canonical `verification/results/linear-material-install.json` owns its
  build, tests and rollback. Do not install current main automatically.
- Installed: 168 material pairs / 137 originals, six additional SM3 glass pairs,
  bloom gain **0.375 / scatter 0.65**, target/node/fade capture diagnostics.
  TAA/AgX/FP16, Auto max +1.5 EV, accepted chase defaults and loading fixes remain.
  Linear materials, bloom and emissions remain opt-in. Camera remains vanilla
  by default. No voice repair, cutout runtime or distance-fade runtime is installed.
- No new enhanced gameplay run is requested. `docs/verification/user-runs.md`
  remains the sole user run queue. Run 28 (user run 9) is the latest gameplay
  evidence, saved at `/tmp/x3-bottleX3-run28/` before any new run.

## Saved work: do not recreate it

Persistent backup outside the Git repository:
**`/Users/asvetl/x3-mod-resume-2026-09-14/`**. It contains authored unfinished
cutout source, task handoffs, decoder recipe, and retained build artifacts.
Original `/tmp` worktrees/evidence remain in place. The backup has no game
originals or raw decompiler output. Individual task handoffs describe their
earlier pending states; the current decisions below supersede those where stated.

### 1. Distance fade: correctness qualified; reduce cost next

Source is now merged. Owning note: `docs/architecture/linear-distance-fade.md`.
Canonical actual result: `verification/results/bottle-X3/linear-distance-fade-live.json`
(about 100 KB; query selected fields, never read it whole).

The retained production DLL was built once from **`ad3fefe`**, SHA256
`e901415f993e1a30e78aea86b9e532ade65876d1c63e61b1bbad24e6895bb447`;
linked x87 audit passes 219 reachable functions. Fixture-only revisions end at
`f5af8b0`; production stayed unchanged. `/tmp/x3-fade-runtime-candidate.json` is
now updated to live-correctness-passed/performance-not-accepted and bound into
the canonical result. No pending candidate-record update remains.

Actual X3 R3 passes **15 processes / 302 frames / 4,502,255 functional/admission
checks / 140 exact TAA readbacks**. Same Sol/high reviewer approved source and
actual evidence. R1/R2 failures were fixture/parser errors; R3 preserves their
witnesses and does not weaken production recovery. In particular, export
quarantine survives frames and Reset; a valid empty mask is not always a safe
history source. The owning note records the exact scope and failures.

Performance is **not accepted**. Source-window overhead is +0.790–0.823 ms for
one ordered DIP and +7.638–8.464 ms for 16 at 1080p. These EVENT-fenced timings
are not GPU timestamps or game FPS. Keep the option off; root chose **no install**
before a concrete performance decision. Do not hide cost by capping draws or
mixing policies partway through a frame.

Next design, saved in backup `fade-performance-next.md`:

1. Small candidate: fade RGB-only writes guarantee `B.a == A.a`; remove the B
   texture sample from fade composition, preserve A alpha, retain full B for
   recovery. Estimated traffic 56→48 bytes/pixel/DIP; not implemented or measured.
2. Larger candidate: initialize B=A, E=0, C=A in the existing preparation pass,
   then compose only a proven conservative screen region. No extra pool or source
   replay. A reliable raster bound is a prerequisite; current finite-buffer data
   has no spatial bound. Targeted game RE is proposed around `0047d9c0`→`0047e076`
   / deferred `0047e6e0`, mesh-part `+64`. Near-plane/unknown cases use full screen.
   Merely scissoring an uninitialized C would publish stale pixels. No batching
   decision is approved; per-draw quantization and reader boundaries matter.

Run27 has one matching fade-state Asteroid candidate in each of 16 captured
frames; that is not a general scene count. Retained candidate/seam/R3 fixture
are backed up under `artifacts/`; the original qualification worktree is
`/tmp/x3-fade-runtime-candidate`. Do not repeat its unchanged broad qualification.

### 2. Alpha-tested materials: unfinished source saved

Worktree **`/tmp/x3-cutout-runtime`**, branch
`qualification/linear-cutout-runtime`, HEAD
**`0c30855872424d0b65cf9f0232adcd7aa551ce0f`**. It contains 14 uncommitted files:
nine tracked deltas and five new authored files. Main does not contain this
runtime implementation. Backup `cutout-source/` has the exact tracked patch,
new-file archive, base and restore commands. Read backup `cutout-handoff.md`.

Core source review is **approved**, including the fix that restores a held mip
bias before an application setter and retains correct failure state. The host
sampler test expansion is saved but unfinished/unverified. Live fixture/runner
review has **not started**. Finish those checks before freezing/building.
The retained cutout EXE (`d4183ba4…`) is **stale** after the legacy mip-model edit;
do not run it as current source. No candidate DLL or Wine run exists for this work.

The bounded runtime arm is two exact Argon DEFAULT/BUMP pairs, existing material
flag, native alpha-test GE/ref1 and RGB7 preserved, public cached capability
checks, 32 safe shadow entries, and no-seed temporal invalidation when recognized
foreground coverage is missed. Nonzero mip bias remains explicitly refused.
The fixture includes mixed fade/cutout and changing-alpha/near-coplanar cases;
actual metrics must determine acceptance. It does not prove all cutouts or the
docking-port bug. Detailed pending tests, compilation, source files and remaining
fault coverage are in the handoff; preserve existing detached qualification.

### 3. Selection stalls / speech: optional decoder build next

Read `docs/reverse-engineering/voice-stream-creation.md` and backup
`voice-decoder-handoff.md`, `gstreamer-wma-local-recipe.md`, and
`gstreamer-local-inventory.md`. All diagnostic sources/results are committed.

Run28's target-selection pauses occur in synchronous voice-stream creation;
the user hears no target-name speech. R1 fails all 24 graph constructions. R2
gets misleading ASF Load success but no pins. R3 directly opens both files and
finds **ASF recognized, then no suitable WMA8 decoder**, with Open E_FAIL.
The game already caches successful streams; negative caching would hide speech.

The corrected native-null-event synthetic PCM control passes both graphs and
eight nonzero reads. Its initial external-event call was a probe mismatch,
resolved by game disassembly. Exact timeline accuracy remains open: 100 ms of
PCM reports 95.1304 ms. Public-source DWORD arithmetic reproduces the discrepancy
but installed instructions/chunk size are not proved; the game uses sample
start/end to trim cues. Open success alone is not restored speech.

Root approved the **isolated build** recipe after review; no download/build/load
was started. The recipe review is complete; explicit `--disable-swresample` was
its sole correction and is applied. On user-authorized resume, proceed with the
build without asking the user for another permission. Root must review the
produced dependency/symbol evidence before loading it in a fixture.

Recipe: stock gst-libav 1.28.4 plus minimal LGPL-only FFmpeg 7.1.5 arm64 wmav2,
matching build-only GLib 2.78.0 / GStreamer SDK. Runtime must use CrossOver's
public core/GLib once, with a private FFmpeg closure; no Homebrew/SDK core runtime,
app/bottle/global writes or `DYLD_LIBRARY_PATH`. Use versioned process-local
`GST_PLUGIN_PATH_1_0` and `GST_REGISTRY_1_0`; preserve bundled system plugins.
GitHub verified-tag metadata is not a locally verified cryptographic signature.

This optional CrossOver audio adapter is not a Windows prerequisite: keep the
native documented COM path. No custom DirectShow source/filter or game hook is
approved yet. If isolated loading is viable, root runs the retained sync probe
then native-null-event actual-file decode/seek/lifetime checks under the Wine
lock. Game speech and selection latency still require a later user run.

## Resume orchestration

- Restart focused agents, not historical tasks: Astra for cutout completion,
  fade performance work and the isolated audio build; Sol/high for bounded
  analysis and independent reviews. Do not use xhigh by default. All prior
  workers are stopped; no stale handle needs waiting or killing.
- Root owns architecture, merge/build/install decisions and the single Wine
  queue. Every Wine command uses `verification/probe/wine_lock.py` and
  `X3M_FIXTURE_BOTTLE=X3`. CrossOver **Preview**, bottle **X3** only.
- Never launch the game. Combine useful changes before updating the brief user
  run queue. Preserve EXE/CAT/DAT and unrelated settings. Review/fix before
  commits, scope tests to changed dependencies, and keep one retained candidate.
- No project test/build/game process remained at pause. Existing background
  Wine services and an old Steam launcher wrapper were left untouched.
- Do not call the full goal complete: whole-scene linear lighting, HDR display,
  AO, shadows, reflections, particles, volumetrics, lens effects, clustered
  lighting, window/cursor and additional acceptance remain in `docs/goals.md`.

