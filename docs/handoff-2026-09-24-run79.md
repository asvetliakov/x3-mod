# Handoff 2026-09-24 (evening, Run79)

Previous handoff: [archive/handoff-2026-09-24-morning.md](archive/handoff-2026-09-24-morning.md). Rules: AGENTS.md, CLAUDE.md;
memory rules that matter: no host load during flights; fog merges run the route bridge in the gate; the tree is frozen only
while a qualification agent runs from the checkout (candidates build from a detached worktree); mod trees read-only; never
run Wine from a copied CrossOver bundle; the **shader slot budget** rule (AGENTS.md: 512 is a floor, plan against 32768,
no fallback program set, ceiling 2,048 per TAA program pinned by RESOLVE_BUDGET).

## Installed (docs/status.md is authoritative)

- **DLL Run79** `d3683ced009245e7891ad2da5994d219e33390c1d3b522f179d4bf35ea4d1d0f` (56,358,862 B, installed 08:56) from `df01f23b` (install record verification/results/run79-candidate-install.json,
  qualification run79-candidate-qualification.json); rollback Run78 `d4ba9f05…` at `/tmp/x3-run78-candidate/build/d3d9.dll`
  (accepted in Run 78 A: dither, exit fix, scale 4, S3).
- Run79 carries, unflown: **TAA A'** (`--taa-region-hold on|off`, default on, df01f23b) and the **Terran station LOD patch**
  (`--terran-station-lod size|distance`, default size, dd862b9f; census `flag31=`). Plus the route bridge harness fix 026d24ac.
- Game data: fleet overlay `addon/05` (488 bodies) + `addon/06` (123), 611 bodies (install-fleet2). Slot 06 bodies are all
  Terran station parts so far; they never switched because of the engine's distance branch (fixed by the patch, unflown).

## Decisions today (user)

- Slot budget: treat the cap as 32768 (measured: nothing enforces 512 on this runtime; run-time ~1 us per slot per frame at
  5120x1440; first-draw compile is the ceiling). No fallback program set; creation failure = pass off with one row.
- Terran stations: DLL patch of the bit-31 reader (not the baker ladder); covers every station of every race incl. mods.
- Fog march scale 4 default accepted; dither accepted; S3 look accepted on the default (the `--taa-history-taps 16` A/B was
  skipped by the user); bolts ok (Run 77 D closed). The exit-crash fix is accepted by the absence of the fault: the
  `engine_memory_read_refused` row is never written (the game's last device release skips our hook), expectation withdrawn.
- Merged worktrees may be removed; the user sleeps after this handoff, so the autonomous list below is the next work.

## State at the 20:30 compaction (2026-09-24)

- **Installed:** Run80 DLL `593112dc…` (56,609,477 B) from `9cf5decf`, installed 19:49 (records
  `verification/results/run80-candidate-{qualification,install}.json`); rollback Run79 `d3683ced…` at
  `/tmp/x3-run79-candidate/build/d3d9.dll`. Game data: fleet overlay **install-fleet3** (620 bodies, installed 20:16,
  `verification/results/lod-overlay-batch/install-fleet3/install.json`; rollback needs a rebake from 33aba284).
- **Queued:** Run 80 A only (user-runs.md): baseline; `--lod-occlusion all` on the coarse Orbital Defence Station (AO
  back + red plates from the rebake, first-sight hitch, vanilla distant stations); `--lod-switch-log` while turning near
  it (single pop or continuous flicker); `--taa-thin-vote on` on the lattice stand; `--fade-rt2-owner on` on the run214
  stand under a slow vertical pan; rebake look; menu exit. Triage pattern: `verification/results/run299-303-run79a/`
  (scripts parametrised by run number) plus `terran-colour/occl_lod_census.py` (the 2048^2 map at s5 on LOD > 0 draws),
  `ods-flicker/lod_switch_rows.py`, and the Run 80 item lists in `temporal-resolve.md` (thin vote, fade owner) and
  `lod-occlusion.md`.
- **Agents in flight at compaction (worktrees, uncommitted, may finish unattended):**
  - `agent-a31baf52218b40c72` (from 9cf5decf): **TAA S4** half-resolution box, opt-in `--taa-box-resolution full|half`
    (taa-plan-lifted-slot-cap.md section 5). On resume: read its ledger section "S4 half-resolution box" in
    `temporal-resolve.md` and the worktree diff; review (Opus `review`, then Fable if clean and consequential), 3-way
    apply, commit.
  - `agent-a81e2a392d5cabb83` (from a0a5e645): **`--fov <vertical degrees>`** (field-of-view.md; default 58.72 =
    F 0x3470, `game` = 0x4000; immediate at 0x0041c9dc; bounds 36..120; small-parts factor F/0x4000; site verifier +
    identity case + Wine fixture + ledger `docs/verification/field-of-view.md`). Same resume procedure.
  - If a worktree is gone or clean, the agent did not finish: re-dispatch from the brief in the memory/handoff.
- **Next after Run 80 A is reported:** triage on the pattern above; decide the occlusion patch default, thin vote and
  fade owner acceptance (then the sentinel-stabiliser removal per fade-rt2-ownership.md section 5), whether the ODS
  flicker is the LOD pop (then hysteresis on the overlay switch or a baker change); Run 81 candidate = S4 + `--fov`
  (+ anything accepted from Run 80 made default). No Run 81 candidate before Run 80 A is flown.
- Rules that bit today: `/tmp` cleanup deleted the shader corpus (`/tmp/x3-shader-sweep/programs`; rebuild in 10 s per
  shader-sweep.md "Restoring the local corpus") and three fixture inputs that the Run80 gate rebuilt; the launcher's game
  guard now uses `game_guard.game_running()`; a crashed fixture is cleared with `wine_lock.py --clean-orphans`.

## Run 79 A flown (2026-09-24 evening; run299/300/302/303, run301 aborted)

- **A' accepted** (user look report: no difference on/off on the stand, pans, silhouettes, shards; gpu-sync: mask
  2.93 -> 1.54 ms with the x/y dilation draws gone, box +0.23 ms at rest and pan, net -1.1 ms per frame at 5120x1440;
  `temporal-resolve.md` "Run 79 A"). The hold-off chain is removed on main (bb3a691f): `--taa-region-hold` no longer
  exists (the launcher refuses it), 19 TAA programs, DLL -78 KB, refusal = region off with one row. Next in the TAA plan:
  **S4** (half-resolution box).
- **Terran station LOD patch works** (status=patched write=atomic, 16 slot-06 bodies flag31=1, lod 1 below s/T_pad 1.0,
  the coarse Orbital Defence Station confirmed in screenshots/terran-lod1.png). Two coarse-record defects found:
  - **red plates lost**: the baker's alpha rule folded 255-alpha plate materials into the alpha group (fixed 5aa645e3:
    alpha only when the alpha can drop below 1; fleet eligible 622 -> 625, no new borrowing). **Needs the full rebake**
    (`--batch --sync`, ~45 min, no launches meanwhile; the resolver change already changed the tool hash): user's go.
  - **ambient occlusion lost**: the engine binds `t_OcclusionTexture` only at LOD 0 (`texture-lookup.md` section 12);
    user chose the opt-in EXE patch `--lod-occlusion record0|all` (ac381be7) for a Run 80 A/B.
- **One ODS part flickers under motion** on both hold settings: every draw routed, no animation, no LOD flip inside the
  frozen bursts; inferred record 0 <-> C pop at each part's own threshold (no hysteresis); `--lod-switch-log` (e8af9e46)
  settles it in Run 80.
- Also on main since: bolt shape telemetry, B thin vote and fade RT2 owner (both opt-in), Wine queue orphan cleanup,
  distance-fade chain repair. `/tmp` and the game's capture folder were emptied at the user's request (sessions below
  run295 gone; candidate DLLs kept).

## Done while the flight was pending (2026-09-24, afternoon; all reviewed, on main, nothing installed)

- **B thickness vote** `--taa-thin-vote on|off` (default off; 4de081ae): scene-end subset histogram through READONLY
  Locks under a readable-MANAGED creation policy armed only by the vote's route gate, per-draw one lookup, flag in
  c218.x of a 12-float pixel upload, RT2 `.a = 1 - thin` on opaque routed rows (`.a = w` with the option off), tests-draw
  thin twins vote on `validDepth && 0 <= a < 1`; write invalidation via an ownership queue; 205 motion cases (199 pinned
  identical), temporal/lattice unchanged; ledger `temporal-resolve.md`. Run 80 A/B item list in the ledger.
- **Terran patch Wine fixture** `run_terran_lod_patch.py` 92/92 (3fa3ba8d): the production write path against a real
  image page at the production address; FlushInstructionCache measured unnecessary under FEX (kept for Windows).
- **Bolt shape refusals** (2dbb990d, aa6d2e18): all 84 in run298 are real bolts at the draw-shape clause, cause inferred =
  a batch above the 6,144-vertex scan bound; the refusal row now carries a sub-clause bitmask, `primitives=` and
  `stream0_bytes=`, the window row `refused_max_prims=` / `refused_shape_bits=` (flight confirms).
- **Texture lookup RE** `texture-lookup.md` (ff70bca8, 38d7d01f): leading numbers are Materials ids (tex/true/<n>),
  negative ids are `types\Animations` rows (dds/-N.pck members are dead data), `\Desktop\` names take placeholders.
  **Baker** follows it (a3b23069): eligible 616 -> 622, `texture_unresolved` 44 -> 0, new reasons
  `texture_animation_unsupported` (168) / `texture_generated` (1). **The tool hash changed: the next `--batch --sync`
  rebakes every body (user decision).**
- **Fade RT2 ownership** `--fade-rt2-owner on|off` (default off; ed038a7d) per the ratified `fade-rt2-ownership.md`
  (bbab97cb; prepass unknown settled in `distance-fade.md` section 8: ShowSpace sets `0x40000`, no class test;
  fe353a1d): fade-arm draws own RT2 with exact depth through the blend (`.a = 1`), arm identity widened to the run214
  station families under original shading; 220 motion cases (205 pinned identical); resets measured: one current-only
  frame per falling edge plus the re-route frame as today. Run 80 conditions in design section 5 / the ledger.
  zonly RT2 hole-count cases added (0 holes jittered, 392/392 witness unjittered). Open: native FP32 blend exactness.
- **Detached distance-fade chain repaired** (066792fd): missing `linear_emission.cpp` in the build script since
  72e799b0 and a dangling else in the fixture since d8642469; bisect clears production; 78 cases PASS again.
- **Wine queue**: a crashed fixture no longer holds the lock (afcb34ef): `fixture_process.py`, `wine_lock.py
  --clean-orphans`; `run_motion_output.py` cleans its own case on timeout (the 127 other runners keep their own path).
- Merged worktrees removed (about 60 GB).

## Autonomous while the flight is pending (user asked 2026-09-24 09:00; order recommended)

Nothing that changes A', removes the dilated chain, starts S4 or tunes Terran ladders: those wait for Run 79 A. Safe now:
1. **B**: per-draw thickness flag as a vote (taa-thin-geometry-alternatives.md 3.2; measured in the CloneMesh-destination
   Unlock observer, `c216.z` -> RT2 `.a`, read in the tests draw); one-line census of 0x440 clones / INDEX32 first.
2. A Wine fixture for the Terran patch's real write path (a copied code page in a fixture process; today host-tested on a
   buffer only).
3. triage-deep on run298's 84 bolt shape refusals (which shape test; existing logs, read-only).
4. RE: the Khaak `texture_unresolved` lookup under tex/true/ (disassembly, read-only) so the baker can stop refusing them.
5. Fade routing (distance-fade.md: fade draws above an alpha threshold as RT2 pixel owners); the sentinel-stabiliser removal
   after it needs a pan flight.
No Run 80 candidate without flight results.

## Run 80 (after Run 79 A is reported; no candidate before)

Candidate content already on main, all opt-in: `--taa-thin-vote on` (B) and `--fade-rt2-owner on`; the A/B item
lists are in `docs/verification/temporal-resolve.md` (sections "thin vote" and "fade owner"). Also on the next DLL:
bolt shape-refusal telemetry (confirms the 6,144-vertex inference). The baker rebake (`--batch --sync`, every body)
is a separate user decision.

## Next steps, in order

1. **Run 79 A** (user-runs.md): A' on/off look A/B (crawl at rest, trails, silhouette flicker under a pan, popping shards,
   the 8 frames after a pan stops) with `--gpu-sync-timing` for the `taa_mask` / `taa_box` rows (box under a pan is the open
   cost question; mask x/y must be gone); Terran bursts (USC dock, SPP XL below the 150 px switch: expect `flag31=1`, `lod=1`,
   log row `terran_station_lod site=0047d01c status=patched reason=ok write=atomic`), then `--terran-station-lod distance`
   (lod 0 again); a non-Terran slot-06 control if reachable; menu exit. Triage pattern: verification/results/run295-298-run78a/.
2. On acceptance of A': remove the `--taa-region-hold off` option and the dilated far_camera program (the refusal path then
   = region off); then **S4** (half-res box) per taa-plan-lifted-slot-cap.md section 5; then B (thickness flag as a vote);
   the fade routing (docs/reverse-engineering/distance-fade.md) and then drop the sentinel stabiliser after a pan flight.
3. If Terran stations now switch: measure the draw-count and look change on vanilla Terran ladders (RE predicts different
   picks for 528 of 1,081 census rows); a USC dock at lod 1 is the visible check.
4. Open, smaller: 84 bolt shape refusals in run298 (1.1 %, no capture); `texture_unresolved` Khaak bodies (engine lookup
   trace under tex/true/); adeffects g_TexMatrix; NULL-diffuse placeholder colour; the Terran patch's real write path has no
   Wine fixture (host-tested on a buffer only; the flight row `write=atomic` confirms it); the A' section-32 replay dumps are gone.
5. Closed no-go: D3D9->D3D11 translation (measured), DXVK (MoltenVK; a newer MoltenVK is an app-bundle change the user decides).

## Runs

- Completed today: 77 A/B/C/C2/A2(folded)/D, 78 A (run295-298). Open: 79 A.
- Worktrees: merged and removable: agent-aff3f80d9067fb785 (dither), agent-a5b01ee9bcf70b679 (S3), agent-a3e7804f8bae015ed
  (bridge fix), agent-a1794a0df9b11ed17 (Terran), agent-a0442d1e9f65d92f2 (A'); keep agent-a0d5b30b56e14bd37 and
  agent-a2b067e4005541a7a. Candidate trees: /tmp/x3-run7{6,7,8,9}-candidate (DLLs retained for rollback).

## State at 22:20 (Run81 installed, Run 81 A queued)

Run 80 A flown (run304 baseline, run305 `--lod-occlusion all --lod-switch-log`, run306 thin vote, run307 fade owner, run308
gpu-sync baseline) and triaged (commits 98b4fb41, 5ba25c55, 06c37e20, afcdbfc8; results under
`verification/results/run30{4-305,6,7,8}-run80a*/`). Outcomes: occlusion patch works and is now the default `all` (user);
thin vote and fade owner accepted as defaults (no GPU pass cost within 50 µs against run308; A'-only TAA 6.78 ms at
5120x1440); ODS flicker = one LOD pop per crossing, no hysteresis (cull-census.md); coarse red plates 1.41x brighter =
the baker's one synthesised material carrying the dominant material's constants, **user accepts it** (no shading
classes, no rebake; lod-overlay.md); underside transition = record 0's alpha-tested plates excluded from the shadow
replay (opt-in `--shadow-alpha-casters`, 17868e9d); sentinel stabiliser removal needs the S = 0 launch (Run 81 A launch 3).
Merged today after Run 80 A: `--fov` (a5fddc6a), S4 half box (a2409fd5), thin-vote telemetry (c59fc6ac), alpha casters
(17868e9d), Run 81 defaults (9e1645be). **Installed**: Run81 DLL `b4e945ed…` (56,719,270 B) from 9e1645be at 22:15
(qualification + install records `verification/results/run81-candidate-*.json`; rollback Run80 `593112dc…` at
`/tmp/x3-run80-candidate/build/d3d9.dll`); fleet overlay install-fleet3 unchanged. **Run 81 A is the only queued run**
(user-runs.md: three launches, defaults + FOV + alpha casters with gpu-sync; S4 half; sentinel stabiliser 0). No agents
in flight; no worktrees. Next after the flight: triage on the run304-308 pattern (`verification/results/run308-run80a-gpu-
baseline/` scripts for GPU rows; `run307-run80a-fade-owner/` for the owner and the §5 conditions; fov rows `fov`,
`fov_confirm`, `cull_small_parts_value focus=`; `shadow_alpha_casters` per-frame row, `refused_pool` must be 0), then
decide: S4 half default, sentinel stabiliser removal (fade-rt2-ownership.md §5), alpha casters default, FOV acceptance
(and whether the in-game menu value survives a reload). Open follow-ups: a mask-source option (`vote|screen|both`) to
test dropping the screen-space thin-region detection (the user asked; `--taa-thin-region 0` is not that test, it removes
the weight), a LOD hysteresis patch only if the ODS pop stays visible, the thin-vote `draw_us` needs `X3M_TELEMETRY_DRAW=1`.
Rules that bit: a `wine_lock.py --clean-orphans` by one agent ended another agent's running fixture (parent PID 1 is not
proof of an orphan; check the runner shell first); agents' shells naming X3AP.exe trip the launcher game guard in tests.

## State at 2026-09-25 00:30 (Run82 installed, Run 82 A queued)

Run 81 A flown (run309 defaults + `--shadow-alpha-casters on` + gpu-sync, run310 `--taa-box-resolution half`, run311
`--taa-sentinel-stabiliser 0`) and triaged (e9455a03, 483a113d, 6fc39137; results `verification/results/run309-run81a-launch1/`,
`run310-run81a-s4-half/`, `run311-run81a-stabiliser-off/`). Outcomes and the changes they produced, all merged on main:
ODS underside fixed by the alpha casters (clean rows); S4 half accepted (-1.06 ms box) and made the default (c2aa8d4e);
`--fov` was overridden by the in-game menu (starts from its own 90) -> the **remap model** (user, like X4: the game's N =
"N horizontal on 16:9"; constructor immediate + stub at 0x0042dbf8 with an 81-entry table N 50..130; 250cc48d; RE note §7);
the sun vanished at F 100 near the view centre = **engine 32-bit overflow in the lens-flare collector** (RE §9; saturating
stub at 0x0047e391, `--sun-flare-fix` default on, 72439e2c); the chase ship 1.333x larger -> proxy boom compensation
(`--chase-fov-compensate`, 7187771f); the cull read a HUD projection -> scene projection hand-over (5a81df27); stabiliser 0
shimmered because the fog-band plants' alpha-tested cutouts (panels, trusses) were sentinel-class -> design
fade-alpha-cutout-ownership.md ratified (9eaeb180) and implemented (d1a4e360: the fade arm admits alpha-tested Z-write-off
fade pairs, native mip bias kept); `--taa-thin-region-source both|screen|vote` opt-in (4855dc5f; `vote` saves only ~0.1 ms
because the mask draw also writes gates/far weight/sentinel/depth fold). **Installed**: Run82 DLL `cd8ef8e4…` (56,777,721 B)
from d1a4e360 at 00:26 (records `verification/results/run82-candidate-*.json`; rollback Run81 `b4e945ed…`); fleet overlay
install-fleet3 unchanged. **Run 82 A is the only queued run** (three launches: defaults with the FOV menu / sun / chase
checks + gpu-sync; `--taa-sentinel-stabiliser 0` retry on the fog-band plants; `--taa-thin-region-source vote` A/B). No
agents in flight; no worktrees. Next after the flight: triage on the run309-311 pattern (`fov_timeline`, sun group angle
scripts, `chase_fov_compensate` rows, `fade_tested`/`fade_owner_masked`, station detail valid depth, §5 conditions), then
decide the stabiliser removal, the FOV/menu acceptance, `vote` as a source, and the Run 83 candidate. Open: a 5120x1440
replay for §5 condition 3; the temporal pass state block does not refresh stream offsets on Capture (filed, pre-existing);
`test_game_phase_sites` arena model does not count fov/sun bytes (~300 B of 4.8 KB headroom). Rules that bit: `git add -A`
swept an in-progress agent's untracked results into a commit (reset --soft and recommit); agents must not chain the game
guard with `&&`; the runner needs `verification/probe/build/` in a fresh worktree.

## State at 2026-09-25 ~05:30 (Run83 candidate qualifying; Run 83 A queued)

Run 82 A flown (run312-318) and triaged (fa387b88): FOV menu, sun at 100, chase and the fog-band plants accepted; run312 loaded
a pre-patch save and ran vanilla until the first menu step (the registry serializer restores registry+0x24, RE §7.4) -> **FOV
load-site claim** at 0x0041c8c1 with the exact-match rule (3fb71497, two reviews; a decimal `--fov` colliding with a vanilla entry
is nudged one unit); stabiliser 0 accepted (cutout owner) -> launcher default 0 (59ae2dfe) -> superseded the same day by the
**mask fold** (80394271: no mask draw, the resolve computes the gates and writes the depth history as MRT2, sentinel class retired,
`--taa-sentinel-stabiliser` refused, screen search compiled but off by default = `--taa-thin-region-source vote` default, state
block stream fix; fixture TAA at rest 5.79 -> 3.93 ms against the 0.7 baseline, about -0.6 to -0.9 ms against the flown default
inferred; two reviews); vote-source A/B: no saving, and the Terran lattice crawl was not TAA at all: the coarse record's louvre
gaps/girders at 0.3-0.5 px -> **baker louvre recipe** (9a7de6f2, per-body, record 1 welded to flat plates, 3 draws/node) and
**install-fleet4** (f895a8c5, 620 bodies, 50 min) -> run321: crawl gone, accepted (6c8c82c1). Ad hoc: run319 defaults, run320
`--taa-thin-region 0` = a little shimmer under pan (rejected, the hold stays); **`--gpu-sync-timing` doubles the frame** (dt p50
30-33 ms with it vs 15-18 ms without, 7a1c68f3; the user plays at 50-60 fps at 5120x1440; quote fps only from launches without
it). **Window**: the game positions its WS_POPUP window at the work-area origin (0,31), under the macOS menu bar -> `--window-monitor-rect`
default on (SetWindowPos to the monitor rect at create/reset), opt-in `--window-trace` and `--cursor-reassert` for the double cursor
(6f122cfa, design window-mode-and-cursor-fix.md, two reviews; whether the menu bar hides and the cursor goes = flight). User
decisions: goal 1 True HDR closed (467aaab2); `--original-fill 0.02` launcher default on `--hdr` (4b5e9bb8). **Candidate**: Run83
from 80394271 qualifying in /tmp/x3-run83-candidate (agent; record run83-candidate-qualification.json), then the orchestrator
installs (records run83-candidate-install.json, status installed-build section, this section updated with the hash). **Run 83 A is
the only queued run** (user-runs.md: launch 1 defaults incl. the pre-patch save load, window, folded TAA look, fill, fps; launch 2
`--gpu-sync-timing` attribution; launch 3 `--window-trace --cursor-reassert` alt-tab recipe). Open: FOV save-side inverse map and the
cockpit +0x230 site; §5 condition 3 replay; ODS LOD pop hysteresis (diagnosed, no fix decided); native Windows (3-MRT mixed
formats, hooks) unverified; the user's shell exported `X3M_TAA_THIN_REGION_SOURCE=vote` and `X3M_TAA_SENTINEL_STABILISER=0`
through run321 (asked to unset). Rules that bit: a Bash background job dies at the 10-minute tool timeout (the fleet rebake was
restarted with nohup); zsh does not word-split unquoted variables (`${=a}`); a worktree agent may start from an older base, tell it
to fast-forward; the thin-vote launcher default needs `--sun-shadow-lane`, so a minimal `--taa` dry run shows no source variable.
