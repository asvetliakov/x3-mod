# Negative witness for fixture case n_route_lifecycle: the seam DLL built with main's acquisition shape
# (motion_output_sun_occlusion_inc.h acquire_lens_depth: the GetContainer outside taa_call), everything else the fix.
# Command (worktree agent-a03f4da4789dd5c14, 2026-09-25 19:40, DLL build/d3d9.dll of the fixed tree):
#   X3M_FIXTURE_BOTTLE=X3 python3 verification/probe/wine_lock.py python3 verification/probe/run_motion_output.py \
#     --dll build/d3d9.dll --seam verification/probe/build/motion-output-seam/d3d9.dll \
#     --fixture verification/probe/build/motion_output_fixture.exe seam-ownership-shadow-retention-live
# Runner: AssertionError('seam-ownership-shadow-retention-live: exit 1, traces 1')
# fixture-stdout.txt (case begins at frame 854; the sum 16 - k + 0 + 0 + 1 + 3 wraps at cycle k = 21, frame 886):
RETENTION_CASE name=n_route_lifecycle state=begin frame=854
RETENTION_WITNESS case=n_route_lifecycle stat=7 value=0 expected=2
CHECK the replay issues every kept record into both cascades FAIL
RESULT FAIL the replay issues every kept record into both cascades
# session log (the store flushed by the hook's final-Release probe, mid-session):
shadow_retention_flush device=1 frame=886 reason=teardown nodes=2 refs=3
shadow_retention_flush device=1 frame=886 reason=teardown nodes=1 refs=2
# With the fix (the same command, four cases): every setting passes; RETENTION_ROUTE_LIFECYCLE cycles=301 device_term_first=16 device_term_last=16 teardown_flushes=0
