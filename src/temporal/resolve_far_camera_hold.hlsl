// The camera-gate resolve with the region and closure holds of A' (the mask's tests target at s8, no dilation draws); see resolve.hlsl, X3M_REGION_HOLD.
#define X3M_REGION_HOLD 1
#include "resolve.hlsl"
