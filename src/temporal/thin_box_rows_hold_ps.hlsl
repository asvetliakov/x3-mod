// The separable box's row draw gated on the mask's tests target (A', --taa-region-hold on); see thin_box_rows_ps.hlsl, X3M_REGION_HOLD_MASK.
#define X3M_REGION_HOLD_MASK 1
#include "thin_box_rows_ps.hlsl"
